**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 6.7 手写防抖与节流

> 掌握 debounce / throttle 实现原理，含立即执行和取消功能

---

## 🎯 题目描述

实现防抖（debounce）和节流（throttle）函数：
1. **防抖**：延迟执行，频繁触发只执行最后一次
2. **节流**：固定频率执行，降低触发频率
3. 支持立即执行（leading/trailing）
4. 支持取消（进阶）

---

## 📋 核心考点

- ⭐⭐⭐ 闭包与定时器
- ⭐⭐⭐ this 绑定
- ⭐⭐ 高阶函数
- ⭐⭐ 时间戳与 Date.now()

---

## 💻 防抖（Debounce）实现

### 基础实现（60分）

```javascript
function debounce(func, wait) {
  let timeoutId;
  
  return function(...args) {
    clearTimeout(timeoutId);
    
    timeoutId = setTimeout(() => {
      func.apply(this, args);
    }, wait);
  };
}

// 使用
const handleInput = debounce((value) => {
  console.log('搜索:', value);
}, 500);

handleInput('a');  // 不执行
handleInput('ab');  // 不执行
handleInput('abc');  // 500ms后执行
```

**评分：60分**
- ✅ 基础防抖功能
- ❌ 无立即执行
- ❌ 无取消功能

---

### 完整实现（100分）

```javascript
/**
 * 防抖函数
 * @param {Function} func - 目标函数
 * @param {number} wait - 延迟时间（ms）
 * @param {Object} options - 配置
 * @param {boolean} options.leading - 是否立即执行
 * @param {boolean} options.trailing - 是否延迟执行
 */
function debounce(func, wait, options = {}) {
  const { leading = false, trailing = true } = options;
  
  let timeoutId;
  let lastCallTime;
  let lastInvokeTime = 0;
  let lastArgs;
  let lastThis;
  
  function invokeFunc(time) {
    const args = lastArgs;
    const thisArg = lastThis;
    
    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    
    return func.apply(thisArg, args);
  }
  
  function leadingEdge(time) {
    lastInvokeTime = time;
    timeoutId = setTimeout(timerExpired, wait);
    
    return leading ? invokeFunc(time) : undefined;
  }
  
  function timerExpired() {
    const time = Date.now();
    
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    
    // 重新设置定时器
    timeoutId = setTimeout(timerExpired, remainingWait(time));
  }
  
  function trailingEdge(time) {
    timeoutId = undefined;
    
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    
    lastArgs = lastThis = undefined;
    return undefined;
  }
  
  function shouldInvoke(time) {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    
    return (
      lastCallTime === undefined ||
      timeSinceLastCall >= wait ||
      timeSinceLastInvoke >= wait
    );
  }
  
  function remainingWait(time) {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    const timeWaiting = wait - timeSinceLastCall;
    
    return timeWaiting;
  }
  
  function debounced(...args) {
    const time = Date.now();
    const isInvoking = shouldInvoke(time);
    
    lastArgs = args;
    lastThis = this;
    lastCallTime = time;
    
    if (isInvoking) {
      if (timeoutId === undefined) {
        return leadingEdge(lastCallTime);
      }
    }
    
    if (timeoutId === undefined) {
      timeoutId = setTimeout(timerExpired, wait);
    }
    
    return undefined;
  }
  
  // 取消
  debounced.cancel = function() {
    if (timeoutId !== undefined) {
      clearTimeout(timeoutId);
    }
    
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timeoutId = undefined;
  };
  
  // 立即执行
  debounced.flush = function() {
    return timeoutId === undefined ? undefined : trailingEdge(Date.now());
  };
  
  return debounced;
}

// 使用示例
const handleInput = debounce((value) => {
  console.log('搜索:', value);
}, 500, { leading: true, trailing: true });

handleInput('a');    // 立即执行
handleInput('ab');   // 不执行
handleInput('abc');  // 500ms后执行

// 取消
handleInput.cancel();

// 立即执行
handleInput.flush();
```

---

### 简化版（90分，推荐）

```javascript
/**
 * 防抖函数（简化版）
 * @param {Function} func - 目标函数
 * @param {number} wait - 延迟时间
 * @param {boolean} immediate - 是否立即执行
 */
function debounce(func, wait, immediate = false) {
  let timeoutId;
  
  function debounced(...args) {
    const later = () => {
      timeoutId = null;
      if (!immediate) {
        func.apply(this, args);
      }
    };
    
    const callNow = immediate && !timeoutId;
    
    clearTimeout(timeoutId);
    timeoutId = setTimeout(later, wait);
    
    if (callNow) {
      func.apply(this, args);
    }
  }
  
  debounced.cancel = function() {
    clearTimeout(timeoutId);
    timeoutId = null;
  };
  
  return debounced;
}
```

---

## 💻 节流（Throttle）实现

### 基础实现（60分）

```javascript
function throttle(func, wait) {
  let lastTime = 0;
  
  return function(...args) {
    const now = Date.now();
    
    if (now - lastTime >= wait) {
      lastTime = now;
      func.apply(this, args);
    }
  };
}

// 使用
const handleScroll = throttle(() => {
  console.log('滚动中...');
}, 1000);

window.addEventListener('scroll', handleScroll);
```

**评分：60分**
- ✅ 基础节流功能
- ❌ 尾部调用缺失
- ❌ 无取消功能

---

### 完整实现（100分）

```javascript
/**
 * 节流函数
 * @param {Function} func - 目标函数
 * @param {number} wait - 间隔时间
 * @param {Object} options - 配置
 * @param {boolean} options.leading - 是否首次立即执行
 * @param {boolean} options.trailing - 是否尾部执行
 */
function throttle(func, wait, options = {}) {
  const { leading = true, trailing = true } = options;
  
  let timeoutId;
  let lastArgs;
  let lastThis;
  let lastCallTime;
  let lastInvokeTime = 0;
  
  function invokeFunc(time) {
    const args = lastArgs;
    const thisArg = lastThis;
    
    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    
    return func.apply(thisArg, args);
  }
  
  function leadingEdge(time) {
    lastInvokeTime = time;
    timeoutId = setTimeout(timerExpired, wait);
    
    return leading ? invokeFunc(time) : undefined;
  }
  
  function remainingWait(time) {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    const timeWaiting = wait - timeSinceLastInvoke;
    
    return timeWaiting;
  }
  
  function shouldInvoke(time) {
    const timeSinceLastCall = time - lastCallTime;
    const timeSinceLastInvoke = time - lastInvokeTime;
    
    return (
      lastCallTime === undefined ||
      timeSinceLastCall >= wait ||
      timeSinceLastInvoke >= wait
    );
  }
  
  function timerExpired() {
    const time = Date.now();
    
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    
    timeoutId = setTimeout(timerExpired, remainingWait(time));
  }
  
  function trailingEdge(time) {
    timeoutId = undefined;
    
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    
    lastArgs = lastThis = undefined;
    return undefined;
  }
  
  function throttled(...args) {
    const time = Date.now();
    const isInvoking = shouldInvoke(time);
    
    lastArgs = args;
    lastThis = this;
    lastCallTime = time;
    
    if (isInvoking) {
      if (timeoutId === undefined) {
        return leadingEdge(lastCallTime);
      }
      
      timeoutId = setTimeout(timerExpired, wait);
      return invokeFunc(lastCallTime);
    }
    
    if (timeoutId === undefined) {
      timeoutId = setTimeout(timerExpired, wait);
    }
    
    return undefined;
  }
  
  throttled.cancel = function() {
    if (timeoutId !== undefined) {
      clearTimeout(timeoutId);
    }
    
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timeoutId = undefined;
  };
  
  return throttled;
}
```

---

### 简化版（90分，推荐）

```javascript
/**
 * 节流函数（简化版）
 * @param {Function} func - 目标函数
 * @param {number} wait - 间隔时间
 */
function throttle(func, wait) {
  let timeoutId;
  let lastTime = 0;
  
  function throttled(...args) {
    const now = Date.now();
    const remaining = wait - (now - lastTime);
    
    if (remaining <= 0 || remaining > wait) {
      if (timeoutId) {
        clearTimeout(timeoutId);
        timeoutId = null;
      }
      
      lastTime = now;
      func.apply(this, args);
    } else if (!timeoutId) {
      timeoutId = setTimeout(() => {
        lastTime = Date.now();
        timeoutId = null;
        func.apply(this, args);
      }, remaining);
    }
  }
  
  throttled.cancel = function() {
    clearTimeout(timeoutId);
    timeoutId = null;
    lastTime = 0;
  };
  
  return throttled;
}
```

---

## 🧪 测试用例

```javascript
// 测试防抖
function testDebounce() {
  let count = 0;
  
  const debounced = debounce(() => {
    count++;
  }, 100);
  
  debounced();
  debounced();
  debounced();
  
  setTimeout(() => {
    console.log('防抖执行次数:', count);  // 应该为 1
  }, 200);
}

// 测试节流
function testThrottle() {
  let count = 0;
  
  const throttled = throttle(() => {
    count++;
  }, 100);
  
  const interval = setInterval(() => {
    throttled();
  }, 10);
  
  setTimeout(() => {
    clearInterval(interval);
    console.log('节流执行次数:', count);  // 约 3次（300ms / 100ms）
  }, 300);
}

// 测试立即执行
function testImmediate() {
  const debounced = debounce(() => {
    console.log('立即执行:', Date.now());
  }, 1000, true);
  
  debounced();  // 立即执行
  debounced();  // 不执行
  debounced();  // 不执行
}

// 测试取消
function testCancel() {
  const debounced = debounce(() => {
    console.log('应该不会执行');
  }, 1000);
  
  debounced();
  debounced.cancel();  // 取消
}

testDebounce();
testThrottle();
testImmediate();
testCancel();
```

---

## 📊 评分标准

| 分数 | 实现要求 | 关键点 |
|------|---------|--------|
| **60分** | 基础防抖/节流 | clearTimeout / 时间戳 |
| **70分** | + this 绑定 | apply(this, args) |
| **80分** | + 立即执行 | immediate 参数 |
| **90分** | + 取消功能 | cancel 方法 |
| **100分** | + leading/trailing | 完整配置 |

---

## ⚠️ 常见错误

### 错误1：this 绑定错误

```javascript
// ❌ 错误
function debounce(func, wait) {
  let timeoutId;
  
  return function(...args) {
    clearTimeout(timeoutId);
    
    timeoutId = setTimeout(() => {
      func(...args);  // this 丢失
    }, wait);
  };
}

// ✅ 正确
return function(...args) {
  clearTimeout(timeoutId);
  
  timeoutId = setTimeout(() => {
    func.apply(this, args);  // 绑定 this
  }, wait);
};
```

### 错误2：节流首次不执行

```javascript
// ❌ 错误（首次需要等待）
function throttle(func, wait) {
  let lastTime = Date.now();  // 初始化为当前时间
  
  return function(...args) {
    const now = Date.now();
    
    if (now - lastTime >= wait) {
      // ...
    }
  };
}

// ✅ 正确（首次立即执行）
let lastTime = 0;  // 初始化为 0
```

### 错误3：忘记清理定时器

```javascript
// ❌ 错误（内存泄漏）
function debounce(func, wait) {
  let timeoutId;
  
  return function(...args) {
    // 忘记 clearTimeout
    timeoutId = setTimeout(() => {
      func.apply(this, args);
    }, wait);
  };
}

// ✅ 正确
clearTimeout(timeoutId);
timeoutId = setTimeout(...);
```

---

## 🎯 面试技巧

### 答题流程（15-20分钟）

**1. 先问需求（1分钟）**
> "需要实现防抖还是节流，还是两个都要？"  
> "需要立即执行吗？"  
> "需要取消功能吗？"

**2. 说明原理（2分钟）**

**防抖：**
> "延迟执行，每次触发重置定时器。"  
> "频繁触发只执行最后一次。"

**节流：**
> "固定频率执行，用时间戳控制。"  
> "每 N 毫秒最多执行一次。"

**3. 写实现（10分钟）**

**4. 写测试用例（3分钟）**

**5. 说明应用场景（2分钟）**

### 加分项
- 提到 lodash 实现
- 讨论 requestAnimationFrame 节流
- 讨论 React 中的使用（useCallback）

---

## 📝 应用场景

### 防抖场景
- 搜索框输入（等待用户输入完成）
- 表单验证（延迟验证）
- 窗口 resize（停止调整后执行）
- 按钮防重复点击

### 节流场景
- 滚动加载（scroll）
- 鼠标移动（mousemove）
- 拖拽（drag）
- 动画（requestAnimationFrame）

---

## 💡 总结

### 核心要点
1. **防抖**：延迟执行，重置定时器
2. **节流**：固定频率，时间戳控制
3. **this 绑定**：apply(this, args)
4. **取消功能**：clearTimeout

### 实现关键

**防抖：**
```javascript
function debounce(func, wait, immediate) {
  let timeoutId;
  
  return function(...args) {
    const callNow = immediate && !timeoutId;
    
    clearTimeout(timeoutId);
    timeoutId = setTimeout(() => {
      timeoutId = null;
      if (!immediate) {
        func.apply(this, args);
      }
    }, wait);
    
    if (callNow) {
      func.apply(this, args);
    }
  };
}
```

**节流：**
```javascript
function throttle(func, wait) {
  let lastTime = 0;
  let timeoutId;
  
  return function(...args) {
    const now = Date.now();
    const remaining = wait - (now - lastTime);
    
    if (remaining <= 0) {
      clearTimeout(timeoutId);
      lastTime = now;
      func.apply(this, args);
    } else if (!timeoutId) {
      timeoutId = setTimeout(() => {
        lastTime = Date.now();
        timeoutId = null;
        func.apply(this, args);
      }, remaining);
    }
  };
}
```

### 面试重点
- ✅ 防抖 vs 节流区别
- ✅ this 绑定
- ✅ 闭包应用
- ✅ 应用场景

### 注意事项
- ⚠️ this 绑定要用 apply
- ⚠️ 清理定时器
- ⚠️ 立即执行的实现
- ⚠️ 尾部调用的处理

---

**下一题：** [6.8 实现 LRU 缓存](./6.8-LRU缓存.md)
