**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 13.4 错误统一处理

> 自定义错误类、全局错误处理、异步错误捕获、错误响应格式化、错误日志

---

## 📖 知识点概述

### 核心概念

**统一错误处理**是将应用中的所有错误按照统一的格式处理和响应，提高代码可维护性，改善用户体验。

### 为什么需要统一错误处理？

```
❌ 不统一的错误处理：

app.get('/users/:id', async (req, res) => {
  const user = await User.findById(req.params.id);
  if (!user) {
    return res.status(404).send('User not found');
  }
  res.json(user);
});

app.post('/users', async (req, res) => {
  if (!req.body.email) {
    return res.status(400).json({ message: 'Email is required' });
  }
  // ...
});

问题：
- 错误格式不一致
- 重复的错误处理代码
- 难以维护和修改
- 无法统一记录日志

✅ 统一错误处理：

app.get('/users/:id', async (req, res) => {
  const user = await User.findById(req.params.id);
  if (!user) {
    throw new NotFoundError('User not found');
  }
  res.json(user);
});

// 全局错误处理中间件
app.use(errorHandler);

优点：
- 错误格式统一
- 代码简洁
- 易于维护
- 自动记录日志
```

---

## 🚀 使用指南

### 实践1：自定义错误类

```typescript
// utils/errors.ts

// 💡 基础错误类
export class ApiError extends Error {
  constructor(
    public statusCode: number,
    public code: string,
    message: string,
    public isOperational: boolean = true
  ) {
    super(message);
    this.name = this.constructor.name;
    Error.captureStackTrace(this, this.constructor);
  }
}

// 💡 具体错误类
export class BadRequestError extends ApiError {
  constructor(message: string = 'Bad request') {
    super(400, 'BAD_REQUEST', message);
  }
}

export class UnauthorizedError extends ApiError {
  constructor(message: string = 'Unauthorized') {
    super(401, 'UNAUTHORIZED', message);
  }
}

export class ForbiddenError extends ApiError {
  constructor(message: string = 'Forbidden') {
    super(403, 'FORBIDDEN', message);
  }
}

export class NotFoundError extends ApiError {
  constructor(message: string = 'Resource not found') {
    super(404, 'NOT_FOUND', message);
  }
}

export class ConflictError extends ApiError {
  constructor(message: string = 'Resource conflict') {
    super(409, 'CONFLICT', message);
  }
}

export class ValidationError extends ApiError {
  constructor(
    message: string = 'Validation failed',
    public errors?: any[]
  ) {
    super(422, 'VALIDATION_ERROR', message);
  }
}

export class InternalServerError extends ApiError {
  constructor(message: string = 'Internal server error') {
    super(500, 'INTERNAL_SERVER_ERROR', message, false);
  }
}

// 使用
import { NotFoundError, ConflictError } from '../utils/errors';

export class UserService {
  async findById(id: string) {
    const user = await this.userRepository.findById(id);
    
    if (!user) {
      throw new NotFoundError('User not found');
    }
    
    return user;
  }
  
  async createUser(data: CreateUserDto) {
    const existing = await this.userRepository.findByEmail(data.email);
    
    if (existing) {
      throw new ConflictError('Email already exists');
    }
    
    return this.userRepository.create(data);
  }
}
```

### 实践2：全局错误处理中间件

```typescript
// middleware/error.middleware.ts
import { Request, Response, NextFunction } from 'express';
import { ApiError } from '../utils/errors';
import { logger } from '../utils/logger';

// 💡 错误响应格式
interface ErrorResponse {
  success: false;
  error: {
    code: string;
    message: string;
    errors?: any[];
    stack?: string;
  };
}

// 💡 全局错误处理中间件
export function errorHandler(
  err: Error,
  req: Request,
  res: Response,
  next: NextFunction
) {
  // 记录错误日志
  logger.error({
    err,
    req: {
      id: req.id,
      method: req.method,
      url: req.url,
      headers: req.headers,
      body: req.body
    }
  }, 'Request error');
  
  // 💡 处理ApiError
  if (err instanceof ApiError) {
    const response: ErrorResponse = {
      success: false,
      error: {
        code: err.code,
        message: err.message
      }
    };
    
    // 添加验证错误详情
    if (err instanceof ValidationError && err.errors) {
      response.error.errors = err.errors;
    }
    
    // 开发环境返回堆栈
    if (process.env.NODE_ENV === 'development') {
      response.error.stack = err.stack;
    }
    
    return res.status(err.statusCode).json(response);
  }
  
  // 💡 处理MongoDB错误
  if (err.name === 'CastError') {
    return res.status(400).json({
      success: false,
      error: {
        code: 'INVALID_ID',
        message: 'Invalid ID format'
      }
    });
  }
  
  if (err.name === 'ValidationError') {
    return res.status(422).json({
      success: false,
      error: {
        code: 'VALIDATION_ERROR',
        message: 'Validation failed',
        errors: Object.values((err as any).errors).map((e: any) => ({
          field: e.path,
          message: e.message
        }))
      }
    });
  }
  
  // 💡 处理JWT错误
  if (err.name === 'JsonWebTokenError') {
    return res.status(401).json({
      success: false,
      error: {
        code: 'INVALID_TOKEN',
        message: 'Invalid token'
      }
    });
  }
  
  if (err.name === 'TokenExpiredError') {
    return res.status(401).json({
      success: false,
      error: {
        code: 'TOKEN_EXPIRED',
        message: 'Token expired'
      }
    });
  }
  
  // 💡 未知错误
  res.status(500).json({
    success: false,
    error: {
      code: 'INTERNAL_SERVER_ERROR',
      message: process.env.NODE_ENV === 'development' 
        ? err.message 
        : 'An unexpected error occurred',
      ...(process.env.NODE_ENV === 'development' && { stack: err.stack })
    }
  });
}

// 💡 404错误处理
export function notFoundHandler(req: Request, res: Response) {
  res.status(404).json({
    success: false,
    error: {
      code: 'NOT_FOUND',
      message: `Cannot ${req.method} ${req.path}`
    }
  });
}

// app.ts
import express from 'express';
import { errorHandler, notFoundHandler } from './middleware/error.middleware';

const app = express();

// 路由...

// 💡 404处理（放在所有路由后面）
app.use(notFoundHandler);

// 💡 错误处理（放在最后）
app.use(errorHandler);

export default app;
```

### 实践3：异步错误捕获

#### 方式1：使用async/await + try/catch

```typescript
// controllers/user.controller.ts
export class UserController {
  async create(req: Request, res: Response, next: NextFunction) {
    try {
      const dto: CreateUserDto = req.body;
      const user = await this.userService.createUser(dto);
      
      res.status(201).json({
        success: true,
        data: user
      });
    } catch (error) {
      next(error); // 💡 传递给错误处理中间件
    }
  }
}
```

#### 方式2：使用asyncHandler包装器

```typescript
// utils/async-handler.ts
import { Request, Response, NextFunction } from 'express';

// 💡 异步路由处理器包装器
export function asyncHandler(
  fn: (req: Request, res: Response, next: NextFunction) => Promise<any>
) {
  return (req: Request, res: Response, next: NextFunction) => {
    Promise.resolve(fn(req, res, next)).catch(next);
  };
}

// controllers/user.controller.ts
import { asyncHandler } from '../utils/async-handler';

export class UserController {
  // 💡 不需要try/catch
  create = asyncHandler(async (req, res) => {
    const dto: CreateUserDto = req.body;
    const user = await this.userService.createUser(dto);
    
    res.status(201).json({
      success: true,
      data: user
    });
  });
  
  findAll = asyncHandler(async (req, res) => {
    const users = await this.userService.findAll();
    res.json({ success: true, data: users });
  });
}
```

#### 方式3：使用express-async-errors

```bash
npm install express-async-errors
```

```typescript
// app.ts
import 'express-async-errors'; // 💡 自动捕获async错误

import express from 'express';

const app = express();

// 💡 不需要try/catch或asyncHandler
app.get('/users/:id', async (req, res) => {
  const user = await User.findById(req.params.id);
  
  if (!user) {
    throw new NotFoundError('User not found');
  }
  
  res.json({ success: true, data: user });
});

// 错误处理中间件
app.use(errorHandler);
```

### 实践4：参数验证错误

```typescript
// middleware/validate.middleware.ts
import { Request, Response, NextFunction } from 'express';
import { AnyZodObject, ZodError } from 'zod';
import { ValidationError } from '../utils/errors';

// 💡 使用Zod验证
export function validate(schema: AnyZodObject) {
  return (req: Request, res: Response, next: NextFunction) => {
    try {
      schema.parse({
        body: req.body,
        query: req.query,
        params: req.params
      });
      
      next();
    } catch (error) {
      if (error instanceof ZodError) {
        const errors = error.errors.map(err => ({
          field: err.path.join('.'),
          message: err.message
        }));
        
        next(new ValidationError('Validation failed', errors));
      } else {
        next(error);
      }
    }
  };
}

// dtos/user.dto.ts
import { z } from 'zod';

export const createUserSchema = z.object({
  body: z.object({
    email: z.string().email('Invalid email format'),
    password: z.string().min(8, 'Password must be at least 8 characters'),
    name: z.string().min(1, 'Name is required')
  })
});

// routes/user.routes.ts
import { validate } from '../middleware/validate.middleware';
import { createUserSchema } from '../dtos/user.dto';

router.post('/users', validate(createUserSchema), asyncHandler(async (req, res) => {
  // 参数已验证，可以安全使用
  const user = await userService.createUser(req.body);
  res.status(201).json({ success: true, data: user });
}));
```

---

## 🔧 错误处理

### 高频错误1：未捕获的异常

**问题：**
```typescript
// 异步操作未捕获
setTimeout(() => {
  throw new Error('Uncaught error');
}, 1000);

// Promise拒绝未处理
Promise.reject(new Error('Unhandled rejection'));
```

**解决方案：**
```typescript
// server.ts

// 💡 捕获未捕获的异常
process.on('uncaughtException', (err) => {
  logger.fatal({ err }, 'Uncaught exception');
  
  // 优雅关闭
  process.exit(1);
});

// 💡 捕获未处理的Promise拒绝
process.on('unhandledRejection', (reason, promise) => {
  logger.fatal({ reason, promise }, 'Unhandled rejection');
  
  // 优雅关闭
  process.exit(1);
});

// 💡 优雅关闭
function gracefulShutdown(signal: string) {
  logger.info(`Received ${signal}, shutting down gracefully`);
  
  server.close(() => {
    logger.info('Server closed');
    process.exit(0);
  });
  
  // 强制关闭超时
  setTimeout(() => {
    logger.error('Forced shutdown');
    process.exit(1);
  }, 10000);
}

process.on('SIGTERM', () => gracefulShutdown('SIGTERM'));
process.on('SIGINT', () => gracefulShutdown('SIGINT'));
```

### 高频错误2：错误信息泄露

**问题：**
```typescript
// ❌ 生产环境暴露详细错误
res.status(500).json({
  error: err.message,
  stack: err.stack
});
```

**解决方案：**
```typescript
// ✅ 区分环境
export function errorHandler(err: Error, req: Request, res: Response, next: NextFunction) {
  const isDevelopment = process.env.NODE_ENV === 'development';
  
  // 💡 ApiError返回详细信息
  if (err instanceof ApiError && err.isOperational) {
    return res.status(err.statusCode).json({
      success: false,
      error: {
        code: err.code,
        message: err.message,
        ...(isDevelopment && { stack: err.stack })
      }
    });
  }
  
  // 💡 非操作性错误（未知错误）
  res.status(500).json({
    success: false,
    error: {
      code: 'INTERNAL_SERVER_ERROR',
      message: isDevelopment ? err.message : 'An unexpected error occurred',
      ...(isDevelopment && { stack: err.stack })
    }
  });
}
```

### 高频错误3：错误日志不完整

**问题：**
```typescript
// ❌ 日志信息不足
logger.error('Error occurred');
```

**解决方案：**
```typescript
// ✅ 完整的错误日志
logger.error({
  err,                     // 错误对象（包含堆栈）
  req: {
    id: req.id,            // 请求ID
    method: req.method,    // HTTP方法
    url: req.url,          // 请求URL
    headers: req.headers,  // 请求头
    body: req.body,        // 请求体
    userId: req.user?.id   // 用户ID（如果已认证）
  },
  context: {
    service: 'UserService',
    method: 'createUser'
  }
}, 'Request error');
```

---

## 📚 应用场景

### 场景：完整的错误处理系统

```typescript
// utils/errors.ts
export class ApiError extends Error {
  constructor(
    public statusCode: number,
    public code: string,
    message: string,
    public isOperational: boolean = true,
    public metadata?: Record<string, any>
  ) {
    super(message);
    this.name = this.constructor.name;
    Error.captureStackTrace(this, this.constructor);
  }
}

export class NotFoundError extends ApiError {
  constructor(resource: string = 'Resource', id?: string) {
    const message = id 
      ? `${resource} with ID ${id} not found`
      : `${resource} not found`;
    super(404, 'NOT_FOUND', message);
  }
}

export class ValidationError extends ApiError {
  constructor(errors: Array<{ field: string; message: string }>) {
    super(
      422,
      'VALIDATION_ERROR',
      'Validation failed',
      true,
      { errors }
    );
  }
}

// middleware/error.middleware.ts
export function errorHandler(err: Error, req: Request, res: Response, next: NextFunction) {
  // 记录错误
  const logLevel = err instanceof ApiError && err.statusCode < 500 ? 'warn' : 'error';
  
  logger[logLevel]({
    err,
    req: {
      id: req.id,
      method: req.method,
      url: req.url,
      ip: req.ip,
      userId: req.user?.id
    }
  }, 'Request error');
  
  // 错误监控（Sentry、Bugsnag等）
  if (!(err instanceof ApiError && err.isOperational)) {
    // 上报未预期的错误
    // Sentry.captureException(err);
  }
  
  // 返回响应
  if (err instanceof ApiError) {
    return res.status(err.statusCode).json({
      success: false,
      error: {
        code: err.code,
        message: err.message,
        ...(err.metadata && { ...err.metadata }),
        ...(process.env.NODE_ENV === 'development' && { stack: err.stack })
      }
    });
  }
  
  // 未知错误
  res.status(500).json({
    success: false,
    error: {
      code: 'INTERNAL_SERVER_ERROR',
      message: process.env.NODE_ENV === 'development' 
        ? err.message 
        : 'An unexpected error occurred'
    }
  });
}

// services/user.service.ts
import { NotFoundError, ConflictError, ValidationError } from '../utils/errors';

export class UserService {
  async findById(id: string) {
    // 验证ID格式
    if (!isValidObjectId(id)) {
      throw new ValidationError([{
        field: 'id',
        message: 'Invalid ID format'
      }]);
    }
    
    const user = await this.userRepository.findById(id);
    
    if (!user) {
      throw new NotFoundError('User', id);
    }
    
    return user;
  }
  
  async createUser(dto: CreateUserDto) {
    const existing = await this.userRepository.findByEmail(dto.email);
    
    if (existing) {
      throw new ConflictError('Email already exists');
    }
    
    try {
      return await this.userRepository.create(dto);
    } catch (error) {
      // 数据库错误
      logger.error({ err: error }, 'Failed to create user');
      throw new InternalServerError('Failed to create user');
    }
  }
}
```

---

## 📋 总结

### 核心要点

1. **自定义错误类**：ApiError基类 + 具体错误类
2. **全局错误处理**：errorHandler中间件
3. **异步错误捕获**：asyncHandler包装器
4. **参数验证**：Zod/Joi验证 + ValidationError

### 实现关键

1. **ApiError**: 继承Error，添加statusCode和code
2. **errorHandler**: 统一处理错误，格式化响应
3. **asyncHandler**: 自动捕获async/await错误
4. **日志记录**: 记录完整的错误上下文

### 应用价值

- **一致性**：统一的错误格式
- **可维护性**：集中处理，易于修改
- **可观测性**：完整的错误日志
- **用户体验**：友好的错误提示

### 面试重点

**Q1：如何实现统一错误处理？**
答：1) 自定义ApiError类 2) 全局errorHandler中间件 3) asyncHandler捕获异步错误 4) 统一错误响应格式。

**Q2：如何捕获异步错误？**
答：1) try/catch 2) asyncHandler包装器 3) express-async-errors库 4) Promise.catch()。

**Q3：如何区分操作性错误和编程错误？**
答：ApiError.isOperational标识。操作性错误（用户输入错误）正常响应；编程错误（bug）记录日志、上报监控、返回500。

**Q4：生产环境如何处理错误？**
答：1) 不暴露敏感信息 2) 记录完整日志 3) 上报错误监控 4) 返回友好提示。

### 注意事项

⚠️ **不要泄露敏感信息**：生产环境隐藏堆栈
⚠️ **记录完整上下文**：请求信息、用户ID等
⚠️ **区分错误类型**：操作性 vs 编程错误
⚠️ **优雅关闭**：捕获uncaughtException

---

<div align="center">

[⬅️ 上一节：日志集中处理](./13.3-日志集中处理.md) | [返回章节目录](./章节目录.md) | [下一节：Git提交规范 ➡️](./13.5-Git提交规范.md)

</div>

