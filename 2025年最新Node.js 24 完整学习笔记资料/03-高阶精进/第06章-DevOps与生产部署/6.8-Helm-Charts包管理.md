**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 6.8 Helm Charts 包管理与应用模板化

> Kubernetes 的包管理器：简化部署、版本管理、多环境配置

---

## 📚 知识导读

### 什么是 Helm？

**Helm** 是 Kubernetes 的包管理器，类似于：
- **npm/yarn** for Node.js
- **pip** for Python
- **apt/yum** for Linux

**原生 YAML vs Helm Chart：**

```
┌────────────────────────────────────────────────────────┐
│           原生 Kubernetes YAML                          │
├────────────────────────────────────────────────────────┤
│                                                        │
│  部署一个应用需要：                                    │
│  - deployment.yaml                                     │
│  - service.yaml                                        │
│  - ingress.yaml                                        │
│  - configmap.yaml                                      │
│  - secret.yaml                                         │
│  - hpa.yaml                                            │
│                                                        │
│  ❌ 问题：                                             │
│  - 需要手动管理6个文件                                 │
│  - 重复代码（dev/staging/prod 三份）                  │
│  - 难以升级和回滚                                      │
│  - 无版本管理                                          │
│                                                        │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│              Helm Chart                                │
├────────────────────────────────────────────────────────┤
│                                                        │
│  my-app/                                               │
│  ├── Chart.yaml        (Chart 元数据)                  │
│  ├── values.yaml       (默认配置)                      │
│  ├── values-dev.yaml   (开发环境配置)                  │
│  ├── values-prod.yaml  (生产环境配置)                  │
│  └── templates/                                        │
│      ├── deployment.yaml                               │
│      ├── service.yaml                                  │
│      ├── ingress.yaml                                  │
│      └── ...                                           │
│                                                        │
│  ✅ 优势：                                             │
│  - 一键部署：helm install my-app ./my-app             │
│  - 多环境：helm install -f values-prod.yaml           │
│  - 版本管理：helm upgrade、helm rollback               │
│  - 可复用：发布到 Chart 仓库                          │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 🎯 Helm 安装与基础

### 1. 安装 Helm

```bash
# macOS
brew install helm

# Windows
choco install kubernetes-helm

# Linux
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

# 验证安装
helm version
# version.BuildInfo{Version:"v3.14.0", GitCommit:"...", GitTreeState:"clean", GoVersion:"go1.21.0"}
```

---

### 2. Helm 核心概念

| 概念 | 说明 | 类比 |
|------|------|------|
| **Chart** | Helm 包，包含部署应用所需的所有资源定义 | npm package |
| **Release** | Chart 的一个部署实例 | 已安装的 npm package |
| **Repository** | Chart 仓库 | npm registry |
| **Values** | 配置参数 | package.json |

**基本命令：**

```bash
# 搜索 Chart
helm search repo nginx

# 安装 Chart
helm install my-release bitnami/nginx

# 列出 Release
helm list

# 升级 Release
helm upgrade my-release bitnami/nginx

# 回滚 Release
helm rollback my-release 1

# 卸载 Release
helm uninstall my-release

# 查看 Release 历史
helm history my-release
```

---

## 💻 创建 Helm Chart

### 1. 初始化 Chart

```bash
# 创建 Chart 骨架
helm create my-app

# 生成的目录结构
my-app/
├── Chart.yaml          # Chart 元数据
├── values.yaml         # 默认配置值
├── charts/             # 依赖的 Chart
├── templates/          # Kubernetes 资源模板
│   ├── deployment.yaml
│   ├── service.yaml
│   ├── ingress.yaml
│   ├── _helpers.tpl   # 辅助模板函数
│   └── NOTES.txt      # 安装后的提示信息
└── .helmignore         # 忽略文件
```

---

### 2. Chart.yaml

```yaml
# Chart.yaml
apiVersion: v2
name: my-app
description: A Node.js API application
type: application
version: 1.0.0  # Chart 版本
appVersion: "1.0.0"  # 应用版本

keywords:
  - nodejs
  - api
  - express

maintainers:
  - name: Your Name
    email: your-email@example.com
    url: https://github.com/yourname

icon: https://example.com/icon.png
home: https://example.com
sources:
  - https://github.com/yourname/my-app

# 依赖其他 Chart
dependencies:
  - name: mongodb
    version: "13.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: mongodb.enabled
  - name: redis
    version: "18.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled
```

---

### 3. values.yaml（默认配置）

```yaml
# values.yaml
replicaCount: 3

image:
  repository: my-app
  pullPolicy: IfNotPresent
  tag: "1.0.0"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1001
  fsGroup: 1001

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
    - ALL

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  hosts:
    - host: api.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: api-example-com-tls
      hosts:
        - api.example.com

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity: {}

# 应用配置
env:
  NODE_ENV: production
  PORT: "3000"
  LOG_LEVEL: info

# 依赖服务
mongodb:
  enabled: true
  auth:
    enabled: true
    username: appuser
    password: changeme
    database: myapp

redis:
  enabled: true
  auth:
    enabled: true
    password: changeme
```

---

### 4. templates/deployment.yaml

```yaml
# templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "my-app.fullname" . }}
  labels:
    {{- include "my-app.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "my-app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "my-app.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "my-app.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.targetPort }}
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        env:
        {{- range $key, $value := .Values.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        {{- if .Values.mongodb.enabled }}
        - name: MONGODB_URI
          value: "mongodb://{{ .Values.mongodb.auth.username }}:{{ .Values.mongodb.auth.password }}@{{ .Release.Name }}-mongodb:27017/{{ .Values.mongodb.auth.database }}"
        {{- end }}
        {{- if .Values.redis.enabled }}
        - name: REDIS_URL
          value: "redis://:{{ .Values.redis.auth.password }}@{{ .Release.Name }}-redis-master:6379"
        {{- end }}
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
```

---

### 5. templates/_helpers.tpl（辅助函数）

```yaml
{{/*
Expand the name of the chart.
*/}}
{{- define "my-app.name" -}}
{{- default .Chart.Name .Values.nameOverride | trunc 63 | trimSuffix "-" }}
{{- end }}

{{/*
Create a default fully qualified app name.
*/}}
{{- define "my-app.fullname" -}}
{{- if .Values.fullnameOverride }}
{{- .Values.fullnameOverride | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- $name := default .Chart.Name .Values.nameOverride }}
{{- if contains $name .Release.Name }}
{{- .Release.Name | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- printf "%s-%s" .Release.Name $name | trunc 63 | trimSuffix "-" }}
{{- end }}
{{- end }}
{{- end }}

{{/*
Common labels
*/}}
{{- define "my-app.labels" -}}
helm.sh/chart: {{ include "my-app.chart" . }}
{{ include "my-app.selectorLabels" . }}
{{- if .Chart.AppVersion }}
app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
{{- end }}
app.kubernetes.io/managed-by: {{ .Release.Service }}
{{- end }}

{{/*
Selector labels
*/}}
{{- define "my-app.selectorLabels" -}}
app.kubernetes.io/name: {{ include "my-app.name" . }}
app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}

{{/*
Create chart name and version as used by the chart label.
*/}}
{{- define "my-app.chart" -}}
{{- printf "%s-%s" .Chart.Name .Chart.Version | replace "+" "_" | trunc 63 | trimSuffix "-" }}
{{- end }}

{{/*
Create the name of the service account to use
*/}}
{{- define "my-app.serviceAccountName" -}}
{{- if .Values.serviceAccount.create }}
{{- default (include "my-app.fullname" .) .Values.serviceAccount.name }}
{{- else }}
{{- default "default" .Values.serviceAccount.name }}
{{- end }}
{{- end }}
```

---

## 💻 多环境配置

### 1. 开发环境配置

```yaml
# values-dev.yaml
replicaCount: 1

image:
  tag: "latest"
  pullPolicy: Always

ingress:
  hosts:
    - host: api-dev.example.com
      paths:
        - path: /
          pathType: Prefix

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false

env:
  NODE_ENV: development
  LOG_LEVEL: debug

mongodb:
  enabled: true
  auth:
    password: dev-password

redis:
  enabled: true
  auth:
    password: dev-password
```

---

### 2. 生产环境配置

```yaml
# values-prod.yaml
replicaCount: 5

image:
  tag: "1.0.0"
  pullPolicy: IfNotPresent

ingress:
  hosts:
    - host: api.example.com
      paths:
        - path: /
          pathType: Prefix

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 5
  maxReplicas: 20

env:
  NODE_ENV: production
  LOG_LEVEL: warn

mongodb:
  enabled: false  # 使用外部托管数据库
  externalUri: "mongodb://prod-db:27017/myapp"

redis:
  enabled: false  # 使用外部托管 Redis
  externalUrl: "redis://prod-redis:6379"
```

---

### 3. 部署不同环境

```bash
# 开发环境
helm install my-app-dev ./my-app \
  -f my-app/values-dev.yaml \
  --namespace dev \
  --create-namespace

# 生产环境
helm install my-app-prod ./my-app \
  -f my-app/values-prod.yaml \
  --namespace production \
  --create-namespace

# 查看所有 Release
helm list --all-namespaces
```

---

## 💻 Chart 依赖管理

### 1. 添加依赖

```yaml
# Chart.yaml
dependencies:
  - name: mongodb
    version: "13.6.4"
    repository: https://charts.bitnami.com/bitnami
  
  - name: redis
    version: "18.5.0"
    repository: https://charts.bitnami.com/bitnami
```

**更新依赖：**

```bash
# 下载依赖 Chart
helm dependency update ./my-app

# 查看依赖
helm dependency list ./my-app
# NAME      VERSION   REPOSITORY                              STATUS
# mongodb   13.6.4    https://charts.bitnami.com/bitnami      ok
# redis     18.5.0    https://charts.bitnami.com/bitnami      ok
```

---

### 2. 条件依赖

```yaml
# Chart.yaml
dependencies:
  - name: mongodb
    version: "13.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: mongodb.enabled  # 根据 values.yaml 中的值决定是否启用

  - name: redis
    version: "18.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled
```

```yaml
# values.yaml
mongodb:
  enabled: true  # 启用 MongoDB

redis:
  enabled: false  # 禁用 Redis
```

---

## 💻 Helm Hooks（生命周期钩子）

**Hooks 允许在特定时间点执行操作：**

| Hook | 执行时机 |
|------|---------|
| `pre-install` | 安装前 |
| `post-install` | 安装后 |
| `pre-upgrade` | 升级前 |
| `post-upgrade` | 升级后 |
| `pre-delete` | 删除前 |
| `post-delete` | 删除后 |
| `pre-rollback` | 回滚前 |
| `post-rollback` | 回滚后 |

**示例：数据库迁移 Hook**

```yaml
# templates/hooks/db-migration.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "my-app.fullname" . }}-db-migration
  labels:
    {{- include "my-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-upgrade,pre-install
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  template:
    metadata:
      name: {{ include "my-app.fullname" . }}-db-migration
    spec:
      restartPolicy: Never
      containers:
      - name: db-migration
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        command:
        - npm
        - run
        - migrate
        env:
        - name: MONGODB_URI
          value: "{{ .Values.mongodb.uri }}"
```

**Hook 权重（执行顺序）：**

```yaml
annotations:
  "helm.sh/hook-weight": "-5"  # 权重越小越先执行
```

**Hook 删除策略：**

```yaml
annotations:
  "helm.sh/hook-delete-policy": before-hook-creation  # 创建新 Hook 前删除旧的
  # 其他选项：
  # - hook-succeeded: Hook 成功后删除
  # - hook-failed: Hook 失败后删除
```

---

## 💻 Chart 测试

### 1. Lint 检查

```bash
# 检查 Chart 语法
helm lint ./my-app

# 输出
==> Linting ./my-app
[INFO] Chart.yaml: icon is recommended
[ERROR] templates/: render error in "my-app/templates/deployment.yaml": template: my-app/templates/deployment.yaml:10:14: executing "my-app/templates/deployment.yaml" at <.Values.image.tag>: nil pointer evaluating interface {}.tag

Error: 1 chart(s) linted, 1 chart(s) failed
```

---

### 2. 模板渲染测试

```bash
# 渲染模板（不部署）
helm template my-app ./my-app

# 渲染特定环境
helm template my-app ./my-app -f values-prod.yaml

# 只渲染特定文件
helm template my-app ./my-app -s templates/deployment.yaml

# 调试模式
helm template my-app ./my-app --debug
```

---

### 3. 安装测试

```bash
# 模拟安装（不实际部署）
helm install my-app ./my-app --dry-run --debug

# 输出所有 Kubernetes 资源定义
```

---

## 💻 发布 Chart 到仓库

### 1. 打包 Chart

```bash
# 打包 Chart
helm package ./my-app

# 输出
Successfully packaged chart and saved it to: /path/to/my-app-1.0.0.tgz
```

---

### 2. 创建 Chart 仓库（GitHub Pages）

```bash
# 1. 创建仓库目录
mkdir helm-charts
cd helm-charts

# 2. 复制打包的 Chart
cp ../my-app-1.0.0.tgz .

# 3. 生成索引文件
helm repo index . --url https://yourname.github.io/helm-charts

# 4. 提交到 GitHub
git init
git add .
git commit -m "Initial commit"
git push origin main

# 5. 启用 GitHub Pages（Settings → Pages → Source: main branch）
```

---

### 3. 使用自定义仓库

```bash
# 添加仓库
helm repo add my-charts https://yourname.github.io/helm-charts

# 更新仓库索引
helm repo update

# 搜索 Chart
helm search repo my-app

# 安装
helm install my-release my-charts/my-app
```

---

## ⚠️ 三个高频错误

### 错误 1：values.yaml 中的值未引用

**问题：** 模板中引用了不存在的值。

```yaml
# ❌ 错误
{{ .Values.database.host }}  # database 未定义
```

**解决方案：** 使用 `default` 函数提供默认值。

```yaml
# ✅ 正确
{{ .Values.database.host | default "localhost" }}
```

---

### 错误 2：依赖版本冲突

**问题：** 不同 Chart 依赖同一个子 Chart 的不同版本。

```bash
Error: found in Chart.yaml, but missing in charts/ directory: mongodb
```

**解决方案：** 更新依赖。

```bash
helm dependency update ./my-app
```

---

### 错误 3：Release 名称过长

**问题：** Kubernetes 资源名称限制63个字符。

```bash
Error: release name "my-very-long-release-name-that-exceeds-the-limit" is too long
```

**解决方案：** 使用短名称或 `nameOverride`。

```yaml
# values.yaml
nameOverride: "short-name"
```

---

## 📚 FAQ

### 1. Helm 2 vs Helm 3 有什么区别？

| 特性 | Helm 2 | Helm 3 |
|------|--------|--------|
| Tiller | 需要 | 不需要（更安全） |
| Release 存储 | ConfigMap | Secret |
| 命名空间 | 可选 | 必需 |
| 三路合并 | 不支持 | 支持 |

**推荐使用 Helm 3。**

### 2. 如何调试 Helm 部署失败？

```bash
# 查看部署状态
helm status my-release

# 查看历史
helm history my-release

# 查看实际应用的资源
helm get manifest my-release

# 查看 values
helm get values my-release

# 调试模式
helm upgrade my-release ./my-app --debug --dry-run
```

### 3. 如何管理 Secrets？

**方法1：使用 Helm Secrets 插件**

```bash
# 安装插件
helm plugin install https://github.com/jkroepke/helm-secrets

# 加密 values 文件
helm secrets enc values-prod.yaml

# 使用加密的 values
helm secrets install my-release ./my-app -f values-prod.yaml
```

**方法2：使用外部 Secret 管理**

```yaml
# 引用 External Secrets
env:
- name: DATABASE_PASSWORD
  valueFrom:
    secretKeyRef:
      name: external-secret
      key: password
```

---

## 📖 延伸阅读

- [Helm 官方文档](https://helm.sh/docs/)
- [Helm Chart 最佳实践](https://helm.sh/docs/chart_best_practices/)
- [Artifact Hub](https://artifacthub.io/) (Chart 仓库)

---

## 💪 课后练习

### 练习 1：创建自定义 Chart

1. 使用 `helm create` 创建 Chart
2. 修改 templates 和 values
3. 部署到 Kubernetes

### 练习 2：多环境部署

1. 创建 dev、staging、prod 三个 values 文件
2. 部署到不同命名空间
3. 验证配置差异

### 练习 3：发布 Chart 到仓库

1. 打包 Chart
2. 创建 GitHub Pages 仓库
3. 发布并使用

---

## 📝 总结

本节学习了：

✅ **Helm 基础**：Chart、Release、Repository、Values  
✅ **创建 Chart**：目录结构、模板语法、辅助函数  
✅ **多环境配置**：values-dev.yaml、values-prod.yaml  
✅ **依赖管理**：Chart.yaml dependencies、条件依赖  
✅ **Hooks**：pre-install、post-upgrade、数据库迁移  
✅ **测试与调试**：lint、template、dry-run  
✅ **Chart 仓库**：打包、发布、使用

🎉 **恭喜！第6章《DevOps与生产部署》全部完成！**

---

## 🎓 第6章总结

### 已完成文档清单

| 文件 | 状态 | 行数 | 核心内容 |
|------|------|------|---------|
| **章节目录** | ✅ | ~400 | 学习路径、环境准备 |
| **6.1 Docker多阶段构建** | ✅ | ~1400 | Alpine、多阶段、镜像优化 |
| **6.2 非root用户运行容器** | ✅ | ~1350 | 容器安全、Trivy扫描 |
| **6.3 Kubernetes部署** | ✅ | ~1600 | Deployment、Service、Ingress、HPA |
| **6.4 日志收集** | ✅ | ~1300 | Loki、Fluentd、Grafana |
| **6.5 CI/CD自动化** | ✅ | ~1300 | GitHub Actions、多环境部署 |
| **6.6 蓝绿部署与金丝雀** | ✅ | ~1200 | Istio、Flagger、流量管理 |
| **6.7 健康检查与优雅关闭** | ✅ | ~1200 | 探针配置、SIGTERM处理 |
| **6.8 Helm Charts** | ✅ | ~1200 | Chart开发、多环境、Hooks |
| **总计** | **9个文档** | **~11,000行** | 完整的DevOps体系 |

**下一步：** 可以继续创建第7章或其他章节！
