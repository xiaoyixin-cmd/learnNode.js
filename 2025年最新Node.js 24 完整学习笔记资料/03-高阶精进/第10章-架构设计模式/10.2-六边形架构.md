**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 10.2 六边形架构

> 使用端口与适配器模式隔离业务逻辑，实现可测试、可替换的架构

---

## 📚 知识导读

### 什么是六边形架构？

**定义：**  
六边形架构（Hexagonal Architecture），也称为端口与适配器架构（Ports and Adapters），是一种将业务逻辑与外部依赖隔离的架构模式。

**核心思想：**
- **业务逻辑**在中心（领域层）
- **端口（Ports）**：定义接口
- **适配器（Adapters）**：实现具体技术细节
- **依赖倒置**：业务逻辑不依赖具体实现

**优势：**
- ✅ 易于测试（Mock 适配器）
- ✅ 技术栈可替换（换数据库不影响业务）
- ✅ 清晰的边界（领域 vs 基础设施）

---

## 🎯 核心概念

### 1. 分层结构

```
┌─────────────────────────────────────────┐
│          适配器层（Adapters）            │
│  HTTP │ GraphQL │ CLI │ WebSocket       │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│          应用层（Application）           │
│       Use Cases / Commands / Queries    │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│          领域层（Domain）                │
│    Entities / Value Objects / Services  │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│       基础设施层（Infrastructure）        │
│   MongoDB │ PostgreSQL │ Redis │ SMTP   │
└─────────────────────────────────────────┘
```

---

### 2. 端口（Ports）

**入站端口（Inbound）**：应用暴露的接口
```typescript
// domain/ports/user-service.port.ts
export interface UserServicePort {
  createUser(data: CreateUserDto): Promise<User>;
  getUserById(id: string): Promise<User | null>;
  updateUser(id: string, data: UpdateUserDto): Promise<User>;
  deleteUser(id: string): Promise<void>;
}
```

**出站端口（Outbound）**：应用依赖的接口
```typescript
// domain/ports/user-repository.port.ts
export interface UserRepositoryPort {
  findById(id: string): Promise<User | null>;
  findByEmail(email: string): Promise<User | null>;
  save(user: User): Promise<User>;
  delete(id: string): Promise<void>;
}

// domain/ports/email-service.port.ts
export interface EmailServicePort {
  sendWelcomeEmail(to: string, name: string): Promise<void>;
}
```

---

## 🎯 领域层实现

### 1. 实体（Entity）

```typescript
// domain/entities/user.entity.ts
import { v4 as uuidv4 } from 'uuid';

export class User {
  constructor(
    public readonly id: string,
    public name: string,
    public email: string,
    public readonly createdAt: Date,
    public updatedAt: Date
  ) {}
  
  static create(name: string, email: string): User {
    return new User(
      uuidv4(),
      name,
      email,
      new Date(),
      new Date()
    );
  }
  
  updateName(newName: string): void {
    if (!newName || newName.trim().length < 2) {
      throw new Error('Name must be at least 2 characters');
    }
    this.name = newName;
    this.updatedAt = new Date();
  }
  
  updateEmail(newEmail: string): void {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(newEmail)) {
      throw new Error('Invalid email format');
    }
    this.email = newEmail;
    this.updatedAt = new Date();
  }
}
```

---

### 2. 值对象（Value Object）

```typescript
// domain/value-objects/email.vo.ts
export class Email {
  private readonly value: string;
  
  constructor(email: string) {
    this.validate(email);
    this.value = email.toLowerCase();
  }
  
  private validate(email: string): void {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
      throw new Error('Invalid email format');
    }
  }
  
  toString(): string {
    return this.value;
  }
  
  equals(other: Email): boolean {
    return this.value === other.value;
  }
}

// 使用
const email = new Email('user@example.com');
console.log(email.toString());  // "user@example.com"
```

---

### 3. 领域服务

```typescript
// domain/services/user.service.ts
import { UserRepositoryPort } from '../ports/user-repository.port';
import { EmailServicePort } from '../ports/email-service.port';
import { UserServicePort } from '../ports/user-service.port';
import { User } from '../entities/user.entity';

export class UserService implements UserServicePort {
  constructor(
    private readonly userRepository: UserRepositoryPort,
    private readonly emailService: EmailServicePort
  ) {}
  
  async createUser(data: { name: string; email: string }): Promise<User> {
    // 检查邮箱是否已存在
    const existingUser = await this.userRepository.findByEmail(data.email);
    if (existingUser) {
      throw new Error('Email already exists');
    }
    
    // 创建用户
    const user = User.create(data.name, data.email);
    
    // 保存
    await this.userRepository.save(user);
    
    // 发送欢迎邮件
    await this.emailService.sendWelcomeEmail(user.email, user.name);
    
    return user;
  }
  
  async getUserById(id: string): Promise<User | null> {
    return await this.userRepository.findById(id);
  }
  
  async updateUser(id: string, data: { name?: string; email?: string }): Promise<User> {
    const user = await this.userRepository.findById(id);
    if (!user) {
      throw new Error('User not found');
    }
    
    if (data.name) {
      user.updateName(data.name);
    }
    
    if (data.email) {
      user.updateEmail(data.email);
    }
    
    return await this.userRepository.save(user);
  }
  
  async deleteUser(id: string): Promise<void> {
    await this.userRepository.delete(id);
  }
}
```

---

## 🎯 基础设施层（适配器）

### 1. MongoDB 适配器

```typescript
// infrastructure/adapters/mongo-user-repository.adapter.ts
import { UserRepositoryPort } from '../../domain/ports/user-repository.port';
import { User } from '../../domain/entities/user.entity';
import { model, Schema } from 'mongoose';

const UserSchema = new Schema({
  _id: String,
  name: String,
  email: { type: String, unique: true },
  createdAt: Date,
  updatedAt: Date,
});

const UserModel = model('User', UserSchema);

export class MongoUserRepository implements UserRepositoryPort {
  async findById(id: string): Promise<User | null> {
    const doc = await UserModel.findById(id);
    if (!doc) return null;
    
    return new User(
      doc._id,
      doc.name,
      doc.email,
      doc.createdAt,
      doc.updatedAt
    );
  }
  
  async findByEmail(email: string): Promise<User | null> {
    const doc = await UserModel.findOne({ email });
    if (!doc) return null;
    
    return new User(
      doc._id,
      doc.name,
      doc.email,
      doc.createdAt,
      doc.updatedAt
    );
  }
  
  async save(user: User): Promise<User> {
    await UserModel.findByIdAndUpdate(
      user.id,
      {
        name: user.name,
        email: user.email,
        createdAt: user.createdAt,
        updatedAt: user.updatedAt,
      },
      { upsert: true, new: true }
    );
    
    return user;
  }
  
  async delete(id: string): Promise<void> {
    await UserModel.findByIdAndDelete(id);
  }
}
```

---

### 2. PostgreSQL 适配器（可替换）

```typescript
// infrastructure/adapters/postgres-user-repository.adapter.ts
import { Pool } from 'pg';
import { UserRepositoryPort } from '../../domain/ports/user-repository.port';
import { User } from '../../domain/entities/user.entity';

export class PostgresUserRepository implements UserRepositoryPort {
  constructor(private readonly pool: Pool) {}
  
  async findById(id: string): Promise<User | null> {
    const result = await this.pool.query(
      'SELECT * FROM users WHERE id = $1',
      [id]
    );
    
    if (result.rows.length === 0) return null;
    
    const row = result.rows[0];
    return new User(row.id, row.name, row.email, row.created_at, row.updated_at);
  }
  
  async findByEmail(email: string): Promise<User | null> {
    const result = await this.pool.query(
      'SELECT * FROM users WHERE email = $1',
      [email]
    );
    
    if (result.rows.length === 0) return null;
    
    const row = result.rows[0];
    return new User(row.id, row.name, row.email, row.created_at, row.updated_at);
  }
  
  async save(user: User): Promise<User> {
    await this.pool.query(
      `INSERT INTO users (id, name, email, created_at, updated_at)
       VALUES ($1, $2, $3, $4, $5)
       ON CONFLICT (id) DO UPDATE
       SET name = $2, email = $3, updated_at = $5`,
      [user.id, user.name, user.email, user.createdAt, user.updatedAt]
    );
    
    return user;
  }
  
  async delete(id: string): Promise<void> {
    await this.pool.query('DELETE FROM users WHERE id = $1', [id]);
  }
}
```

---

### 3. SMTP 邮件适配器

```typescript
// infrastructure/adapters/smtp-email-service.adapter.ts
import nodemailer from 'nodemailer';
import { EmailServicePort } from '../../domain/ports/email-service.port';

export class SmtpEmailService implements EmailServicePort {
  private transporter;
  
  constructor() {
    this.transporter = nodemailer.createTransport({
      host: process.env.SMTP_HOST,
      port: Number(process.env.SMTP_PORT),
      auth: {
        user: process.env.SMTP_USER,
        pass: process.env.SMTP_PASS,
      },
    });
  }
  
  async sendWelcomeEmail(to: string, name: string): Promise<void> {
    await this.transporter.sendMail({
      from: '"MyApp" <noreply@myapp.com>',
      to,
      subject: 'Welcome to MyApp',
      html: `<h1>Hello ${name}!</h1><p>Welcome to MyApp.</p>`,
    });
  }
}

// 测试适配器（不发送真实邮件）
export class MockEmailService implements EmailServicePort {
  private emails: Array<{ to: string; name: string }> = [];
  
  async sendWelcomeEmail(to: string, name: string): Promise<void> {
    console.log(`[Mock] Sending welcome email to ${to}`);
    this.emails.push({ to, name });
  }
  
  getEmails() {
    return this.emails;
  }
}
```

---

## 🎯 应用层（HTTP 适配器）

### 1. Express 适配器

```typescript
// infrastructure/adapters/express-user.controller.ts
import { Request, Response } from 'express';
import { UserServicePort } from '../../domain/ports/user-service.port';

export class ExpressUserController {
  constructor(private readonly userService: UserServicePort) {}
  
  createUser = async (req: Request, res: Response) => {
    try {
      const { name, email } = req.body;
      const user = await this.userService.createUser({ name, email });
      
      res.status(201).json(user);
    } catch (error: any) {
      res.status(400).json({ error: error.message });
    }
  };
  
  getUser = async (req: Request, res: Response) => {
    try {
      const { id } = req.params;
      const user = await this.userService.getUserById(id);
      
      if (!user) {
        return res.status(404).json({ error: 'User not found' });
      }
      
      res.json(user);
    } catch (error: any) {
      res.status(500).json({ error: error.message });
    }
  };
  
  updateUser = async (req: Request, res: Response) => {
    try {
      const { id } = req.params;
      const user = await this.userService.updateUser(id, req.body);
      
      res.json(user);
    } catch (error: any) {
      res.status(400).json({ error: error.message });
    }
  };
  
  deleteUser = async (req: Request, res: Response) => {
    try {
      const { id } = req.params;
      await this.userService.deleteUser(id);
      
      res.status(204).send();
    } catch (error: any) {
      res.status(500).json({ error: error.message });
    }
  };
}
```

---

### 2. GraphQL 适配器

```typescript
// infrastructure/adapters/graphql-user.resolver.ts
import { UserServicePort } from '../../domain/ports/user-service.port';

export class GraphQLUserResolver {
  constructor(private readonly userService: UserServicePort) {}
  
  Query = {
    user: async (_: any, { id }: { id: string }) => {
      return await this.userService.getUserById(id);
    },
  };
  
  Mutation = {
    createUser: async (_: any, { input }: { input: any }) => {
      return await this.userService.createUser(input);
    },
    
    updateUser: async (_: any, { id, input }: { id: string; input: any }) => {
      return await this.userService.updateUser(id, input);
    },
    
    deleteUser: async (_: any, { id }: { id: string }) => {
      await this.userService.deleteUser(id);
      return true;
    },
  };
}

// GraphQL Schema
const typeDefs = `
  type User {
    id: ID!
    name: String!
    email: String!
    createdAt: String!
    updatedAt: String!
  }
  
  input CreateUserInput {
    name: String!
    email: String!
  }
  
  input UpdateUserInput {
    name: String
    email: String
  }
  
  type Query {
    user(id: ID!): User
  }
  
  type Mutation {
    createUser(input: CreateUserInput!): User!
    updateUser(id: ID!, input: UpdateUserInput!): User!
    deleteUser(id: ID!): Boolean!
  }
`;
```

---

### 3. CLI 适配器

```typescript
// infrastructure/adapters/cli-user.command.ts
import { Command } from 'commander';
import { UserServicePort } from '../../domain/ports/user-service.port';

export class CliUserCommand {
  constructor(private readonly userService: UserServicePort) {}
  
  register(program: Command) {
    const userCmd = program.command('user');
    
    userCmd
      .command('create <name> <email>')
      .description('Create a new user')
      .action(async (name, email) => {
        try {
          const user = await this.userService.createUser({ name, email });
          console.log('✅ User created:', user);
        } catch (error: any) {
          console.error('❌ Error:', error.message);
        }
      });
    
    userCmd
      .command('get <id>')
      .description('Get user by ID')
      .action(async (id) => {
        try {
          const user = await this.userService.getUserById(id);
          if (!user) {
            console.log('❌ User not found');
          } else {
            console.log('✅ User:', user);
          }
        } catch (error: any) {
          console.error('❌ Error:', error.message);
        }
      });
  }
}
```

---

## 🎯 依赖注入

### 1. 手动注入

```typescript
// app.ts
import express from 'express';
import { MongoUserRepository } from './infrastructure/adapters/mongo-user-repository.adapter';
import { SmtpEmailService } from './infrastructure/adapters/smtp-email-service.adapter';
import { UserService } from './domain/services/user.service';
import { ExpressUserController } from './infrastructure/adapters/express-user.controller';

// 创建适配器
const userRepository = new MongoUserRepository();
const emailService = new SmtpEmailService();

// 创建领域服务
const userService = new UserService(userRepository, emailService);

// 创建控制器
const userController = new ExpressUserController(userService);

// 配置路由
const app = express();
app.use(express.json());

app.post('/users', userController.createUser);
app.get('/users/:id', userController.getUser);
app.put('/users/:id', userController.updateUser);
app.delete('/users/:id', userController.deleteUser);

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000');
});
```

---

### 2. 使用 InversifyJS

```bash
npm install inversify reflect-metadata
```

```typescript
// di-container.ts
import { Container } from 'inversify';
import 'reflect-metadata';

const container = new Container();

// 绑定适配器
container.bind('UserRepository').to(MongoUserRepository);
container.bind('EmailService').to(SmtpEmailService);

// 绑定服务
container.bind('UserService').to(UserService);

// 绑定控制器
container.bind('ExpressUserController').to(ExpressUserController);

export { container };

// 使用
import { injectable, inject } from 'inversify';

@injectable()
class UserService {
  constructor(
    @inject('UserRepository') private userRepository: UserRepositoryPort,
    @inject('EmailService') private emailService: EmailServicePort
  ) {}
  
  // ...
}
```

---

## 🎯 测试

### 1. 单元测试（Mock 适配器）

```typescript
// __tests__/user.service.test.ts
import { UserService } from '../domain/services/user.service';
import { User } from '../domain/entities/user.entity';

describe('UserService', () => {
  let userService: UserService;
  let mockUserRepository: any;
  let mockEmailService: any;
  
  beforeEach(() => {
    // Mock 适配器
    mockUserRepository = {
      findById: jest.fn(),
      findByEmail: jest.fn(),
      save: jest.fn(),
      delete: jest.fn(),
    };
    
    mockEmailService = {
      sendWelcomeEmail: jest.fn(),
    };
    
    userService = new UserService(mockUserRepository, mockEmailService);
  });
  
  it('should create user', async () => {
    mockUserRepository.findByEmail.mockResolvedValue(null);
    mockUserRepository.save.mockResolvedValue(
      User.create('Alice', 'alice@example.com')
    );
    
    const user = await userService.createUser({
      name: 'Alice',
      email: 'alice@example.com',
    });
    
    expect(user.name).toBe('Alice');
    expect(mockEmailService.sendWelcomeEmail).toHaveBeenCalledWith(
      'alice@example.com',
      'Alice'
    );
  });
  
  it('should throw error if email exists', async () => {
    mockUserRepository.findByEmail.mockResolvedValue(
      User.create('Existing', 'existing@example.com')
    );
    
    await expect(
      userService.createUser({ name: 'New', email: 'existing@example.com' })
    ).rejects.toThrow('Email already exists');
  });
});
```

---

### 2. 集成测试（真实数据库）

```typescript
// __tests__/integration/user.test.ts
import { MongoMemoryServer } from 'mongodb-memory-server';
import mongoose from 'mongoose';
import { MongoUserRepository } from '../../infrastructure/adapters/mongo-user-repository.adapter';
import { MockEmailService } from '../../infrastructure/adapters/smtp-email-service.adapter';
import { UserService } from '../../domain/services/user.service';

describe('User Integration Test', () => {
  let mongoServer: MongoMemoryServer;
  let userService: UserService;
  
  beforeAll(async () => {
    mongoServer = await MongoMemoryServer.create();
    await mongoose.connect(mongoServer.getUri());
    
    const userRepository = new MongoUserRepository();
    const emailService = new MockEmailService();
    userService = new UserService(userRepository, emailService);
  });
  
  afterAll(async () => {
    await mongoose.disconnect();
    await mongoServer.stop();
  });
  
  it('should create and retrieve user', async () => {
    const created = await userService.createUser({
      name: 'Bob',
      email: 'bob@example.com',
    });
    
    const retrieved = await userService.getUserById(created.id);
    
    expect(retrieved?.name).toBe('Bob');
    expect(retrieved?.email).toBe('bob@example.com');
  });
});
```

---

## ⚠️ 三个高频错误

### 错误 1：领域逻辑泄露到适配器

```typescript
// ❌ 错误：业务逻辑在控制器中
class ExpressUserController {
  createUser = async (req: Request, res: Response) => {
    const { name, email } = req.body;
    
    // ❌ 业务逻辑不应该在这里
    if (await this.userRepository.findByEmail(email)) {
      return res.status(400).json({ error: 'Email exists' });
    }
    
    const user = User.create(name, email);
    await this.userRepository.save(user);
    res.status(201).json(user);
  };
}

// ✅ 正确：业务逻辑在领域服务
class UserService {
  async createUser(data: { name: string; email: string }) {
    // ✅ 业务逻辑在这里
    if (await this.userRepository.findByEmail(data.email)) {
      throw new Error('Email exists');
    }
    
    const user = User.create(data.name, data.email);
    return await this.userRepository.save(user);
  }
}
```

---

### 错误 2：适配器直接依赖

```typescript
// ❌ 错误：直接依赖具体实现
class UserService {
  constructor(private mongoRepo: MongoUserRepository) {}
  // 无法替换为 PostgresUserRepository
}

// ✅ 正确：依赖接口
class UserService {
  constructor(private userRepository: UserRepositoryPort) {}
  // 可以注入任何实现
}
```

---

### 错误 3：忘记映射层

```typescript
// ❌ 错误：直接返回数据库模型
class MongoUserRepository {
  async findById(id: string) {
    return await UserModel.findById(id);  // 返回 Mongoose Document
  }
}

// ✅ 正确：映射为领域实体
class MongoUserRepository {
  async findById(id: string): Promise<User | null> {
    const doc = await UserModel.findById(id);
    if (!doc) return null;
    
    // 映射为领域实体
    return new User(doc._id, doc.name, doc.email, doc.createdAt, doc.updatedAt);
  }
}
```

---

## 📚 FAQ

### 1. 六边形架构 vs 三层架构？

| 特性 | 三层架构 | 六边形架构 |
|------|----------|-----------|
| **依赖方向** | 向下（Controller → Service → Repository） | 向内（Adapter → Domain） |
| **测试性** | 中等 | 高（易 Mock） |
| **灵活性** | 低 | 高（易替换适配器） |
| **复杂度** | 低 | 中 |

### 2. 何时使用六边形架构？

**适用：**
- 业务逻辑复杂
- 需要多种入口（HTTP/GraphQL/CLI）
- 需要频繁更换技术栈

**不适用：**
- 简单 CRUD 应用
- 快速原型开发

### 3. 如何处理跨领域操作？

**方案：**
1. **领域事件**：发布事件，其他领域订阅
2. **应用服务**：协调多个领域服务
3. **Saga 模式**：分布式事务

---

## 📖 延伸阅读

- [Hexagonal Architecture](https://alistair.cockburn.us/hexagonal-architecture/)
- [Ports and Adapters Pattern](https://herbertograca.com/2017/09/14/ports-adapters-architecture/)
- [Clean Architecture（Robert C. Martin）](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)

---

## 💪 课后练习

### 练习 1：实现六边形架构

1. 定义领域实体和端口
2. 实现 MongoDB 和 PostgreSQL 适配器
3. 实现 HTTP 和 GraphQL 适配器

### 练习 2：测试驱动

1. 编写单元测试（Mock 适配器）
2. 编写集成测试（真实数据库）
3. 测试适配器替换

### 练习 3：依赖注入

1. 使用 InversifyJS 配置 DI
2. 实现环境切换（开发/测试/生产）
3. 实现适配器热插拔

---

## 📝 总结

本节学习了：

✅ **六边形架构**：端口与适配器模式  
✅ **分层设计**：领域层、应用层、基础设施层  
✅ **端口定义**：入站端口（服务接口）、出站端口（依赖接口）  
✅ **适配器实现**：MongoDB/PostgreSQL/SMTP 适配器  
✅ **多入口支持**：Express/GraphQL/CLI 适配器  
✅ **依赖注入**：手动注入、InversifyJS  
✅ **测试策略**：单元测试、集成测试

**下一步：** 学习《10.3 CQRS 与事件溯源》！
