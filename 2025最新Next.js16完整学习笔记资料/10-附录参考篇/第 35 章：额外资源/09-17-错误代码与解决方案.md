**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 09-21-错误代码与解决方案

## 1. 概述

### 1.1 关于本文档

本文档收集了 Next.js 16 开发中常见的错误代码和对应的解决方案,帮助开发者快速定位和解决问题。

### 1.2 错误分类

| 分类       | 错误数量 | 严重程度 |
| :--------- | :------- | :------- |
| 编译错误   | 15+      | 高       |
| 运行时错误 | 20+      | 高       |
| 类型错误   | 10+      | 中       |
| 缓存错误   | 8+       | 中       |
| 路由错误   | 12+      | 中       |
| API 错误   | 10+      | 高       |
| 部署错误   | 8+       | 高       |

---

## 2. 编译错误

### 2.1 Module not found

**错误信息**:

```
Error: Module not found: Can't resolve '@/components/Button'
```

**原因**:

1. 文件路径错误
2. 路径别名配置错误
3. 文件不存在

**解决方案**:

```json
// tsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

**检查清单**:

- [ ] 文件是否存在
- [ ] 路径是否正确
- [ ] tsconfig.json 配置是否正确
- [ ] 重启开发服务器

---

### 2.2 Unexpected token

**错误信息**:

```
SyntaxError: Unexpected token '<'
```

**原因**:

1. JSX 语法错误
2. 文件扩展名错误
3. 导入非 JS/TS 文件

**解决方案**:

```typescript
// ❌ 错误
import data from "./data.json";

// ✅ 正确
import data from "./data.json" assert { type: "json" };
```

---

### 2.3 Cannot use import statement outside a module

**错误信息**:

```
SyntaxError: Cannot use import statement outside a module
```

**原因**:

1. package.json 缺少 "type": "module"
2. 文件扩展名错误
3. Node.js 版本过低

**解决方案**:

```json
// package.json
{
  "type": "module"
}
```

或使用 .mjs 扩展名

---

### 2.4 TypeScript compilation error

**错误信息**:

```
Type 'string | undefined' is not assignable to type 'string'
```

**原因**:

类型不匹配

**解决方案**:

```typescript
// ❌ 错误
const name: string = formData.get("name");

// ✅ 正确
const name = formData.get("name") as string;

// ✅ 更好
const name = formData.get("name");
if (typeof name !== "string") {
  throw new Error("Name is required");
}
```

---

## 3. 运行时错误

### 3.1 headers() and cookies() are now async

**错误信息**:

### 11.2 如何查看详细错误信息?

**问题**: 错误信息不够详细

**解答**:

**开发模式**:

```bash
npm run dev
```

开发模式会显示详细的错误信息和堆栈跟踪

**生产模式**:

```bash
npm run build
npm run start
```

生产模式错误信息较少,需要查看日志

**查看日志**:

```typescript
// app/error.tsx
"use client";

export default function Error({ error }: { error: Error }) {
  console.error("Error:", error);

  return (
    <div>
      <h2>出错了!</h2>
      <pre>{error.message}</pre>
      <pre>{error.stack}</pre>
    </div>
  );
}
```

---

### 11.3 如何处理异步错误?

**问题**: 异步操作错误处理

**解答**:

**Server Component**:

```typescript
export default async function Page() {
  try {
    const data = await fetchData();
    return <div>{data.title}</div>;
  } catch (error) {
    throw error; // 会被 error.tsx 捕获
  }
}
```

**Client Component**:

```typescript
"use client";

import { useState, useEffect } from "react";

export default function Component() {
  const [data, setData] = useState(null);
  const [error, setError] = useState(null);

  useEffect(() => {
    fetchData().then(setData).catch(setError);
  }, []);

  if (error) {
    return <div>Error: {error.message}</div>;
  }

  if (!data) {
    return <div>Loading...</div>;
  }

  return <div>{data.title}</div>;
}
```

---

### 11.4 如何处理表单验证错误?

**问题**: 表单提交错误处理

**解答**:

```typescript
"use server";

import { z } from "zod";

const schema = z.object({
  email: z.string().email("Invalid email"),
  password: z.string().min(8, "Password must be at least 8 characters"),
});

export async function login(formData: FormData) {
  const data = {
    email: formData.get("email"),
    password: formData.get("password"),
  };

  const result = schema.safeParse(data);

  if (!result.success) {
    return {
      errors: result.error.flatten().fieldErrors,
    };
  }

  // 登录逻辑
  return { success: true };
}
```

```typescript
"use client";

import { useFormState } from "react-dom";
import { login } from "./actions";

export default function LoginForm() {
  const [state, formAction] = useFormState(login, null);

  return (
    <form action={formAction}>
      <input name="email" type="email" />
      {state?.errors?.email && <p>{state.errors.email}</p>}

      <input name="password" type="password" />
      {state?.errors?.password && <p>{state.errors.password}</p>}

      <button type="submit">登录</button>
    </form>
  );
}
```

---

### 11.5 如何处理网络错误?

**问题**: fetch 请求失败

**解答**:

```typescript
async function fetchWithRetry(url: string, options = {}, retries = 3) {
  for (let i = 0; i < retries; i++) {
    try {
      const response = await fetch(url, options);

      if (!response.ok) {
        throw new Error(`HTTP error! status: ${response.status}`);
      }

      return await response.json();
    } catch (error) {
      if (i === retries - 1) {
        throw error;
      }

      // 等待后重试
      await new Promise((resolve) => setTimeout(resolve, 1000 * (i + 1)));
    }
  }
}
```

---

### 11.6 如何处理数据库错误?

**问题**: Prisma 操作失败

**解答**:

```typescript
import { Prisma } from "@prisma/client";

export async function createUser(email: string) {
  try {
    const user = await db.user.create({
      data: { email },
    });
    return { success: true, user };
  } catch (error) {
    if (error instanceof Prisma.PrismaClientKnownRequestError) {
      if (error.code === "P2002") {
        return { error: "Email already exists" };
      }
    }

    return { error: "Failed to create user" };
  }
}
```

**常见 Prisma 错误代码**:

| 错误代码 | 说明         |
| :------- | :----------- |
| P2002    | 唯一约束冲突 |
| P2003    | 外键约束冲突 |
| P2025    | 记录不存在   |

---

### 11.7 如何处理认证错误?

**问题**: NextAuth.js 错误

**解答**:

```typescript
// app/api/auth/[...nextauth]/route.ts
import NextAuth from 'next-auth'

const handler = NextAuth({
  providers: [...],
  callbacks: {
    async signIn({ user, account, profile }) {
      try {
        // 验证逻辑
        return true
      } catch (error) {
        console.error('Sign in error:', error)
        return false
      }
    }
  },
  pages: {
    error: '/auth/error' // 自定义错误页面
  }
})

export { handler as GET, handler as POST }
```

```typescript
// app/auth/error/page.tsx
export default function AuthError({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>;
}) {
  const params = await searchParams;
  const error = params.error;

  return (
    <div>
      <h1>认证错误</h1>
      <p>{error}</p>
    </div>
  );
}
```

---

## 12. 错误预防

### 12.1 使用 TypeScript

**好处**:

- 编译时类型检查
- 更好的 IDE 支持
- 减少运行时错误

**示例**:

```typescript
interface User {
  id: string;
  email: string;
  name: string;
}

async function getUser(id: string): Promise<User> {
  const user = await db.user.findUnique({ where: { id } });

  if (!user) {
    throw new Error("User not found");
  }

  return user;
}
```

---

### 12.2 使用 ESLint

**配置**:

```json
// .eslintrc.json
{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-console": "warn",
    "no-unused-vars": "error"
  }
}
```

**运行**:

```bash
npm run lint
```

---

### 12.3 使用数据验证

**Zod 示例**:

```typescript
import { z } from "zod";

const userSchema = z.object({
  email: z.string().email(),
  name: z.string().min(1),
  age: z.number().min(0).max(150),
});

export async function createUser(data: unknown) {
  const result = userSchema.safeParse(data);

  if (!result.success) {
    throw new Error("Invalid user data");
  }

  return await db.user.create({ data: result.data });
}
```

---

### 12.4 使用错误边界

**示例**:

```typescript
// components/ErrorBoundary.tsx
"use client";

import { Component, ReactNode } from "react";

interface Props {
  children: ReactNode;
  fallback?: ReactNode;
}

interface State {
  hasError: boolean;
  error?: Error;
}

export class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: any) {
    console.error("Error caught by boundary:", error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || <div>Something went wrong</div>;
    }

    return this.props.children;
  }
}
```

**使用**:

```typescript
export default function Layout({ children }) {
  return (
    <ErrorBoundary fallback={<div>Error occurred</div>}>
      {children}
    </ErrorBoundary>
  );
}
```

---

### 12.5 使用测试

**单元测试**:

```typescript
// __tests__/utils.test.ts
import { formatDate } from "@/lib/utils";

describe("formatDate", () => {
  it("formats date correctly", () => {
    const date = new Date("2024-01-01");
    expect(formatDate(date)).toBe("2024-01-01");
  });

  it("handles invalid date", () => {
    expect(() => formatDate(null)).toThrow();
  });
});
```

---

## 13. 错误监控

### 13.1 使用 Sentry

**安装**:

```bash
npm install @sentry/nextjs
```

**配置**:

```typescript
// sentry.client.config.ts
import * as Sentry from "@sentry/nextjs";

Sentry.init({
  dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
  tracesSampleRate: 1.0,
});
```

```typescript
// sentry.server.config.ts
import * as Sentry from "@sentry/nextjs";

Sentry.init({
  dsn: process.env.SENTRY_DSN,
  tracesSampleRate: 1.0,
});
```

---

### 13.2 自定义错误日志

**示例**:

```typescript
// lib/logger.ts
export function logError(error: Error, context?: any) {
  console.error("Error:", {
    message: error.message,
    stack: error.stack,
    context,
    timestamp: new Date().toISOString(),
  });

  // 发送到日志服务
  if (process.env.NODE_ENV === "production") {
    fetch("/api/log", {
      method: "POST",
      body: JSON.stringify({
        error: error.message,
        stack: error.stack,
        context,
      }),
    });
  }
}
```

---

## 14. 总结

### 14.1 错误分类总结

**编译错误**:

- Module not found
- Unexpected token
- TypeScript compilation error

**运行时错误**:

- headers() and cookies() are now async
- params is now a Promise
- Hydration mismatch
- Cannot read properties of undefined
- Maximum update depth exceeded

**类型错误**:

- Property does not exist on type
- Type 'null' is not assignable to type

**缓存错误**:

- fetch cache default changed
- Cache revalidation failed

**路由错误**:

- Route not found
- Multiple root layouts

**API 错误**:

- API route not found
- CORS error

**部署错误**:

- Build failed
- Environment variable not found

### 14.2 错误处理最佳实践

1. **使用 TypeScript**: 编译时类型检查
2. **使用 ESLint**: 代码质量检查
3. **使用数据验证**: 运行时数据验证
4. **使用错误边界**: 捕获组件错误
5. **使用测试**: 预防错误
6. **使用错误监控**: 生产环境错误追踪

### 14.3 快速查找技巧

1. 使用浏览器搜索 (Ctrl+F)
2. 查看错误分类
3. 查看错误代码
4. 查看解决方案

### 14.4 下一步

遇到错误后,建议:

- 09-20: 常见问题解答(FAQ)
- 09-22: 版本差异速查表
- 09-24: 常见错误排查手册

本文档会持续更新,欢迎提交新的错误和解决方案。

```
Error: headers() and cookies() are now async functions
```

**原因**:

Next.js 16 中 headers() 和 cookies() 变为异步函数

**解决方案**:

```typescript
// ❌ Next.js 15
import { headers } from "next/headers";

export function Component() {
  const headersList = headers();
  const userAgent = headersList.get("user-agent");
}

// ✅ Next.js 16
import { headers } from "next/headers";

export async function Component() {
  const headersList = await headers();
  const userAgent = headersList.get("user-agent");
}
```

---

### 3.2 params is now a Promise

**错误信息**:

```
Error: params is now a Promise and should be awaited
```

**原因**:

Next.js 16 中 params 变为 Promise

**解决方案**:

```typescript
// ❌ Next.js 15
export default function Page({ params }: { params: { id: string } }) {
  return <div>ID: {params.id}</div>;
}

// ✅ Next.js 16
export default async function Page({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = await params;
  return <div>ID: {id}</div>;
}
```

---

### 3.3 Hydration mismatch

**错误信息**:

```
Error: Hydration failed because the initial UI does not match what was rendered on the server
```

**原因**:

1. 服务端和客户端渲染不一致
2. 使用了浏览器 API
3. 随机数或时间戳

**解决方案**:

```typescript
// ❌ 错误
export default function Component() {
  return <div>{Math.random()}</div>;
}

// ✅ 正确
("use client");

import { useState, useEffect } from "react";

export default function Component() {
  const [random, setRandom] = useState(0);

  useEffect(() => {
    setRandom(Math.random());
  }, []);

  return <div>{random}</div>;
}
```

---

### 3.4 Cannot read properties of undefined

**错误信息**:

```
TypeError: Cannot read properties of undefined (reading 'map')
```

**原因**:

数据未加载或为 undefined

**解决方案**:

```typescript
// ❌ 错误
export default function Posts({ posts }) {
  return posts.map((post) => <div key={post.id}>{post.title}</div>);
}

// ✅ 正确
export default function Posts({ posts }) {
  if (!posts || posts.length === 0) {
    return <div>No posts</div>;
  }

  return posts.map((post) => <div key={post.id}>{post.title}</div>);
}
```

---

### 3.5 Maximum update depth exceeded

**错误信息**:

```
Error: Maximum update depth exceeded
```

**原因**:

无限循环更新

**解决方案**:

```typescript
// ❌ 错误
export default function Component() {
  const [count, setCount] = useState(0);

  setCount(count + 1); // 无限循环

  return <div>{count}</div>;
}

// ✅ 正确
export default function Component() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    setCount(count + 1);
  }, []); // 只执行一次

  return <div>{count}</div>;
}
```

---

## 4. 类型错误

### 4.1 Property does not exist on type

**错误信息**:

```
Property 'user' does not exist on type 'Session'
```

**原因**:

类型定义不完整

**解决方案**:

```typescript
// types/next-auth.d.ts
import "next-auth";

declare module "next-auth" {
  interface Session {
    user: {
      id: string;
      email: string;
      name: string;
    };
  }
}
```

---

### 4.2 Type 'null' is not assignable to type

**错误信息**:

```
Type 'null' is not assignable to type 'User'
```

**原因**:

严格的 null 检查

**解决方案**:

```typescript
// ❌ 错误
const user: User = await getUser();

// ✅ 正确
const user: User | null = await getUser();

if (!user) {
  throw new Error("User not found");
}
```

---

## 5. 缓存错误

### 5.1 fetch cache default changed

**错误信息**:

```
Warning: fetch cache default changed from 'force-cache' to 'no-store'
```

**原因**:

Next.js 16 改变了 fetch 默认缓存策略

**解决方案**:

```typescript
// 明确指定缓存策略
fetch(url, { cache: "force-cache" }); // 缓存
fetch(url, { cache: "no-store" }); // 不缓存
fetch(url, { next: { revalidate: 3600 } }); // 重新验证
```

---

### 5.2 Cache revalidation failed

**错误信息**:

```
Error: Failed to revalidate cache
```

**原因**:

1. 路径不存在
2. 标签不存在
3. 权限问题

**解决方案**:

```typescript
'use server'

import { revalidatePath, revalidateTag } from 'next/cache'

export async function updatePost() {
  try {
    await db.post.update(...)
    revalidatePath('/posts')
  } catch (error) {
    console.error('Revalidation failed:', error)
  }
}
```

---

## 6. 路由错误

### 6.1 Route not found

**错误信息**:

```
404 | This page could not be found
```

**原因**:

1. 路由文件不存在
2. 文件命名错误
3. 动态路由配置错误

**解决方案**:

检查文件结构:

```
app/
├── page.tsx           # /
├── about/
│   └── page.tsx       # /about
└── blog/
    └── [slug]/
        └── page.tsx   # /blog/:slug
```

---

### 6.2 Multiple root layouts

**错误信息**:

```
Error: Multiple root layouts found
```

**原因**:

存在多个根布局文件

**解决方案**:

只保留一个 app/layout.tsx

---

## 7. API 错误

### 7.1 API route not found

**错误信息**:

```
404 | API route not found
```

**原因**:

API 路由文件命名或位置错误

**解决方案**:

```
app/
└── api/
    └── posts/
        └── route.ts   # /api/posts
```

```typescript
// app/api/posts/route.ts
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ posts: [] });
}
```

---

### 7.2 CORS error

**错误信息**:

```
Access to fetch at 'http://localhost:3000/api/posts' from origin 'http://localhost:3001' has been blocked by CORS policy
```

**原因**:

跨域请求被阻止

**解决方案**:

```typescript
// app/api/posts/route.ts
import { NextResponse } from "next/server";

export async function GET() {
  const response = NextResponse.json({ posts: [] });

  response.headers.set("Access-Control-Allow-Origin", "*");
  response.headers.set(
    "Access-Control-Allow-Methods",
    "GET, POST, PUT, DELETE"
  );

  return response;
}
```

---

## 8. 部署错误

### 8.1 Build failed

**错误信息**:

```
Error: Build failed with exit code 1
```

**原因**:

1. TypeScript 错误
2. ESLint 错误
3. 环境变量缺失

**解决方案**:

```bash
# 本地构建测试
npm run build

# 检查 TypeScript
npm run type-check

# 检查 ESLint
npm run lint
```

---

### 8.2 Environment variable not found

**错误信息**:

```
Error: Environment variable DATABASE_URL is not defined
```

**原因**:

环境变量未配置

**解决方案**:

在部署平台配置环境变量:

```
DATABASE_URL=postgresql://...
NEXT_PUBLIC_API_URL=https://api.example.com
```

---

## 9. 适用场景

### 9.1 快速查找错误

使用浏览器搜索 (Ctrl+F) 查找错误信息

### 9.2 学习错误处理

理解错误原因和解决方案

### 9.3 预防错误

了解常见错误,提前避免

---

## 10. 注意事项

### 10.1 版本差异

不同版本的 Next.js 错误可能不同

### 10.2 环境差异

开发环境和生产环境错误可能不同

### 10.3 及时更新

关注官方文档和社区讨论

---

## 11. 常见问题

### 11.1 如何调试错误?

**问题**: 不知道如何定位错误

**解答**:

**调试步骤**:

1. 查看错误信息
2. 查看堆栈跟踪
3. 使用 console.log
4. 使用浏览器调试工具
5. 查看网络请求

**示例**:

```typescript
export default async function Page() {
  try {
    const data = await fetchData();
    console.log("Data:", data);
    return <div>{data.title}</div>;
  } catch (error) {
    console.error("Error:", error);
    throw error;
  }
}
```
