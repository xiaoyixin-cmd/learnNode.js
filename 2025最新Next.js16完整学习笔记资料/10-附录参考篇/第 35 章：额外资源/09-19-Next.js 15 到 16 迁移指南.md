**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 09-23-Next.js 15 到 16 迁移指南

## 1. 概述

### 1.1 关于本指南

本指南提供了从 Next.js 15 升级到 Next.js 16 的完整步骤和最佳实践。

### 1.2 迁移准备

| 项目     | 说明               | 重要性 |
| :------- | :----------------- | :----- |
| 备份代码 | Git 提交或创建分支 | 高     |
| 测试覆盖 | 确保有足够的测试   | 高     |
| 依赖检查 | 检查第三方库兼容性 | 中     |
| 文档阅读 | 阅读官方迁移指南   | 中     |

---

## 2. 升级步骤

### 2.1 步骤 1: 备份项目

```bash
# 创建新分支
git checkout -b upgrade-nextjs-16

# 提交当前更改
git add .
git commit -m "Before upgrading to Next.js 16"
```

---

### 2.2 步骤 2: 更新依赖

```bash
# 更新 Next.js
npm install next@latest react@latest react-dom@latest

# 或使用 yarn
yarn add next@latest react@latest react-dom@latest

# 或使用 pnpm
pnpm add next@latest react@latest react-dom@latest
```

**检查版本**:

```bash
npm list next react react-dom
```

---

### 2.3 步骤 3: 更新 TypeScript 配置

```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

---

### 2.4 步骤 4: 更新 headers() 和 cookies()

**查找所有使用**:

```bash
# 查找 headers()
grep -r "headers()" app/

# 查找 cookies()
grep -r "cookies()" app/
```

**更新代码**:

```typescript
// ❌ Next.js 15
import { headers, cookies } from "next/headers";

export function Component() {
  const headersList = headers();
  const cookieStore = cookies();
}

// ✅ Next.js 16
import { headers, cookies } from "next/headers";

export async function Component() {
  const headersList = await headers();
  const cookieStore = await cookies();
}
```

---

### 2.5 步骤 5: 更新 params 和 searchParams

**查找所有页面**:

```bash
# 查找所有 page.tsx
find app -name "page.tsx"

# 查找所有 layout.tsx
find app -name "layout.tsx"
```

**更新代码**:

````typescript
// ❌ Next.js 15
export default function Page({
  params,
  searchParams
}: {
  params: { id: string }
  searchParams: { q: string }
}) {
  return <div>ID: {params.id}</div>
}


### 10.2 类型错误如何解决?

**问题**: TypeScript 报错

**解答**:

**常见类型错误**:

1. **params 类型错误**:

```typescript
// ❌ 错误
export default function Page({ params }: { params: { id: string } }) {
  return <div>{params.id}</div>
}

// ✅ 正确
export default async function Page({
  params
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params
  return <div>{id}</div>
}
````

2. **headers() 类型错误**:

```typescript
// ❌ 错误
const headersList = headers();

// ✅ 正确
const headersList = await headers();
```

---

### 10.3 如何处理第三方库不兼容?

**问题**: 某些库不支持 Next.js 16

**解答**:

**步骤**:

1. 查看库的 GitHub Issues
2. 查找是否有更新版本
3. 寻找替代方案
4. 提交 issue 给库作者

**示例**:

```bash
# 查看库的版本
npm list package-name

# 查看可用版本
npm view package-name versions

# 升级到最新版本
npm install package-name@latest
```

---

### 10.4 升级后性能下降?

**问题**: 页面加载变慢

**解答**:

**检查项**:

1. **fetch 缓存策略**:

```typescript
// 检查是否明确指定了缓存策略
fetch(url, { cache: "force-cache" });
```

2. **使用新的缓存 API**:

```typescript
"use cache";

import { unstable_cacheLife as cacheLife } from "next/cache";

export async function getData() {
  cacheLife("hours");
  return await fetchData();
}
```

3. **使用性能分析工具**:

```bash
# 分析 Bundle
ANALYZE=true npm run build
```

---

### 10.5 如何处理数据库迁移?

**问题**: Prisma 或其他 ORM 需要更新吗

**解答**:

通常不需要更新数据库,但建议:

1. 备份数据库
2. 测试环境验证
3. 检查 ORM 兼容性

**Prisma 示例**:

```bash
# 检查 Prisma 版本
npm list @prisma/client

# 升级 Prisma
npm install @prisma/client@latest prisma@latest

# 重新生成客户端
npx prisma generate
```

---

### 10.6 如何处理环境变量?

**问题**: 环境变量配置需要改变吗

**解答**:

环境变量配置不需要改变,但建议检查:

```bash
# .env.local
DATABASE_URL=postgresql://...
NEXT_PUBLIC_API_URL=https://api.example.com
```

**注意**:

- 服务端变量: 直接使用
- 客户端变量: 必须以 `NEXT_PUBLIC_` 开头

---

### 10.7 如何处理静态导出?

**问题**: 静态导出还能用吗

**解答**:

静态导出仍然支持:

```javascript
// next.config.js
module.exports = {
  output: "export",
};
```

**注意**:

- 不能使用 Server Actions
- 不能使用动态路由(除非使用 generateStaticParams)
- 不能使用 Image Optimization

---

## 11. 迁移检查清单

### 11.1 升级前检查

- [ ] 备份代码和数据库
- [ ] 阅读官方迁移指南
- [ ] 检查第三方库兼容性
- [ ] 确保有足够的测试覆盖
- [ ] 制定回滚计划

---

### 11.2 升级中检查

- [ ] 更新依赖
- [ ] 更新 TypeScript 配置
- [ ] 更新 headers() 和 cookies()
- [ ] 更新 params 和 searchParams
- [ ] 更新 fetch 缓存策略
- [ ] 运行类型检查
- [ ] 运行 ESLint
- [ ] 测试构建
- [ ] 运行测试

---

### 11.3 升级后检查

- [ ] 功能测试
- [ ] 性能测试
- [ ] 安全测试
- [ ] 兼容性测试
- [ ] 部署到测试环境
- [ ] 部署到生产环境
- [ ] 监控性能指标

---

## 12. 最佳实践

### 12.1 渐进式迁移

**步骤**:

1. 创建新分支
2. 升级依赖
3. 修复编译错误
4. 修复运行时错误
5. 测试验证
6. 逐步合并

**好处**:

- 降低风险
- 易于回滚
- 团队协作

---

### 12.2 自动化测试

**单元测试**:

```typescript
// __tests__/utils.test.ts
import { formatDate } from "@/lib/utils";

describe("formatDate", () => {
  it("formats date correctly", () => {
    const date = new Date("2024-01-01");
    expect(formatDate(date)).toBe("2024-01-01");
  });
});
```

**E2E 测试**:

```typescript
// e2e/login.spec.ts
import { test, expect } from "@playwright/test";

test("login flow", async ({ page }) => {
  await page.goto("http://localhost:3000/login");

  await page.fill('input[name="email"]', "test@example.com");
  await page.fill('input[name="password"]', "password");
  await page.click('button[type="submit"]');

  await expect(page).toHaveURL("http://localhost:3000/dashboard");
});
```

---

### 12.3 代码审查

**审查重点**:

1. 所有 async/await 是否正确
2. 类型定义是否正确
3. 缓存策略是否合理
4. 错误处理是否完善

**示例**:

```typescript
// ✅ 好的代码
export default async function Page({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  try {
    const { id } = await params;
    const data = await fetchData(id);

    return <div>{data.title}</div>;
  } catch (error) {
    console.error("Error:", error);
    throw error;
  }
}
```

---

### 12.4 文档更新

**更新内容**:

1. README.md
2. API 文档
3. 部署文档
4. 开发指南

**示例**:

```markdown
# README.md

## 技术栈

- Next.js 16
- React 19
- TypeScript 5
- Tailwind CSS 3

## 开发

\`\`\`bash
npm install
npm run dev
\`\`\`

## 构建

\`\`\`bash
npm run build
npm run start
\`\`\`
```

---

## 13. 故障排除

### 13.1 构建错误

**错误**: Build failed

**解决方案**:

```bash
# 1. 清理缓存
rm -rf .next

# 2. 重新安装依赖
rm -rf node_modules
npm install

# 3. 运行类型检查
npm run type-check

# 4. 运行 ESLint
npm run lint

# 5. 重新构建
npm run build
```

---

### 13.2 运行时错误

**错误**: Runtime error

**解决方案**:

1. 查看浏览器控制台
2. 查看服务器日志
3. 使用调试工具
4. 添加错误边界

```typescript
// app/error.tsx
"use client";

export default function Error({
  error,
  reset,
}: {
  error: Error;
  reset: () => void;
}) {
  return (
    <div>
      <h2>出错了!</h2>
      <p>{error.message}</p>
      <button onClick={reset}>重试</button>
    </div>
  );
}
```

---

### 13.3 性能问题

**问题**: 页面加载慢

**解决方案**:

1. **使用缓存**:

```typescript
"use cache";

export async function getData() {
  return await fetchData();
}
```

2. **使用动态导入**:

```typescript
import dynamic from "next/dynamic";

const HeavyComponent = dynamic(() => import("./HeavyComponent"), {
  loading: () => <div>Loading...</div>,
});
```

3. **优化图片**:

```typescript
import Image from "next/image";

export function Avatar() {
  return (
    <Image src="/avatar.jpg" alt="Avatar" width={200} height={200} priority />
  );
}
```

---

## 14. 总结

### 14.1 迁移步骤总结

1. **准备阶段**:

   - 备份代码
   - 阅读文档
   - 检查兼容性

2. **升级阶段**:

   - 更新依赖
   - 更新代码
   - 运行测试

3. **验证阶段**:

   - 功能测试
   - 性能测试
   - 部署测试

4. **上线阶段**:
   - 部署到生产
   - 监控性能
   - 收集反馈

### 14.2 关键变更

1. **headers() 和 cookies()**: 变为异步
2. **params 和 searchParams**: 变为 Promise
3. **fetch 缓存**: 默认策略改变
4. **新特性**: 'use cache', cacheLife(), cacheTag(), after()

### 14.3 最佳实践

1. **渐进式迁移**: 降低风险
2. **自动化测试**: 确保质量
3. **代码审查**: 发现问题
4. **文档更新**: 保持同步

### 14.4 下一步

迁移完成后,建议:

- 09-22: 版本差异速查表
- 09-21: 错误代码与解决方案
- 09-24: 常见错误排查手册

本指南会持续更新,欢迎提交反馈和建议。

// ✅ Next.js 16
export default async function Page({
params,
searchParams
}: {
params: Promise<{ id: string }>
searchParams: Promise<{ q: string }>
}) {
const { id } = await params
const { q } = await searchParams

return <div>ID: {id}</div>
}

````

**更新 generateMetadata**:

```typescript
// ❌ Next.js 15
export function generateMetadata({ params }: { params: { id: string } }) {
  return {
    title: `Post ${params.id}`
  }
}

// ✅ Next.js 16
export async function generateMetadata({
  params
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params

  return {
    title: `Post ${id}`
  }
}
````

---

### 2.6 步骤 6: 更新 fetch 缓存策略

**查找所有 fetch 调用**:

```bash
grep -r "fetch(" app/
```

**更新代码**:

```typescript
// ❌ Next.js 15 (默认缓存)
fetch("https://api.example.com/data");

// ✅ Next.js 16 (明确指定)
// 需要缓存
fetch("https://api.example.com/data", { cache: "force-cache" });

// 不需要缓存
fetch("https://api.example.com/data", { cache: "no-store" });

// 重新验证
fetch("https://api.example.com/data", { next: { revalidate: 3600 } });
```

---

### 2.7 步骤 7: 运行类型检查

```bash
# 运行 TypeScript 类型检查
npm run type-check

# 或
npx tsc --noEmit
```

**修复类型错误**:

1. 查看错误信息
2. 根据错误提示修改代码
3. 重新运行类型检查
4. 重复直到没有错误

---

### 2.8 步骤 8: 运行 ESLint

```bash
npm run lint
```

**修复 lint 错误**:

```bash
# 自动修复
npm run lint -- --fix
```

---

### 2.9 步骤 9: 测试构建

```bash
# 清理缓存
rm -rf .next

# 构建项目
npm run build
```

**常见构建错误**:

1. 类型错误: 检查 TypeScript 配置
2. 模块错误: 检查依赖版本
3. 缓存错误: 清理 .next 目录

---

### 2.10 步骤 10: 运行测试

```bash
# 运行所有测试
npm test

# 运行特定测试
npm test -- path/to/test
```

---

## 3. 常见迁移场景

### 3.1 迁移中间件

**Next.js 15**:

```typescript
// middleware.ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export function middleware(request: NextRequest) {
  const token = request.cookies.get("token");

  if (!token) {
    return NextResponse.redirect(new URL("/login", request.url));
  }

  return NextResponse.next();
}
```

**Next.js 16**:

```typescript
// middleware.ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export async function middleware(request: NextRequest) {
  const cookieStore = await request.cookies;
  const token = cookieStore.get("token");

  if (!token) {
    return NextResponse.redirect(new URL("/login", request.url));
  }

  return NextResponse.next();
}
```

---

### 3.2 迁移 API 路由

**Next.js 15**:

```typescript
// app/api/posts/route.ts
import { NextRequest, NextResponse } from "next/server";
import { headers } from "next/headers";

export async function GET(request: NextRequest) {
  const headersList = headers();
  const userAgent = headersList.get("user-agent");

  return NextResponse.json({ userAgent });
}
```

**Next.js 16**:

```typescript
// app/api/posts/route.ts
import { NextRequest, NextResponse } from "next/server";
import { headers } from "next/headers";

export async function GET(request: NextRequest) {
  const headersList = await headers();
  const userAgent = headersList.get("user-agent");

  return NextResponse.json({ userAgent });
}
```

---

### 3.3 迁移动态路由

**Next.js 15**:

```typescript
// app/blog/[slug]/page.tsx
export default function BlogPost({ params }: { params: { slug: string } }) {
  return <h1>{params.slug}</h1>;
}

export async function generateStaticParams() {
  const posts = await getPosts();

  return posts.map((post) => ({
    slug: post.slug,
  }));
}
```

**Next.js 16**:

```typescript
// app/blog/[slug]/page.tsx
export default async function BlogPost({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  const { slug } = await params;

  return <h1>{slug}</h1>;
}

export async function generateStaticParams() {
  const posts = await getPosts();

  return posts.map((post) => ({
    slug: post.slug,
  }));
}
```

---

## 4. 性能优化

### 4.1 使用新的缓存 API

```typescript
"use cache";

import { unstable_cacheLife as cacheLife } from "next/cache";

export async function getProducts() {
  cacheLife("hours");

  const products = await db.product.findMany();
  return products;
}
```

---

### 4.2 使用 cacheTag

```typescript
"use cache";

import { unstable_cacheTag as cacheTag } from "next/cache";

export async function getPosts() {
  cacheTag("posts");

  const posts = await db.post.findMany();
  return posts;
}
```

**重新验证**:

```typescript
"use server";

import { revalidateTag } from "next/cache";

export async function createPost(data: any) {
  await db.post.create({ data });
  revalidateTag("posts");
}
```

---

### 4.3 使用 after()

```typescript
import { after } from "next/server";

export async function POST(request: Request) {
  const data = await request.json();

  const response = NextResponse.json({ success: true });

  after(async () => {
    await sendEmail(data.email);
    await logActivity(data);
  });

  return response;
}
```

---

## 5. 测试策略

### 5.1 单元测试

```typescript
// __tests__/page.test.tsx
import { render, screen } from "@testing-library/react";
import Page from "@/app/page";

describe("Page", () => {
  it("renders correctly", () => {
    render(<Page />);
    expect(screen.getByRole("heading")).toBeInTheDocument();
  });
});
```

---

### 5.2 集成测试

```typescript
// __tests__/api.test.ts
import { GET } from "@/app/api/posts/route";

describe("API", () => {
  it("returns posts", async () => {
    const response = await GET(new Request("http://localhost:3000/api/posts"));
    const data = await response.json();

    expect(data.posts).toBeDefined();
  });
});
```

---

### 5.3 E2E 测试

```typescript
// e2e/home.spec.ts
import { test, expect } from "@playwright/test";

test("home page", async ({ page }) => {
  await page.goto("http://localhost:3000");

  await expect(page.getByRole("heading")).toBeVisible();
});
```

---

## 6. 部署

### 6.1 Vercel 部署

```bash
# 安装 Vercel CLI
npm install -g vercel

# 部署
vercel
```

---

### 6.2 自托管部署

```bash
# 构建
npm run build

# 启动
npm run start
```

**Docker 部署**:

```dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

---

## 7. 回滚计划

### 7.1 如何回滚

```bash
# 回滚依赖
npm install next@15 react@19.0.0-rc react-dom@19.0.0-rc

# 恢复代码
git checkout previous-version

# 重新构建
npm run build
```

---

### 7.2 回滚检查清单

- [ ] 备份数据库
- [ ] 通知团队
- [ ] 回滚代码
- [ ] 回滚依赖
- [ ] 测试验证
- [ ] 部署上线

---

## 8. 适用场景

### 8.1 小型项目

**特点**:

- 代码量少
- 依赖简单
- 团队小

**建议**:

- 一次性升级
- 1-2 天完成

---

### 8.2 中型项目

**特点**:

- 代码量中等
- 依赖较多
- 团队中等

**建议**:

- 分阶段升级
- 3-5 天完成

---

### 8.3 大型项目

**特点**:

- 代码量大
- 依赖复杂
- 团队大

**建议**:

- 渐进式升级
- 1-2 周完成

---

## 9. 注意事项

### 9.1 测试覆盖

确保有足够的测试覆盖,特别是:

- 页面组件
- API 路由
- Server Actions
- 中间件

---

### 9.2 依赖兼容性

检查第三方库是否兼容 Next.js 16:

```bash
npm outdated
```

---

### 9.3 性能监控

升级后监控性能指标:

- 构建时间
- 首次内容绘制
- 最大内容绘制
- 交互时间

---

## 10. 常见问题

### 10.1 升级后构建失败?

**问题**: 运行 npm run build 失败

**解答**:

1. 清理缓存: `rm -rf .next`
2. 重新安装依赖: `rm -rf node_modules && npm install`
3. 检查 TypeScript 错误: `npm run type-check`
4. 检查 ESLint 错误: `npm run lint`
