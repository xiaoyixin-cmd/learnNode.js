**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 05-14-文件上传与处理

## 概述

文件上传是 Web 应用中常见的功能需求。Next.js 16 通过 Server Actions 提供了原生的文件上传支持,无需额外的 API 路由。本文将详细介绍如何在 Next.js 16 中实现文件上传、处理、验证和优化。

### 核心特性

- **Server Actions 支持**: 直接在 Server Actions 中处理文件
- **FormData API**: 使用标准的 FormData API
- **流式上传**: 支持大文件流式上传
- **类型安全**: TypeScript 类型支持
- **进度跟踪**: 实时上传进度
- **多文件上传**: 同时上传多个文件
- **文件验证**: 类型、大小、格式验证
- **云存储集成**: 与 S3、Cloudinary 等集成

### 文件上传方式对比

| 方式           | 优点           | 缺点             | 适用场景   |
| -------------- | -------------- | ---------------- | ---------- |
| Server Actions | 简单、类型安全 | 需要 Next.js 16+ | 现代应用   |
| API Routes     | 灵活、兼容性好 | 代码量大         | 传统应用   |
| 第三方服务     | 功能强大       | 依赖外部服务     | 大规模应用 |
| 直接上传       | 性能好         | 安全性低         | 简单场景   |

### 工作原理

```
┌─────────────┐
│   客户端    │
│  选择文件   │
└──────┬──────┘
       │
       │ FormData
       ▼
┌─────────────┐
│Server Action│
│  接收文件   │
└──────┬──────┘
       │
       │ 验证
       ▼
┌─────────────┐
│  文件处理   │
│ 保存/上传   │
└──────┬──────┘
       │
       │ 返回结果
       ▼
┌─────────────┐
│   客户端    │
│  显示结果   │
└─────────────┘
```

---

## 1. 基础用法

### 1.1 简单文件上传

```tsx
// app/actions/upload.ts
"use server";

import { writeFile } from "fs/promises";
import { join } from "path";

export async function uploadFile(formData: FormData) {
  const file = formData.get("file") as File;

  if (!file) {
    return { success: false, error: "没有选择文件" };
  }

  try {
    const bytes = await file.arrayBuffer();
    const buffer = Buffer.from(bytes);

    // 保存到public目录
    const path = join(process.cwd(), "public", "uploads", file.name);
    await writeFile(path, buffer);

    return { success: true, path: `/uploads/${file.name}` };
  } catch (error) {
    return { success: false, error: "上传失败" };
  }
}
```

```tsx
// app/components/SimpleUploadForm.tsx
"use client";

import { uploadFile } from "../actions/upload";
import { useState } from "react";

export function SimpleUploadForm() {
  const [uploading, setUploading] = useState(false);
  const [result, setResult] = useState<string | null>(null);

  async function handleSubmit(formData: FormData) {
    setUploading(true);
    const response = await uploadFile(formData);
    setUploading(false);

    if (response.success) {
      setResult(response.path);
    } else {
      alert(response.error);
    }
  }

  return (
    <form action={handleSubmit}>
      <input type="file" name="file" required />
      <button type="submit" disabled={uploading}>
        {uploading ? "上传中..." : "上传"}
      </button>
      {result && (
        <div>
          <p>上传成功!</p>
          <img src={result} alt="上传的图片" />
        </div>
      )}
    </form>
  );
}
```

### 1.2 使用 useActionState

```tsx
// app/components/UploadFormWithState.tsx
"use client";

import { useActionState } from "react";
import { uploadFile } from "../actions/upload";

export function UploadFormWithState() {
  const [state, formAction, isPending] = useActionState(uploadFile, {});

  return (
    <form action={formAction}>
      <input type="file" name="file" required />
      <button type="submit" disabled={isPending}>
        {isPending ? "上传中..." : "上传"}
      </button>

      {state.success && (
        <div className="success">
          <p>上传成功!</p>
          <img src={state.path} alt="上传的图片" />
        </div>
      )}

      {state.error && (
        <div className="error">
          <p>{state.error}</p>
        </div>
      )}
    </form>
  );
}
```

### 1.3 多文件上传

```tsx
// app/actions/upload-multiple.ts
"use server";

import { writeFile } from "fs/promises";
import { join } from "path";

export async function uploadMultipleFiles(formData: FormData) {
  const files = formData.getAll("files") as File[];

  if (files.length === 0) {
    return { success: false, error: "没有选择文件" };
  }

  try {
    const uploadedPaths: string[] = [];

    for (const file of files) {
      const bytes = await file.arrayBuffer();
      const buffer = Buffer.from(bytes);
      const path = join(process.cwd(), "public", "uploads", file.name);
      await writeFile(path, buffer);
      uploadedPaths.push(`/uploads/${file.name}`);
    }

    return { success: true, paths: uploadedPaths };
  } catch (error) {
    return { success: false, error: "上传失败" };
  }
}
```

```tsx
// app/components/MultipleUploadForm.tsx
"use client";

import { uploadMultipleFiles } from "../actions/upload-multiple";
import { useState } from "react";

export function MultipleUploadForm() {
  const [uploading, setUploading] = useState(false);
  const [paths, setPaths] = useState<string[]>([]);

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setUploading(true);

    const formData = new FormData(e.currentTarget);
    const response = await uploadMultipleFiles(formData);
    setUploading(false);

    if (response.success) {
      setPaths(response.paths);
    } else {
      alert(response.error);
    }
  }

  return (
    <form onSubmit={handleSubmit}>
      <input type="file" name="files" multiple required />
      <button type="submit" disabled={uploading}>
        {uploading ? "上传中..." : "上传"}
      </button>

      {paths.length > 0 && (
        <div className="grid grid-cols-3 gap-4">
          {paths.map((path, index) => (
            <img key={index} src={path} alt={`上传的图片 ${index + 1}`} />
          ))}
        </div>
      )}
    </form>
  );
}
```

---

## 2. 文件验证

### 2.1 基础验证

```tsx
// app/lib/file-validation.ts
export interface FileValidationOptions {
  maxSize?: number; // 字节
  allowedTypes?: string[];
  allowedExtensions?: string[];
}

export function validateFile(
  file: File,
  options: FileValidationOptions = {}
): { valid: boolean; error?: string } {
  const {
    maxSize = 5 * 1024 * 1024, // 默认5MB
    allowedTypes = ["image/jpeg", "image/png", "image/webp"],
    allowedExtensions = [".jpg", ".jpeg", ".png", ".webp"],
  } = options;

  // 检查文件大小
  if (file.size > maxSize) {
    return {
      valid: false,
      error: `文件大小不能超过 ${maxSize / 1024 / 1024}MB`,
    };
  }

  // 检查文件类型
  if (!allowedTypes.includes(file.type)) {
    return {
      valid: false,
      error: `不支持的文件类型: ${file.type}`,
    };
  }

  // 检查文件扩展名
  const extension = file.name
    .toLowerCase()
    .substring(file.name.lastIndexOf("."));
  if (!allowedExtensions.includes(extension)) {
    return {
      valid: false,
      error: `不支持的文件扩展名: ${extension}`,
    };
  }

  return { valid: true };
}
```

```tsx
// app/actions/upload-with-validation.ts
"use server";

import { writeFile } from "fs/promises";
import { join } from "path";
import { validateFile } from "../lib/file-validation";

export async function uploadWithValidation(formData: FormData) {
  const file = formData.get("file") as File;

  if (!file) {
    return { success: false, error: "没有选择文件" };
  }

  // 验证文件
  const validation = validateFile(file, {
    maxSize: 10 * 1024 * 1024, // 10MB
    allowedTypes: ["image/jpeg", "image/png", "image/webp"],
  });

  if (!validation.valid) {
    return { success: false, error: validation.error };
  }

  try {
    const bytes = await file.arrayBuffer();
    const buffer = Buffer.from(bytes);

    // 生成唯一文件名
    const uniqueName = `${Date.now()}-${file.name}`;
    const path = join(process.cwd(), "public", "uploads", uniqueName);
    await writeFile(path, buffer);

    return { success: true, path: `/uploads/${uniqueName}` };
  } catch (error) {
    return { success: false, error: "上传失败" };
  }
}
```

### 2.2 使用 Zod 验证

```tsx
// app/lib/file-schemas.ts
import { z } from "zod";

export const imageFileSchema = z
  .instanceof(File)
  .refine((file) => file.size <= 5 * 1024 * 1024, {
    message: "文件大小不能超过5MB",
  })
  .refine(
    (file) => ["image/jpeg", "image/png", "image/webp"].includes(file.type),
    {
      message: "只支持JPEG、PNG、WebP格式",
    }
  );

export const documentFileSchema = z
  .instanceof(File)
  .refine((file) => file.size <= 10 * 1024 * 1024, {
    message: "文件大小不能超过10MB",
  })
  .refine(
    (file) => ["application/pdf", "application/msword"].includes(file.type),
    {
      message: "只支持PDF、DOC格式",
    }
  );

export const uploadSchema = z.object({
  file: imageFileSchema,
  title: z.string().min(1, "标题不能为空"),
  description: z.string().optional(),
});
```

```tsx
// app/actions/upload-with-zod.ts
"use server";

import { writeFile } from "fs/promises";
import { join } from "path";
import { uploadSchema } from "../lib/file-schemas";

export async function uploadWithZod(formData: FormData) {
  const file = formData.get("file") as File;
  const title = formData.get("title") as string;
  const description = formData.get("description") as string;

  // 使用Zod验证
  const result = uploadSchema.safeParse({ file, title, description });

  if (!result.success) {
    return {
      success: false,
      errors: result.error.flatten().fieldErrors,
    };
  }

  try {
    const bytes = await file.arrayBuffer();
    const buffer = Buffer.from(bytes);
    const uniqueName = `${Date.now()}-${file.name}`;
    const path = join(process.cwd(), "public", "uploads", uniqueName);
    await writeFile(path, buffer);

    return {
      success: true,
      data: {
        path: `/uploads/${uniqueName}`,
        title,
        description,
      },
    };
  } catch (error) {
    return { success: false, errors: { _form: ["上传失败"] } };
  }
}
```

---

## 3. 高级用法

### 3.1 上传进度跟踪

```tsx
// app/components/UploadWithProgress.tsx
"use client";

import { useState } from "react";

export function UploadWithProgress() {
  const [progress, setProgress] = useState(0);
  const [uploading, setUploading] = useState(false);

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setUploading(true);
    setProgress(0);

    const formData = new FormData(e.currentTarget);
    const file = formData.get("file") as File;

    // 使用XMLHttpRequest跟踪进度
    const xhr = new XMLHttpRequest();

    xhr.upload.addEventListener("progress", (e) => {
      if (e.lengthComputable) {
        const percentComplete = (e.loaded / e.total) * 100;
        setProgress(percentComplete);
      }
    });

    xhr.addEventListener("load", () => {
      setUploading(false);
      if (xhr.status === 200) {
        alert("上传成功!");
      }
    });

    xhr.open("POST", "/api/upload");
    xhr.send(formData);
  }

  return (
    <form onSubmit={handleSubmit}>
      <input type="file" name="file" required />
      <button type="submit" disabled={uploading}>
        {uploading ? "上传中..." : "上传"}
      </button>

      {uploading && (
        <div className="progress-bar">
          <div className="progress-fill" style={{ width: `${progress}%` }} />
          <span>{Math.round(progress)}%</span>
        </div>
      )}
    </form>
  );
}
```

### 3.2 图片预览

```tsx
// app/components/UploadWithPreview.tsx
"use client";

import { useState } from "react";
import { uploadFile } from "../actions/upload";

export function UploadWithPreview() {
  const [preview, setPreview] = useState<string | null>(null);
  const [uploading, setUploading] = useState(false);

  function handleFileChange(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0];
    if (file) {
      const reader = new FileReader();
      reader.onloadend = () => {
        setPreview(reader.result as string);
      };
      reader.readAsDataURL(file);
    }
  }

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setUploading(true);

    const formData = new FormData(e.currentTarget);
    const response = await uploadFile(formData);
    setUploading(false);

    if (response.success) {
      alert("上传成功!");
      setPreview(null);
    } else {
      alert(response.error);
    }
  }

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="file"
        name="file"
        accept="image/*"
        onChange={handleFileChange}
        required
      />

      {preview && (
        <div className="preview">
          <img src={preview} alt="预览" className="max-w-md" />
        </div>
      )}

      <button type="submit" disabled={uploading || !preview}>
        {uploading ? "上传中..." : "上传"}
      </button>
    </form>
  );
}
```

### 3.3 拖拽上传

```tsx
// app/components/DragDropUpload.tsx
"use client";

import { useState } from "react";
import { uploadFile } from "../actions/upload";

export function DragDropUpload() {
  const [dragging, setDragging] = useState(false);
  const [file, setFile] = useState<File | null>(null);
  const [uploading, setUploading] = useState(false);

  function handleDragOver(e: React.DragEvent) {
    e.preventDefault();
    setDragging(true);
  }

  function handleDragLeave() {
    setDragging(false);
  }

  function handleDrop(e: React.DragEvent) {
    e.preventDefault();
    setDragging(false);

    const droppedFile = e.dataTransfer.files[0];
    if (droppedFile) {
      setFile(droppedFile);
    }
  }

  async function handleUpload() {
    if (!file) return;

    setUploading(true);
    const formData = new FormData();
    formData.append("file", file);

    const response = await uploadFile(formData);
    setUploading(false);

    if (response.success) {
      alert("上传成功!");
      setFile(null);
    } else {
      alert(response.error);
    }
  }

  return (
    <div>
      <div
        className={`drop-zone ${dragging ? "dragging" : ""}`}
        onDragOver={handleDragOver}
        onDragLeave={handleDragLeave}
        onDrop={handleDrop}
      >
        {file ? <p>已选择: {file.name}</p> : <p>拖拽文件到这里或点击选择</p>}
        <input
          type="file"
          onChange={(e) => setFile(e.target.files?.[0] || null)}
          className="hidden"
        />
      </div>

      {file && (
        <button onClick={handleUpload} disabled={uploading}>
          {uploading ? "上传中..." : "上传"}
        </button>
      )}
    </div>
  );
}
```

### 3.4 云存储集成(AWS S3)

```tsx
// app/actions/upload-to-s3.ts
"use server";

import { S3Client, PutObjectCommand } from "@aws-sdk/client-s3";

const s3Client = new S3Client({
  region: process.env.AWS_REGION!,
  credentials: {
    accessKeyId: process.env.AWS_ACCESS_KEY_ID!,
    secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY!,
  },
});

export async function uploadToS3(formData: FormData) {
  const file = formData.get("file") as File;

  if (!file) {
    return { success: false, error: "没有选择文件" };
  }

  try {
    const bytes = await file.arrayBuffer();
    const buffer = Buffer.from(bytes);

    const key = `uploads/${Date.now()}-${file.name}`;

    await s3Client.send(
      new PutObjectCommand({
        Bucket: process.env.AWS_S3_BUCKET!,
        Key: key,
        Body: buffer,
        ContentType: file.type,
      })
    );

    const url = `https://${process.env.AWS_S3_BUCKET}.s3.${process.env.AWS_REGION}.amazonaws.com/${key}`;

    return { success: true, url };
  } catch (error) {
    return { success: false, error: "上传失败" };
  }
}
```

### 3.5 图片压缩

```tsx
// app/lib/image-compression.ts
import sharp from "sharp";

export async function compressImage(file: File): Promise<Buffer> {
  const bytes = await file.arrayBuffer();
  const buffer = Buffer.from(bytes);

  return await sharp(buffer)
    .resize(1920, 1080, {
      fit: "inside",
      withoutEnlargement: true,
    })
    .jpeg({ quality: 80 })
    .toBuffer();
}
```

```tsx
// app/actions/upload-compressed.ts
"use server";

import { writeFile } from "fs/promises";
import { join } from "path";
import { compressImage } from "../lib/image-compression";

export async function uploadCompressed(formData: FormData) {
  const file = formData.get("file") as File;

  if (!file) {
    return { success: false, error: "没有选择文件" };
  }

  try {
    const compressedBuffer = await compressImage(file);
    const uniqueName = `${Date.now()}-${file.name}`;
    const path = join(process.cwd(), "public", "uploads", uniqueName);
    await writeFile(path, compressedBuffer);

    return { success: true, path: `/uploads/${uniqueName}` };
  } catch (error) {
    return { success: false, error: "上传失败" };
  }
}
```

---

## 4. 实战案例

### 4.1 头像上传系统

```tsx
// app/components/AvatarUpload.tsx
"use client";

import { useState } from "react";
import { uploadAvatar } from "../actions/upload-avatar";

export function AvatarUpload() {
  const [preview, setPreview] = useState<string | null>(null);
  const [uploading, setUploading] = useState(false);

  function handleFileChange(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0];
    if (file) {
      if (file.size > 2 * 1024 * 1024) {
        alert("头像大小不能超过2MB");
        return;
      }

      const reader = new FileReader();
      reader.onloadend = () => {
        setPreview(reader.result as string);
      };
      reader.readAsDataURL(file);
    }
  }

  async function handleUpload() {
    if (!preview) return;

    setUploading(true);
    const input = document.querySelector(
      'input[type="file"]'
    ) as HTMLInputElement;
    const file = input.files?.[0];

    if (file) {
      const formData = new FormData();
      formData.append("file", file);

      const response = await uploadAvatar(formData);
      setUploading(false);

      if (response.success) {
        alert("头像上传成功!");
      } else {
        alert(response.error);
      }
    }
  }

  return (
    <div className="avatar-upload">
      <div className="avatar-preview">
        {preview ? (
          <img
            src={preview}
            alt="头像预览"
            className="w-32 h-32 rounded-full"
          />
        ) : (
          <div className="w-32 h-32 rounded-full bg-gray-200" />
        )}
      </div>

      <input
        type="file"
        accept="image/*"
        onChange={handleFileChange}
        className="hidden"
        id="avatar-input"
      />

      <label htmlFor="avatar-input" className="btn">
        选择头像
      </label>

      {preview && (
        <button onClick={handleUpload} disabled={uploading}>
          {uploading ? "上传中..." : "上传"}
        </button>
      )}
    </div>
  );
}
```

### 4.2 文档管理系统

```tsx
// app/components/DocumentUpload.tsx
"use client";

import { useState } from "react";
import { uploadDocument } from "../actions/upload-document";

interface Document {
  id: string;
  name: string;
  size: number;
  url: string;
  uploadedAt: Date;
}

export function DocumentUpload() {
  const [documents, setDocuments] = useState<Document[]>([]);
  const [uploading, setUploading] = useState(false);

  async function handleUpload(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setUploading(true);

    const formData = new FormData(e.currentTarget);
    const response = await uploadDocument(formData);
    setUploading(false);

    if (response.success) {
      setDocuments([...documents, response.document]);
      e.currentTarget.reset();
    } else {
      alert(response.error);
    }
  }

  return (
    <div>
      <form onSubmit={handleUpload}>
        <input type="file" name="file" accept=".pdf,.doc,.docx" required />
        <input type="text" name="title" placeholder="文档标题" required />
        <button type="submit" disabled={uploading}>
          {uploading ? "上传中..." : "上传"}
        </button>
      </form>

      <div className="documents-list">
        {documents.map((doc) => (
          <div key={doc.id} className="document-item">
            <h3>{doc.name}</h3>
            <p>大小: {(doc.size / 1024).toFixed(2)} KB</p>
            <p>上传时间: {doc.uploadedAt.toLocaleString()}</p>
            <a href={doc.url} download>
              下载
            </a>
          </div>
        ))}
      </div>
    </div>
  );
}
```

---

## 5. 适用场景

### 5.1 用户头像上传

**需求**: 用户个人资料头像

**特点**:

- 文件大小限制(通常 2-5MB)
- 图片格式限制
- 需要预览
- 可能需要裁剪

### 5.2 文档管理

**需求**: 企业文档管理系统

**特点**:

- 支持多种文件格式
- 文件大小较大
- 需要分类管理
- 权限控制

### 5.3 图片库

**需求**: 图片展示和管理

**特点**:

- 批量上传
- 图片压缩
- 缩略图生成
- CDN 加速

### 5.4 视频上传

**需求**: 视频分享平台

**特点**:

- 大文件上传
- 分片上传
- 转码处理
- 进度跟踪

### 5.5 附件上传

**需求**: 邮件或表单附件

**特点**:

- 多文件上传
- 文件类型验证
- 临时存储
- 安全检查

---

## 6. 注意事项

### 6.1 文件大小限制

```tsx
// ✗ 错误:没有限制文件大小
export async function uploadFile(formData: FormData) {
  const file = formData.get("file") as File;
  // 直接上传,可能导致服务器资源耗尽
  await saveFile(file);
}

// ✓ 正确:限制文件大小
export async function uploadFile(formData: FormData) {
  const file = formData.get("file") as File;

  const maxSize = 10 * 1024 * 1024; // 10MB
  if (file.size > maxSize) {
    return { success: false, error: "文件大小不能超过10MB" };
  }

  await saveFile(file);
}
```

### 6.2 文件类型验证

```tsx
// ✗ 错误:只验证MIME类型
if (file.type === "image/jpeg") {
  // MIME类型可以被伪造
}

// ✓ 正确:验证MIME类型和扩展名
const allowedTypes = ["image/jpeg", "image/png"];
const allowedExtensions = [".jpg", ".jpeg", ".png"];
const extension = file.name.toLowerCase().substring(file.name.lastIndexOf("."));

if (
  !allowedTypes.includes(file.type) ||
  !allowedExtensions.includes(extension)
) {
  return { success: false, error: "不支持的文件类型" };
}
```

### 6.3 文件名安全

```tsx
// ✗ 错误:直接使用用户提供的文件名
const path = join("uploads", file.name); // 可能包含../等危险字符

// ✓ 正确:生成安全的文件名
import { randomUUID } from "crypto";

const extension = file.name.substring(file.name.lastIndexOf("."));
const safeName = `${randomUUID()}${extension}`;
const path = join("uploads", safeName);
```

### 6.4 存储路径安全

```tsx
// ✗ 错误:将文件保存在代码目录
const path = join(process.cwd(), "uploads", file.name);

// ✓ 正确:使用专门的上传目录
const path = join(process.cwd(), "public", "uploads", file.name);
// 或使用环境变量
const path = join(process.env.UPLOAD_DIR!, file.name);
```

### 6.5 清理临时文件

```tsx
// ✓ 正确:上传失败时清理临时文件
export async function uploadFile(formData: FormData) {
  const file = formData.get("file") as File;
  const tempPath = join(process.cwd(), "temp", file.name);

  try {
    await writeFile(tempPath, buffer);
    // 处理文件...
    await unlink(tempPath); // 清理临时文件
  } catch (error) {
    // 确保清理临时文件
    try {
      await unlink(tempPath);
    } catch {}
    return { success: false, error: "上传失败" };
  }
}
```

---

## 7. 常见问题

### 7.1 如何处理大文件上传?

**问题**: 上传大文件时超时或内存溢出。

**解决方案**: 使用分片上传。

```tsx
// app/components/ChunkedUpload.tsx
"use client";

import { useState } from "react";

const CHUNK_SIZE = 1024 * 1024; // 1MB

export function ChunkedUpload() {
  const [progress, setProgress] = useState(0);

  async function uploadFile(file: File) {
    const totalChunks = Math.ceil(file.size / CHUNK_SIZE);

    for (let i = 0; i < totalChunks; i++) {
      const start = i * CHUNK_SIZE;
      const end = Math.min(start + CHUNK_SIZE, file.size);
      const chunk = file.slice(start, end);

      const formData = new FormData();
      formData.append("chunk", chunk);
      formData.append("chunkIndex", String(i));
      formData.append("totalChunks", String(totalChunks));
      formData.append("fileName", file.name);

      await fetch("/api/upload-chunk", {
        method: "POST",
        body: formData,
      });

      setProgress(((i + 1) / totalChunks) * 100);
    }
  }

  return <div>分片上传组件</div>;
}
```

### 7.2 如何实现断点续传?

**问题**: 上传中断后需要重新开始。

**解决方案**: 记录已上传的分片。

```tsx
async function uploadWithResume(file: File) {
  const fileId = await getFileId(file);
  const uploadedChunks = await getUploadedChunks(fileId);

  for (let i = 0; i < totalChunks; i++) {
    if (uploadedChunks.includes(i)) {
      continue; // 跳过已上传的分片
    }

    // 上传分片...
  }
}
```

### 7.3 如何限制上传速率?

**问题**: 避免占用过多带宽。

**解决方案**: 使用限流。

```tsx
async function uploadWithRateLimit(file: File) {
  const delay = (ms: number) =>
    new Promise((resolve) => setTimeout(resolve, ms));

  for (const chunk of chunks) {
    await uploadChunk(chunk);
    await delay(100); // 每个分片间隔100ms
  }
}
```

### 7.4 如何处理并发上传?

**问题**: 同时上传多个文件。

**解决方案**: 使用 Promise.all 限制并发数。

```tsx
async function uploadMultiple(files: File[]) {
  const concurrency = 3; // 最多同时上传3个

  for (let i = 0; i < files.length; i += concurrency) {
    const batch = files.slice(i, i + concurrency);
    await Promise.all(batch.map((file) => uploadFile(file)));
  }
}
```

### 7.5 如何生成缩略图?

**问题**: 上传图片后需要生成缩略图。

**解决方案**: 使用 sharp 库。

```tsx
import sharp from "sharp";

export async function generateThumbnail(file: File) {
  const bytes = await file.arrayBuffer();
  const buffer = Buffer.from(bytes);

  return await sharp(buffer)
    .resize(200, 200, { fit: "cover" })
    .jpeg({ quality: 80 })
    .toBuffer();
}
```

### 7.6 如何处理文件重复?

**问题**: 避免上传重复文件。

**解决方案**: 使用文件哈希。

```tsx
import crypto from "crypto";

export async function getFileHash(file: File): Promise<string> {
  const bytes = await file.arrayBuffer();
  const buffer = Buffer.from(bytes);
  return crypto.createHash("md5").update(buffer).digest("hex");
}

export async function uploadUnique(formData: FormData) {
  const file = formData.get("file") as File;
  const hash = await getFileHash(file);

  // 检查文件是否已存在
  const existing = await findFileByHash(hash);
  if (existing) {
    return { success: true, path: existing.path, duplicate: true };
  }

  // 上传新文件
  return await uploadFile(formData);
}
```

### 7.7 如何实现拖拽排序?

**问题**: 上传多个文件后需要排序。

**解决方案**: 使用 react-beautiful-dnd。

```tsx
import { DragDropContext, Droppable, Draggable } from "react-beautiful-dnd";

export function SortableFileList({ files, onReorder }) {
  function handleDragEnd(result) {
    if (!result.destination) return;

    const items = Array.from(files);
    const [reorderedItem] = items.splice(result.source.index, 1);
    items.splice(result.destination.index, 0, reorderedItem);

    onReorder(items);
  }

  return (
    <DragDropContext onDragEnd={handleDragEnd}>
      <Droppable droppableId="files">
        {(provided) => (
          <div {...provided.droppableProps} ref={provided.innerRef}>
            {files.map((file, index) => (
              <Draggable key={file.id} draggableId={file.id} index={index}>
                {(provided) => (
                  <div
                    ref={provided.innerRef}
                    {...provided.draggableProps}
                    {...provided.dragHandleProps}
                  >
                    {file.name}
                  </div>
                )}
              </Draggable>
            ))}
            {provided.placeholder}
          </div>
        )}
      </Droppable>
    </DragDropContext>
  );
}
```

### 7.8 如何处理上传失败?

**问题**: 网络错误导致上传失败。

**解决方案**: 实现重试机制。

```tsx
async function uploadWithRetry(file: File, maxRetries = 3) {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await uploadFile(file);
    } catch (error) {
      if (i === maxRetries - 1) throw error;
      await new Promise((resolve) => setTimeout(resolve, 1000 * (i + 1)));
    }
  }
}
```

### 7.9 如何实现图片裁剪?

**问题**: 上传前需要裁剪图片。

**解决方案**: 使用 react-image-crop。

```tsx
import ReactCrop from "react-image-crop";
import "react-image-crop/dist/ReactCrop.css";

export function ImageCropper() {
  const [crop, setCrop] = useState({ aspect: 1 });
  const [image, setImage] = useState(null);

  async function handleCropComplete(crop) {
    const croppedImage = await getCroppedImg(image, crop);
    // 上传裁剪后的图片
  }

  return (
    <ReactCrop crop={crop} onChange={setCrop} onComplete={handleCropComplete}>
      <img src={image} />
    </ReactCrop>
  );
}
```

### 7.10 如何实现文件预览?

**问题**: 上传前预览文件内容。

**解决方案**: 根据文件类型选择预览方式。

```tsx
export function FilePreview({ file }: { file: File }) {
  const [preview, setPreview] = useState<string | null>(null);

  useEffect(() => {
    if (file.type.startsWith("image/")) {
      const reader = new FileReader();
      reader.onloadend = () => setPreview(reader.result as string);
      reader.readAsDataURL(file);
    } else if (file.type === "application/pdf") {
      setPreview(URL.createObjectURL(file));
    }
  }, [file]);

  if (file.type.startsWith("image/")) {
    return <img src={preview} alt="预览" />;
  } else if (file.type === "application/pdf") {
    return <iframe src={preview} />;
  } else {
    return <p>不支持预览此文件类型</p>;
  }
}
```

---

## 8. 总结

### 8.1 核心要点

1. **Server Actions**: 使用 Server Actions 简化文件上传
2. **文件验证**: 验证文件大小、类型、扩展名
3. **安全性**: 文件名安全、路径安全、类型验证
4. **用户体验**: 进度跟踪、预览、拖拽上传
5. **性能优化**: 图片压缩、分片上传、并发控制

### 8.2 最佳实践

| 实践       | 说明               | 优先级 |
| ---------- | ------------------ | ------ |
| 文件验证   | 大小、类型、扩展名 | 高     |
| 安全文件名 | 使用 UUID 或哈希   | 高     |
| 错误处理   | 友好的错误消息     | 高     |
| 进度跟踪   | 实时上传进度       | 中     |
| 图片压缩   | 减少存储空间       | 中     |

### 8.3 上传方案对比

| 方案           | 优点           | 缺点             | 适用场景   |
| -------------- | -------------- | ---------------- | ---------- |
| Server Actions | 简单、类型安全 | 需要 Next.js 16+ | 现代应用   |
| API Routes     | 灵活、兼容性好 | 代码量大         | 传统应用   |
| 云存储         | 可扩展、CDN    | 成本高           | 大规模应用 |
| 分片上传       | 支持大文件     | 实现复杂         | 大文件场景 |

### 8.4 下一步

学习完文件上传后,建议继续学习:

1. **实时表单反馈**: 实现实时验证
2. **复杂表单架构设计**: 设计大型表单系统
3. **图片优化策略**: 优化图片加载
4. **CDN 集成**: 使用 CDN 加速文件访问
5. **安全最佳实践**: 文件上传安全

文件上传是 Web 应用的常见功能。通过正确使用 Next.js 16 的 Server Actions 和最佳实践,你可以构建安全、高效的文件上传系统。
