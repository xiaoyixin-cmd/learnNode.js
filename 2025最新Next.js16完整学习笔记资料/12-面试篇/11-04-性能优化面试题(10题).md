**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 性能优化面试题(10 题)

## 题目一: 如何优化 Next.js 应用的首屏加载性能?

### 问题描述

请说明优化 Next.js 应用首屏加载性能的方法和最佳实践。

### 参考答案

**1. 使用服务端渲染(SSR)**:

```typescript
// app/page.tsx
async function HomePage() {
  const data = await fetch("/api/data").then((r) => r.json());

  return (
    <div>
      <h1>{data.title}</h1>
      <p>{data.description}</p>
    </div>
  );
}

export default HomePage;
```

优点:

- 首屏内容立即可见
- SEO 友好
- 更快的 FCP (First Contentful Paint)

**2. 使用静态生成(SSG)**:

```typescript
// app/posts/[id]/page.tsx
export async function generateStaticParams() {
  const posts = await fetch("/api/posts").then((r) => r.json());

  return posts.map((post: any) => ({
    id: post.id,
  }));
}

async function PostPage({ params }: { params: { id: string } }) {
  const post = await fetch(`/api/posts/${params.id}`).then((r) => r.json());

  return <div>{post.title}</div>;
}

export default PostPage;
```

优点:

- 极快的加载速度
- 可以部署到 CDN
- 零服务器负载

**3. 使用流式渲染**:

```typescript
import { Suspense } from "react";

function HomePage() {
  return (
    <div>
      {/* 快速渲染的内容 */}
      <Header />

      {/* 慢速内容使用 Suspense */}
      <Suspense fallback={<Skeleton />}>
        <SlowComponent />
      </Suspense>

      <Suspense fallback={<Skeleton />}>
        <AnotherSlowComponent />
      </Suspense>
    </div>
  );
}
```

优点:

- 更快的 TTFB (Time to First Byte)
- 渐进式渲染
- 更好的用户体验

**4. 代码分割**:

```typescript
import dynamic from "next/dynamic";

// 动态导入组件
const HeavyComponent = dynamic(() => import("@/components/HeavyComponent"), {
  loading: () => <p>Loading...</p>,
  ssr: false, // 禁用服务端渲染
});

function Page() {
  return (
    <div>
      <h1>Page</h1>
      <HeavyComponent />
    </div>
  );
}
```

优点:

- 减小初始包大小
- 按需加载
- 更快的首屏加载

**5. 图片优化**:

```typescript
import Image from "next/image";

function Page() {
  return (
    <div>
      <Image
        src="/hero.jpg"
        alt="Hero"
        width={1200}
        height={600}
        priority // 优先加载
        placeholder="blur" // 模糊占位符
        blurDataURL="data:image/jpeg;base64,..."
      />

      <Image
        src="/product.jpg"
        alt="Product"
        width={400}
        height={300}
        loading="lazy" // 懒加载
      />
    </div>
  );
}
```

**6. 字体优化**:

```typescript
// app/layout.tsx
import { Inter } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  display: "swap", // 字体交换策略
  preload: true,
});

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="zh" className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
```

**7. 预加载关键资源**:

```typescript
// app/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html>
      <head>
        <link
          rel="preload"
          href="/fonts/custom.woff2"
          as="font"
          type="font/woff2"
          crossOrigin="anonymous"
        />
        <link rel="preconnect" href="https://api.example.com" />
        <link rel="dns-prefetch" href="https://cdn.example.com" />
      </head>
      <body>{children}</body>
    </html>
  );
}
```

**8. 减少 JavaScript 包大小**:

```typescript
// next.config.js
module.exports = {
  compiler: {
    removeConsole: process.env.NODE_ENV === "production",
  },
  experimental: {
    optimizePackageImports: ["lodash", "date-fns"],
  },
};
```

**首屏优化对比**:

| 方法     | FCP 改善 | LCP 改善 | 实现难度 | 适用场景 |
| :------- | :------- | :------- | :------- | :------- |
| SSR      | 高       | 高       | 低       | 动态内容 |
| SSG      | 极高     | 极高     | 低       | 静态内容 |
| 流式渲染 | 高       | 中       | 中       | 复杂页面 |
| 代码分割 | 中       | 中       | 低       | 大型应用 |
| 图片优化 | 中       | 高       | 低       | 图片密集 |
| 字体优化 | 中       | 低       | 低       | 所有场景 |
| 预加载   | 中       | 中       | 低       | 关键资源 |
| 减小包   | 高       | 中       | 中       | 所有场景 |

### 考察点

- 对首屏性能的理解
- 能否选择合适的优化方法
- 是否了解 Core Web Vitals

## 题目二: 如何优化 Next.js 应用的图片加载?

### 问题描述

请说明 Next.js 中图片优化的方法和最佳实践。

### 参考答案

**1. 使用 next/image**:

```typescript
import Image from "next/image";

function Page() {
  return (
    <div>
      {/* 本地图片 */}
      <Image
        src="/images/hero.jpg"
        alt="Hero"
        width={1200}
        height={600}
        quality={85} // 图片质量
      />

      {/* 远程图片 */}
      <Image
        src="https://example.com/image.jpg"
        alt="Remote"
        width={800}
        height={400}
        unoptimized={false} // 启用优化
      />
    </div>
  );
}
```

**2. 优先加载关键图片**:

```typescript
function Page() {
  return (
    <div>
      {/* 首屏图片 - 优先加载 */}
      <Image src="/hero.jpg" alt="Hero" width={1200} height={600} priority />

      {/* 非首屏图片 - 懒加载 */}
      <Image
        src="/product.jpg"
        alt="Product"
        width={400}
        height={300}
        loading="lazy"
      />
    </div>
  );
}
```

**3. 使用占位符**:

```typescript
import Image from "next/image";
import heroImage from "@/public/hero.jpg";

function Page() {
  return (
    <div>
      {/* 模糊占位符 */}
      <Image src={heroImage} alt="Hero" placeholder="blur" />

      {/* 自定义占位符 */}
      <Image
        src="/product.jpg"
        alt="Product"
        width={400}
        height={300}
        placeholder="blur"
        blurDataURL="data:image/jpeg;base64,/9j/4AAQSkZJRg..."
      />
    </div>
  );
}
```

**4. 响应式图片**:

```typescript
function Page() {
  return (
    <div>
      {/* 使用 sizes 属性 */}
      <Image
        src="/hero.jpg"
        alt="Hero"
        width={1200}
        height={600}
        sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
      />

      {/* 填充容器 */}
      <div style={{ position: "relative", width: "100%", height: "400px" }}>
        <Image
          src="/banner.jpg"
          alt="Banner"
          fill
          style={{ objectFit: "cover" }}
        />
      </div>
    </div>
  );
}
```

**5. 配置图片域名**:

```typescript
// next.config.js
module.exports = {
  images: {
    domains: ["example.com", "cdn.example.com"],
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**.example.com",
        port: "",
        pathname: "/images/**",
      },
    ],
    formats: ["image/avif", "image/webp"],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },
};
```

**6. 使用现代图片格式**:

```typescript
// next.config.js
module.exports = {
  images: {
    formats: ["image/avif", "image/webp"],
  },
};

// 组件中使用
<Image
  src="/image.jpg"
  alt="Image"
  width={800}
  height={600}
  // Next.js 会自动生成 AVIF 和 WebP 格式
/>;
```

### 考察点

- 对图片优化的理解
- 能否使用 next/image
- 是否了解现代图片格式

## 题目三: 如何优化 Next.js 应用的 JavaScript 包大小?

### 问题描述

请说明减小 Next.js 应用 JavaScript 包大小的方法。

### 参考答案

**1. 动态导入**:

```typescript
import dynamic from "next/dynamic";

// 动态导入组件
const Chart = dynamic(() => import("@/components/Chart"), {
  loading: () => <p>Loading chart...</p>,
  ssr: false,
});

const Modal = dynamic(() => import("@/components/Modal"));

function Page() {
  const [showModal, setShowModal] = useState(false);

  return (
    <div>
      <Chart data={data} />
      {showModal && <Modal onClose={() => setShowModal(false)} />}
    </div>
  );
}
```

**2. 优化导入**:

```typescript
// 不好 - 导入整个库
import _ from "lodash";
import { format } from "date-fns";

// 好 - 只导入需要的函数
import debounce from "lodash/debounce";
import { format } from "date-fns/format";

// 更好 - 使用 tree-shaking 友好的库
import { debounce } from "lodash-es";
```

**3. 配置包优化**:

```typescript
// next.config.js
module.exports = {
  compiler: {
    removeConsole: process.env.NODE_ENV === "production",
  },
  experimental: {
    optimizePackageImports: ["lodash", "date-fns", "react-icons"],
  },
  webpack: (config, { isServer }) => {
    if (!isServer) {
      config.optimization.splitChunks = {
        chunks: "all",
        cacheGroups: {
          default: false,
          vendors: false,
          commons: {
            name: "commons",
            chunks: "all",
            minChunks: 2,
          },
        },
      };
    }
    return config;
  },
};
```

**4. 使用 Bundle Analyzer**:

```bash
npm install @next/bundle-analyzer
```

```typescript
// next.config.js
const withBundleAnalyzer = require("@next/bundle-analyzer")({
  enabled: process.env.ANALYZE === "true",
});

module.exports = withBundleAnalyzer({
  // 其他配置
});
```

```bash
ANALYZE=true npm run build
```

**5. 移除未使用的依赖**:

```bash
# 检查未使用的依赖
npx depcheck

# 移除未使用的依赖
npm uninstall unused-package
```

**6. 使用轻量级替代品**:

| 重量级库    | 轻量级替代   | 大小对比     |
| :---------- | :----------- | :----------- |
| moment.js   | date-fns     | 67KB → 13KB  |
| lodash      | lodash-es    | 71KB → 24KB  |
| axios       | fetch API    | 13KB → 0KB   |
| react-icons | lucide-react | 1.2MB → 60KB |

**7. 代码分割策略**:

```typescript
// 路由级分割 - 自动
// app/dashboard/page.tsx
export default function Dashboard() {
  return <div>Dashboard</div>;
}

// 组件级分割 - 手动
const HeavyComponent = dynamic(() => import("@/components/Heavy"));

// 条件加载
function Page() {
  const [showAdmin, setShowAdmin] = useState(false);

  const AdminPanel = dynamic(() => import("@/components/AdminPanel"));

  return <div>{showAdmin && <AdminPanel />}</div>;
}
```

**包大小优化对比**:

| 方法            | 减小幅度 | 实现难度 | 适用场景 |
| :-------------- | :------- | :------- | :------- |
| 动态导入        | 20-40%   | 低       | 大组件   |
| 优化导入        | 10-30%   | 低       | 所有场景 |
| 包配置          | 5-15%    | 中       | 生产环境 |
| Bundle Analyzer | -        | 低       | 分析工具 |
| 移除依赖        | 10-50%   | 低       | 清理项目 |
| 轻量替代        | 30-80%   | 中       | 特定库   |
| 代码分割        | 20-50%   | 中       | 大型应用 |

### 考察点

- 对包大小优化的理解
- 能否使用动态导入
- 是否了解 tree-shaking

## 题目四: 如何优化 Next.js 应用的缓存策略?

### 问题描述

请说明 Next.js 中的缓存优化策略和最佳实践。

### 参考答案

**1. 数据缓存**:

```typescript
// 强制缓存
const data = await fetch("/api/data", {
  cache: "force-cache",
});

// 不缓存
const data = await fetch("/api/data", {
  cache: "no-store",
});

// 时间缓存
const data = await fetch("/api/data", {
  next: { revalidate: 60 },
});

// 标签缓存
const data = await fetch("/api/data", {
  next: { tags: ["posts"] },
});
```

**2. 路由缓存**:

```typescript
// 静态路由 - 自动缓存
export default async function Page() {
  const data = await fetch("/api/data");
  return <div>{data}</div>;
}

// 动态路由 - 配置缓存
export const revalidate = 60;

export default async function Page() {
  const data = await fetch("/api/data");
  return <div>{data}</div>;
}

// 禁用缓存
export const dynamic = "force-dynamic";
export const revalidate = 0;
```

**3. 客户端缓存 - SWR**:

```typescript
"use client";

import useSWR from "swr";

function Page() {
  const { data } = useSWR("/api/data", fetcher, {
    dedupingInterval: 2000,
    focusThrottleInterval: 5000,
    revalidateOnFocus: true,
    revalidateOnReconnect: true,
  });

  return <div>{data}</div>;
}
```

**4. 客户端缓存 - React Query**:

```typescript
"use client";

import { useQuery } from "@tanstack/react-query";

function Page() {
  const { data } = useQuery({
    queryKey: ["data"],
    queryFn: fetchData,
    staleTime: 5 * 60 * 1000,
    cacheTime: 10 * 60 * 1000,
  });

  return <div>{data}</div>;
}
```

**5. CDN 缓存**:

```typescript
// next.config.js
module.exports = {
  async headers() {
    return [
      {
        source: "/static/:path*",
        headers: [
          {
            key: "Cache-Control",
            value: "public, max-age=31536000, immutable",
          },
        ],
      },
      {
        source: "/api/:path*",
        headers: [
          {
            key: "Cache-Control",
            value: "public, s-maxage=60, stale-while-revalidate=30",
          },
        ],
      },
    ];
  },
};
```

**6. 按需重新验证**:

```typescript
import { revalidatePath, revalidateTag } from "next/cache";

// 重新验证路径
export async function POST() {
  revalidatePath("/posts");
  return Response.json({ revalidated: true });
}

// 重新验证标签
export async function POST() {
  revalidateTag("posts");
  return Response.json({ revalidated: true });
}
```

**缓存策略对比**:

| 策略        | 命中率 | 新鲜度 | 复杂度 | 适用场景 |
| :---------- | :----- | :----- | :----- | :------- |
| 数据缓存    | 高     | 中     | 低     | API 数据 |
| 路由缓存    | 高     | 低     | 低     | 静态页面 |
| SWR         | 中     | 高     | 低     | 实时数据 |
| React Query | 中     | 高     | 中     | 复杂状态 |
| CDN 缓存    | 极高   | 低     | 中     | 静态资源 |
| 按需验证    | 高     | 高     | 中     | 动态内容 |

### 考察点

- 对缓存机制的理解
- 能否选择合适的缓存策略
- 是否了解缓存层级

## 题目五: 如何优化 Next.js 应用的 Core Web Vitals?

### 问题描述

请说明如何优化 Next.js 应用的 Core Web Vitals 指标。

### 参考答案

**1. 优化 LCP (Largest Contentful Paint)**:

```typescript
// 使用 priority 预加载关键图片
import Image from "next/image";

function Hero() {
  return (
    <Image
      src="/hero.jpg"
      alt="Hero"
      width={1200}
      height={600}
      priority
      placeholder="blur"
    />
  );
}

// 预加载关键资源
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html>
      <head>
        <link
          rel="preload"
          href="/fonts/main.woff2"
          as="font"
          type="font/woff2"
          crossOrigin="anonymous"
        />
        <link rel="preconnect" href="https://api.example.com" />
      </head>
      <body>{children}</body>
    </html>
  );
}

// 使用 SSR 或 SSG
export default async function Page() {
  const data = await fetch("/api/data").then((r) => r.json());
  return <div>{data.content}</div>;
}
```

**2. 优化 FID (First Input Delay) / INP (Interaction to Next Paint)**:

```typescript
// 减少 JavaScript 执行时间
import dynamic from "next/dynamic";

const HeavyComponent = dynamic(() => import("@/components/Heavy"), {
  ssr: false,
});

// 使用 Web Workers
("use client");

import { useEffect, useState } from "react";

function Page() {
  const [result, setResult] = useState(null);

  useEffect(() => {
    const worker = new Worker(new URL("./worker.ts", import.meta.url));

    worker.postMessage({ data: largeData });

    worker.onmessage = (e) => {
      setResult(e.data);
    };

    return () => worker.terminate();
  }, []);

  return <div>{result}</div>;
}

// 优化事件处理
import { useCallback } from "react";
import debounce from "lodash/debounce";

function SearchInput() {
  const handleSearch = useCallback(
    debounce((value: string) => {
      // 搜索逻辑
    }, 300),
    []
  );

  return <input onChange={(e) => handleSearch(e.target.value)} />;
}
```

**3. 优化 CLS (Cumulative Layout Shift)**:

```typescript
// 为图片指定尺寸
import Image from "next/image";

function Page() {
  return (
    <Image
      src="/image.jpg"
      alt="Image"
      width={800}
      height={600}
      // 防止布局偏移
    />
  );
}

// 为动态内容预留空间
function Page() {
  return (
    <div>
      {/* 使用骨架屏 */}
      <Suspense fallback={<Skeleton height={400} />}>
        <DynamicContent />
      </Suspense>
    </div>
  );
}

// 避免在现有内容上方插入内容
function Page() {
  return (
    <div>
      {/* 固定高度的容器 */}
      <div style={{ minHeight: "400px" }}>
        <DynamicContent />
      </div>
    </div>
  );
}
```

**4. 优化 TTFB (Time to First Byte)**:

```typescript
// 使用边缘函数
export const runtime = "edge";

export async function GET() {
  const data = await fetch("/api/data");
  return Response.json(data);
}

// 使用流式渲染
import { Suspense } from "react";

function Page() {
  return (
    <div>
      <Suspense fallback={<Loading />}>
        <SlowComponent />
      </Suspense>
    </div>
  );
}

// 优化服务器响应
export const revalidate = 60;

export default async function Page() {
  const data = await fetch("/api/data", {
    next: { revalidate: 60 },
  });

  return <div>{data}</div>;
}
```

**5. 监控 Core Web Vitals**:

```typescript
// app/layout.tsx
import { SpeedInsights } from "@vercel/speed-insights/next";
import { Analytics } from "@vercel/analytics/react";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html>
      <body>
        {children}
        <SpeedInsights />
        <Analytics />
      </body>
    </html>
  );
}

// 自定义监控
("use client");

import { useReportWebVitals } from "next/web-vitals";

export function WebVitals() {
  useReportWebVitals((metric) => {
    console.log(metric);

    // 发送到分析服务
    if (metric.label === "web-vital") {
      window.gtag?.("event", metric.name, {
        value: Math.round(metric.value),
        event_label: metric.id,
        non_interaction: true,
      });
    }
  });

  return null;
}
```

**Core Web Vitals 优化对比**:

| 指标    | 目标值  | 优化方法              | 影响因素   |
| :------ | :------ | :-------------------- | :--------- |
| LCP     | < 2.5s  | 预加载、SSR、图片优化 | 资源加载   |
| FID/INP | < 100ms | 代码分割、Web Workers | JS 执行    |
| CLS     | < 0.1   | 固定尺寸、骨架屏      | 布局稳定   |
| TTFB    | < 800ms | 边缘函数、缓存        | 服务器响应 |
| FCP     | < 1.8s  | SSR、预加载           | 首次渲染   |

### 考察点

- 对 Core Web Vitals 的理解
- 能否优化各项指标
- 是否了解监控方法

## 题目六: 如何优化 Next.js 应用的数据获取性能?

### 问题描述

请说明优化 Next.js 数据获取性能的方法。

### 参考答案

**1. 并行数据获取**:

```typescript
// 不好 - 顺序获取
async function Page() {
  const posts = await fetchPosts(); // 1s
  const users = await fetchUsers(); // 1s
  const comments = await fetchComments(); // 1s
  // 总共 3s
}

// 好 - 并行获取
async function Page() {
  const [posts, users, comments] = await Promise.all([
    fetchPosts(),
    fetchUsers(),
    fetchComments(),
  ]);
  // 总共 1s
}
```

**2. 使用缓存**:

```typescript
// 默认缓存
const data = await fetch("/api/data");

// 时间缓存
const data = await fetch("/api/data", {
  next: { revalidate: 60 },
});

// 标签缓存
const data = await fetch("/api/data", {
  next: { tags: ["posts"] },
});
```

**3. 流式数据获取**:

```typescript
import { Suspense } from "react";

function Page() {
  return (
    <div>
      <Suspense fallback={<Loading />}>
        <Posts />
      </Suspense>

      <Suspense fallback={<Loading />}>
        <Users />
      </Suspense>
    </div>
  );
}

async function Posts() {
  const posts = await fetchPosts();
  return <div>{/* 渲染 */}</div>;
}
```

**4. 数据预取**:

```typescript
"use client";

import { useRouter } from "next/navigation";
import { useEffect } from "react";

function PostsList({ posts }) {
  const router = useRouter();

  useEffect(() => {
    // 预取详情页
    posts.forEach((post) => {
      router.prefetch(`/posts/${post.id}`);
    });
  }, [posts, router]);

  return <div>...</div>;
}
```

**5. 请求去重**:

```typescript
import { cache } from "react";

// 创建缓存函数
const getPost = cache(async (id: string) => {
  return await db.post.findUnique({ where: { id } });
});

// 多次调用只执行一次
async function Component1({ id }: { id: string }) {
  const post = await getPost(id);
  return <div>{post.title}</div>;
}

async function Component2({ id }: { id: string }) {
  const post = await getPost(id);
  return <div>{post.content}</div>;
}
```

**6. 分页加载**:

```typescript
async function PostsPage({
  searchParams,
}: {
  searchParams: { page?: string };
}) {
  const page = Number(searchParams.page) || 1;
  const limit = 10;

  const { posts, total } = await fetch(
    `/api/posts?page=${page}&limit=${limit}`
  ).then((r) => r.json());

  return (
    <div>
      {posts.map((post) => (
        <div key={post.id}>{post.title}</div>
      ))}
      <Pagination currentPage={page} totalPages={Math.ceil(total / limit)} />
    </div>
  );
}
```

**数据获取优化对比**:

| 方法     | 性能提升 | 实现难度 | 适用场景    |
| :------- | :------- | :------- | :---------- |
| 并行获取 | 高       | 低       | 多数据源    |
| 缓存     | 极高     | 低       | 静态/半静态 |
| 流式获取 | 高       | 中       | 复杂页面    |
| 预取     | 中       | 中       | 列表页面    |
| 请求去重 | 中       | 低       | 所有场景    |
| 分页     | 高       | 低       | 大数据集    |

### 考察点

- 对数据获取优化的理解
- 能否选择合适的方法
- 是否了解缓存机制

## 题目七: 如何优化 Next.js 应用的构建性能?

### 问题描述

请说明优化 Next.js 应用构建性能的方法。

### 参考答案

**1. 使用 Turbopack**:

```typescript
// next.config.js
module.exports = {
  experimental: {
    turbo: {
      // Turbopack 配置
    },
  },
};
```

```bash
# 开发模式使用 Turbopack
next dev --turbo

# 构建时使用 Turbopack
next build --turbo
```

**2. 增量构建**:

```typescript
// next.config.js
module.exports = {
  experimental: {
    incrementalCacheHandlerPath: "./cache-handler.js",
  },
};
```

**3. 并行构建**:

```typescript
// next.config.js
module.exports = {
  experimental: {
    workerThreads: true,
    cpus: 4, // 使用 4 个 CPU 核心
  },
};
```

**4. 优化依赖**:

```bash
# 使用 pnpm 代替 npm
pnpm install

# 使用 yarn 的 PnP 模式
yarn install --pnp
```

**5. 缓存构建结果**:

```yaml
# .github/workflows/build.yml
- name: Cache Next.js build
  uses: actions/cache@v3
  with:
    path: |
      ~/.npm
      ${{ github.workspace }}/.next/cache
    key: ${{ runner.os }}-nextjs-${{ hashFiles('**/package-lock.json') }}
```

**6. 减少构建内容**:

```typescript
// next.config.js
module.exports = {
  // 只构建需要的页面
  experimental: {
    outputFileTracingRoot: path.join(__dirname, "../../"),
  },

  // 排除不需要的文件
  webpack: (config) => {
    config.watchOptions = {
      ignored: /node_modules/,
      poll: 1000,
    };
    return config;
  },
};
```

**构建优化对比**:

| 方法      | 提升幅度 | 实现难度 | 适用场景 |
| :-------- | :------- | :------- | :------- |
| Turbopack | 50-70%   | 低       | 所有场景 |
| 增量构建  | 30-50%   | 中       | 大型项目 |
| 并行构建  | 20-40%   | 低       | 多核 CPU |
| 优化依赖  | 10-30%   | 低       | 所有场景 |
| 缓存结果  | 40-60%   | 中       | CI/CD    |
| 减少内容  | 20-40%   | 中       | 大型项目 |

### 考察点

- 对构建优化的理解
- 能否使用 Turbopack
- 是否了解增量构建

## 题目八: 如何优化 Next.js 应用的运行时性能?

### 问题描述

请说明优化 Next.js 应用运行时性能的方法。

### 参考答案

**1. 使用 React Compiler**:

```typescript
// next.config.js
module.exports = {
  experimental: {
    reactCompiler: true,
  },
};
```

**2. 优化渲染**:

```typescript
"use client";

import { memo, useMemo, useCallback } from "react";

// 使用 memo 避免不必要的重新渲染
const ExpensiveComponent = memo(function ExpensiveComponent({ data }) {
  return <div>{data}</div>;
});

function Page() {
  // 使用 useMemo 缓存计算结果
  const expensiveValue = useMemo(() => {
    return computeExpensiveValue(data);
  }, [data]);

  // 使用 useCallback 缓存函数
  const handleClick = useCallback(() => {
    console.log("clicked");
  }, []);

  return (
    <div>
      <ExpensiveComponent data={expensiveValue} />
      <button onClick={handleClick}>Click</button>
    </div>
  );
}
```

**3. 虚拟化长列表**:

```typescript
"use client";

import { useVirtualizer } from "@tanstack/react-virtual";
import { useRef } from "react";

function LongList({ items }: { items: any[] }) {
  const parentRef = useRef<HTMLDivElement>(null);

  const virtualizer = useVirtualizer({
    count: items.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 50,
    overscan: 5,
  });

  return (
    <div ref={parentRef} style={{ height: "600px", overflow: "auto" }}>
      <div
        style={{
          height: `${virtualizer.getTotalSize()}px`,
          position: "relative",
        }}
      >
        {virtualizer.getVirtualItems().map((virtualItem) => (
          <div
            key={virtualItem.key}
            style={{
              position: "absolute",
              top: 0,
              left: 0,
              width: "100%",
              height: `${virtualItem.size}px`,
              transform: `translateY(${virtualItem.start}px)`,
            }}
          >
            {items[virtualItem.index].name}
          </div>
        ))}
      </div>
    </div>
  );
}
```

**4. 使用 Web Workers**:

```typescript
"use client";

import { useEffect, useState } from "react";

function Page() {
  const [result, setResult] = useState(null);

  useEffect(() => {
    const worker = new Worker(new URL("./worker.ts", import.meta.url));

    worker.postMessage({ data: largeData });

    worker.onmessage = (e) => {
      setResult(e.data);
    };

    return () => worker.terminate();
  }, []);

  return <div>{result}</div>;
}

// worker.ts
self.onmessage = (e) => {
  const result = processLargeData(e.data);
  self.postMessage(result);
};
```

**5. 优化状态管理**:

```typescript
"use client";

import { create } from "zustand";

// 使用 Zustand 代替 Context
const useStore = create((set) => ({
  count: 0,
  increment: () => set((state) => ({ count: state.count + 1 })),
}));

function Counter() {
  const count = useStore((state) => state.count);
  const increment = useStore((state) => state.increment);

  return (
    <div>
      <p>{count}</p>
      <button onClick={increment}>Increment</button>
    </div>
  );
}
```

**6. 减少重新渲染**:

```typescript
"use client";

import { useTransition } from "react";

function SearchPage() {
  const [isPending, startTransition] = useTransition();
  const [query, setQuery] = useState("");
  const [results, setResults] = useState([]);

  const handleSearch = (value: string) => {
    setQuery(value);

    // 将搜索标记为低优先级
    startTransition(() => {
      const filtered = items.filter((item) => item.name.includes(value));
      setResults(filtered);
    });
  };

  return (
    <div>
      <input value={query} onChange={(e) => handleSearch(e.target.value)} />
      {isPending ? <div>Searching...</div> : <Results data={results} />}
    </div>
  );
}
```

**运行时优化对比**:

| 方法           | 性能提升 | 实现难度 | 适用场景 |
| :------------- | :------- | :------- | :------- |
| React Compiler | 20-40%   | 低       | 所有场景 |
| 优化渲染       | 30-50%   | 中       | 复杂组件 |
| 虚拟化         | 50-80%   | 中       | 长列表   |
| Web Workers    | 40-60%   | 高       | 重计算   |
| 状态管理       | 20-40%   | 中       | 大型应用 |
| 减少渲染       | 30-50%   | 中       | 频繁更新 |

### 考察点

- 对运行时优化的理解
- 能否使用 React Compiler
- 是否了解性能分析

## 题目九: 如何监控和分析 Next.js 应用性能?

### 问题描述

请说明监控和分析 Next.js 应用性能的方法。

### 参考答案

**1. 使用 Vercel Analytics**:

```typescript
// app/layout.tsx
import { Analytics } from "@vercel/analytics/react";
import { SpeedInsights } from "@vercel/speed-insights/next";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
        <SpeedInsights />
      </body>
    </html>
  );
}
```

**2. 使用 Web Vitals API**:

```typescript
"use client";

import { useReportWebVitals } from "next/web-vitals";

export function WebVitals() {
  useReportWebVitals((metric) => {
    console.log(metric);

    // 发送到分析服务
    fetch("/api/analytics", {
      method: "POST",
      body: JSON.stringify(metric),
    });
  });

  return null;
}
```

**3. 使用 React DevTools Profiler**:

```typescript
"use client";

import { Profiler } from "react";

function Page() {
  const onRender = (
    id: string,
    phase: "mount" | "update",
    actualDuration: number,
    baseDuration: number,
    startTime: number,
    commitTime: number
  ) => {
    console.log({
      id,
      phase,
      actualDuration,
      baseDuration,
      startTime,
      commitTime,
    });
  };

  return (
    <Profiler id="Page" onRender={onRender}>
      <div>Content</div>
    </Profiler>
  );
}
```

**4. 使用 Performance API**:

```typescript
"use client";

import { useEffect } from "react";

function Page() {
  useEffect(() => {
    // 测量性能
    const start = performance.now();

    // 执行操作
    doSomething();

    const end = performance.now();
    console.log(`Operation took ${end - start}ms`);

    // 获取性能条目
    const entries = performance.getEntriesByType("navigation");
    console.log(entries);
  }, []);

  return <div>Page</div>;
}
```

**5. 使用 Lighthouse**:

```bash
# 安装 Lighthouse
npm install -g lighthouse

# 运行 Lighthouse
lighthouse https://example.com --view

# 使用 CI 模式
lighthouse https://example.com --output=json --output-path=./report.json
```

**6. 使用 Bundle Analyzer**:

```bash
# 安装
npm install @next/bundle-analyzer

# 分析
ANALYZE=true npm run build
```

```typescript
// next.config.js
const withBundleAnalyzer = require("@next/bundle-analyzer")({
  enabled: process.env.ANALYZE === "true",
});

module.exports = withBundleAnalyzer({
  // 配置
});
```

**性能监控对比**:

| 工具             | 实时性 | 详细度 | 成本      | 适用场景 |
| :--------------- | :----- | :----- | :-------- | :------- |
| Vercel Analytics | 高     | 中     | 免费/付费 | 生产环境 |
| Web Vitals       | 高     | 高     | 免费      | 所有环境 |
| React Profiler   | 高     | 极高   | 免费      | 开发环境 |
| Performance API  | 高     | 高     | 免费      | 所有环境 |
| Lighthouse       | 低     | 极高   | 免费      | 测试环境 |
| Bundle Analyzer  | 低     | 极高   | 免费      | 构建时   |

### 考察点

- 对性能监控的理解
- 能否使用监控工具
- 是否了解性能指标

## 题目十: Next.js 性能优化的常见问题和解决方案

### 问题描述

请列举 Next.js 性能优化的常见问题及其解决方案。

### 参考答案

**问题一: 首屏加载慢**

原因: 包太大、未使用 SSR/SSG

解决方案:

```typescript
// 使用 SSR
export default async function Page() {
  const data = await fetch("/api/data").then((r) => r.json());
  return <div>{data}</div>;
}

// 使用代码分割
import dynamic from "next/dynamic";

const Heavy = dynamic(() => import("@/components/Heavy"), {
  ssr: false,
});

// 优化图片
import Image from "next/image";

<Image src="/hero.jpg" alt="Hero" width={1200} height={600} priority />;
```

**问题二: 图片加载慢**

原因: 未优化图片、未使用现代格式

解决方案:

```typescript
// next.config.js
module.exports = {
  images: {
    formats: ["image/avif", "image/webp"],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },
};

// 使用 next/image
<Image
  src="/image.jpg"
  alt="Image"
  width={800}
  height={600}
  placeholder="blur"
  loading="lazy"
/>;
```

**问题三: 布局偏移(CLS)**

原因: 未指定尺寸、动态内容

解决方案:

```typescript
// 为图片指定尺寸
<Image src="/image.jpg" alt="Image" width={800} height={600} />

// 使用骨架屏
<Suspense fallback={<Skeleton height={400} />}>
  <DynamicContent />
</Suspense>

// 固定容器高度
<div style={{ minHeight: '400px' }}>
  <DynamicContent />
</div>
```

**问题四: JavaScript 执行慢**

原因: 包太大、未使用代码分割

解决方案:

```typescript
// 动态导入
const Chart = dynamic(() => import("@/components/Chart"), {
  ssr: false,
});

// 使用 Web Workers
const worker = new Worker(new URL("./worker.ts", import.meta.url));

// 优化导入
import debounce from "lodash/debounce"; // 而不是 import _ from 'lodash'
```

**问题五: 数据获取慢**

原因: 顺序获取、未使用缓存

解决方案:

```typescript
// 并行获取
const [posts, users] = await Promise.all([fetchPosts(), fetchUsers()]);

// 使用缓存
const data = await fetch("/api/data", {
  next: { revalidate: 60 },
});

// 使用流式渲染
<Suspense fallback={<Loading />}>
  <SlowComponent />
</Suspense>;
```

**问题六: 构建时间长**

原因: 依赖多、未使用增量构建

解决方案:

```bash
# 使用 Turbopack
next dev --turbo

# 使用 pnpm
pnpm install

# 缓存构建结果
# 在 CI/CD 中缓存 .next/cache
```

**问题七: 内存泄漏**

原因: 未清理副作用、事件监听器

解决方案:

```typescript
"use client";

import { useEffect } from "react";

function Page() {
  useEffect(() => {
    const handler = () => console.log("resize");
    window.addEventListener("resize", handler);

    // 清理
    return () => {
      window.removeEventListener("resize", handler);
    };
  }, []);

  return <div>Page</div>;
}
```

**问题八: 字体闪烁(FOUT)**

原因: 未优化字体加载

解决方案:

```typescript
// app/layout.tsx
import { Inter } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  preload: true,
});

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
```

**常见问题总结**:

| 问题     | 原因         | 解决方案      |
| :------- | :----------- | :------------ |
| 首屏慢   | 包大、无 SSR | SSR、代码分割 |
| 图片慢   | 未优化       | next/image    |
| 布局偏移 | 无尺寸       | 固定尺寸      |
| JS 慢    | 包大         | 代码分割      |
| 数据慢   | 顺序获取     | 并行、缓存    |
| 构建慢   | 依赖多       | Turbopack     |
| 内存泄漏 | 未清理       | cleanup       |
| 字体闪烁 | 未优化       | next/font     |

### 考察点

- 对常见问题的认识
- 能否快速定位问题
- 是否了解解决方案

## 适用场景

这些性能优化面试题适用于:

1. **Next.js 开发岗位**: 全栈、前端开发
2. **技术面试**: 中级到高级开发者
3. **性能优化**: 专注性能的岗位
4. **架构设计**: 系统架构师

## 注意事项

1. **测量优先**: 先测量再优化
2. **渐进优化**: 从影响最大的开始
3. **权衡取舍**: 性能与开发效率的平衡
4. **持续监控**: 建立性能监控体系
5. **用户体验**: 关注实际用户体验
6. **兼容性**: 注意浏览器兼容性
7. **成本考虑**: 优化成本与收益
8. **文档记录**: 记录优化过程和结果

## 总结

这 10 道性能优化面试题涵盖了:

1. 首屏加载优化
2. 图片优化
3. JavaScript 包大小优化
4. 缓存策略优化
5. Core Web Vitals 优化
6. 数据获取优化
7. 构建性能优化
8. 运行时性能优化
9. 性能监控和分析
10. 常见问题和解决方案

掌握这些知识点可以帮助你:

- 深入理解 Next.js 性能优化
- 实现高性能的 Web 应用
- 优化各项性能指标
- 解决常见性能问题
- 建立性能监控体系
- 通过技术面试
