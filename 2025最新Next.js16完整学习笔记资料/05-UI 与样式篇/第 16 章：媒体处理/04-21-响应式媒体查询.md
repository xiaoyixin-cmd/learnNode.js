**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 响应式媒体查询

## 1. 概述 (Overview)

响应式媒体查询是实现响应式设计的核心技术,通过 CSS 媒体查询和 JavaScript 可以根据设备特性(如屏幕尺寸、分辨率、方向等)调整页面布局和样式。在 Next.js 16 中,我们可以结合服务端渲染和客户端渲染实现更强大的响应式设计。

### 1.1 为什么需要响应式媒体查询

**多设备适配**: 适配手机、平板、桌面等不同设备。

**提升用户体验**: 为不同设备提供最佳体验。

**节省带宽**: 根据设备加载合适的资源。

**SEO 优化**: Google 推荐响应式设计。

### 1.2 媒体查询的类型

**屏幕尺寸**: 根据视口宽度调整布局。

**设备方向**: 横屏和竖屏不同布局。

**分辨率**: 高 DPI 屏幕使用高清图像。

**颜色模式**: 暗黑模式和亮色模式。

**交互方式**: 触摸屏和鼠标不同交互。

### 1.3 Next.js 16 的响应式特性

🆕 **Next.js 16 新增**: 改进的容器查询支持。

⚡ **Next.js 16 增强**: 更好的服务端设备检测。

🆕 **Next.js 16 新增**: 自动响应式图像优化。

---

## 2. CSS 媒体查询 (CSS Media Queries)

### 2.1 基础媒体查询

```css
/* styles/responsive.css */

/* 移动设备 */
@media (max-width: 640px) {
  .container {
    padding: 1rem;
  }
}

/* 平板设备 */
@media (min-width: 641px) and (max-width: 1024px) {
  .container {
    padding: 2rem;
  }
}

/* 桌面设备 */
@media (min-width: 1025px) {
  .container {
    padding: 3rem;
  }
}
```

### 2.2 Tailwind CSS 断点

```tsx
// app/components/ResponsiveLayout.tsx
export default function ResponsiveLayout() {
  return (
    <div
      className="
      px-4 sm:px-6 md:px-8 lg:px-12 xl:px-16
      grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4
      gap-4 sm:gap-6 lg:gap-8
    "
    >
      <div className="bg-blue-500 p-4">项目 1</div>
      <div className="bg-blue-500 p-4">项目 2</div>
      <div className="bg-blue-500 p-4">项目 3</div>
      <div className="bg-blue-500 p-4">项目 4</div>
    </div>
  );
}
```

### 2.3 自定义断点

```js
// tailwind.config.js
module.exports = {
  theme: {
    screens: {
      xs: "475px",
      sm: "640px",
      md: "768px",
      lg: "1024px",
      xl: "1280px",
      "2xl": "1536px",
      "3xl": "1920px",
    },
  },
};
```

---

## 3. 容器查询 (Container Queries)

### 3.1 基础容器查询

```css
/* styles/container.css */

.card-container {
  container-type: inline-size;
  container-name: card;
}

@container card (min-width: 400px) {
  .card {
    display: grid;
    grid-template-columns: 1fr 2fr;
  }
}

@container card (max-width: 399px) {
  .card {
    display: block;
  }
}
```

### 3.2 Tailwind CSS 容器查询

```bash
# 安装容器查询插件
npm install @tailwindcss/container-queries
```

```js
// tailwind.config.js
module.exports = {
  plugins: [require("@tailwindcss/container-queries")],
};
```

```tsx
// app/components/ContainerQuery.tsx
export default function ContainerQuery() {
  return (
    <div className="@container">
      <div className="@sm:grid @sm:grid-cols-2 @lg:grid-cols-3 gap-4">
        <div className="bg-blue-500 p-4">项目 1</div>
        <div className="bg-blue-500 p-4">项目 2</div>
        <div className="bg-blue-500 p-4">项目 3</div>
      </div>
    </div>
  );
}
```

### 3.3 命名容器

```tsx
// app/components/NamedContainer.tsx
export default function NamedContainer() {
  return (
    <div className="@container/card">
      <div className="@[400px]/card:flex @[400px]/card:flex-row">
        <img src="/image.jpg" alt="图片" className="@[400px]/card:w-1/3" />
        <div className="@[400px]/card:w-2/3 p-4">
          <h2>标题</h2>
          <p>内容</p>
        </div>
      </div>
    </div>
  );
}
```

---

## 4. JavaScript 媒体查询 (JavaScript Media Queries)

### 4.1 matchMedia API

```tsx
// app/components/MediaQueryJS.tsx
"use client";

import { useEffect, useState } from "react";

export default function MediaQueryJS() {
  const [isMobile, setIsMobile] = useState(false);

  useEffect(() => {
    const mediaQuery = window.matchMedia("(max-width: 768px)");

    setIsMobile(mediaQuery.matches);

    const handler = (e: MediaQueryListEvent) => {
      setIsMobile(e.matches);
    };

    mediaQuery.addEventListener("change", handler);
    return () => mediaQuery.removeEventListener("change", handler);
  }, []);

  return <div>{isMobile ? <p>移动端布局</p> : <p>桌面端布局</p>}</div>;
}
```

### 4.2 自定义 Hook

```tsx
// app/hooks/useMediaQuery.ts
"use client";

import { useEffect, useState } from "react";

export function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(false);

  useEffect(() => {
    const mediaQuery = window.matchMedia(query);
    setMatches(mediaQuery.matches);

    const handler = (e: MediaQueryListEvent) => {
      setMatches(e.matches);
    };

    mediaQuery.addEventListener("change", handler);
    return () => mediaQuery.removeEventListener("change", handler);
  }, [query]);

  return matches;
}
```

### 4.3 使用自定义 Hook

```tsx
// app/components/ResponsiveComponent.tsx
"use client";

import { useMediaQuery } from "@/hooks/useMediaQuery";

export default function ResponsiveComponent() {
  const isMobile = useMediaQuery("(max-width: 768px)");
  const isTablet = useMediaQuery("(min-width: 769px) and (max-width: 1024px)");
  const isDesktop = useMediaQuery("(min-width: 1025px)");

  return (
    <div>
      {isMobile && <p>移动端</p>}
      {isTablet && <p>平板</p>}
      {isDesktop && <p>桌面端</p>}
    </div>
  );
}
```

---

## 5. 响应式图像 (Responsive Images)

### 5.1 srcset 和 sizes

```tsx
// app/components/ResponsiveImage.tsx
import Image from "next/image";

export default function ResponsiveImage() {
  return (
    <Image
      src="/images/photo.jpg"
      alt="照片"
      width={1200}
      height={800}
      sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
    />
  );
}
```

### 5.2 picture 元素

```tsx
// app/components/PictureElement.tsx
export default function PictureElement() {
  return (
    <picture>
      <source media="(max-width: 768px)" srcSet="/images/mobile.jpg" />
      <source media="(max-width: 1200px)" srcSet="/images/tablet.jpg" />
      <img src="/images/desktop.jpg" alt="响应式图像" />
    </picture>
  );
}
```

### 5.3 艺术指导

```tsx
// app/components/ArtDirection.tsx
"use client";

import { useMediaQuery } from "@/hooks/useMediaQuery";
import Image from "next/image";

export default function ArtDirection() {
  const isMobile = useMediaQuery("(max-width: 768px)");

  return (
    <Image
      src={isMobile ? "/images/mobile.jpg" : "/images/desktop.jpg"}
      alt="响应式图像"
      width={isMobile ? 400 : 1200}
      height={isMobile ? 300 : 800}
    />
  );
}
```

---

## 6. 响应式视频 (Responsive Videos)

### 6.1 流式视频

```tsx
// app/components/FluidVideo.tsx
export default function FluidVideo() {
  return (
    <div className="relative aspect-video">
      <video className="absolute inset-0 w-full h-full" controls>
        <source src="/videos/sample.mp4" type="video/mp4" />
      </video>
    </div>
  );
}
```

### 6.2 多源视频

```tsx
// app/components/MultiSourceVideo.tsx
"use client";

import { useMediaQuery } from "@/hooks/useMediaQuery";

export default function MultiSourceVideo() {
  const isMobile = useMediaQuery("(max-width: 768px)");

  return (
    <video controls width="100%" height="auto">
      <source
        src={isMobile ? "/videos/mobile.mp4" : "/videos/desktop.mp4"}
        type="video/mp4"
      />
    </video>
  );
}
```

### 6.3 响应式 iframe

```tsx
// app/components/ResponsiveIframe.tsx
export default function ResponsiveIframe() {
  return (
    <div className="relative aspect-video">
      <iframe
        className="absolute inset-0 w-full h-full"
        src="https://www.youtube.com/embed/VIDEO_ID"
        title="YouTube视频"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowFullScreen
      />
    </div>
  );
}
```

---

## 7. 设备检测 (Device Detection)

### 7.1 服务端设备检测

```tsx
// app/utils/deviceDetection.ts
import { headers } from "next/headers";

export function getDeviceType() {
  const headersList = headers();
  const userAgent = headersList.get("user-agent") || "";

  const isMobile = /Mobile|Android|iPhone/i.test(userAgent);
  const isTablet = /Tablet|iPad/i.test(userAgent);
  const isDesktop = !isMobile && !isTablet;

  return {
    isMobile,
    isTablet,
    isDesktop,
  };
}
```

```tsx
// app/page.tsx
import { getDeviceType } from "./utils/deviceDetection";

export default function Page() {
  const device = getDeviceType();

  return (
    <div>
      {device.isMobile && <p>移动端</p>}
      {device.isTablet && <p>平板</p>}
      {device.isDesktop && <p>桌面端</p>}
    </div>
  );
}
```

### 7.2 客户端设备检测

```tsx
// app/hooks/useDeviceDetection.ts
"use client";

import { useEffect, useState } from "react";

export function useDeviceDetection() {
  const [device, setDevice] = useState({
    isMobile: false,
    isTablet: false,
    isDesktop: false,
  });

  useEffect(() => {
    const userAgent = navigator.userAgent;
    const isMobile = /Mobile|Android|iPhone/i.test(userAgent);
    const isTablet = /Tablet|iPad/i.test(userAgent);
    const isDesktop = !isMobile && !isTablet;

    setDevice({ isMobile, isTablet, isDesktop });
  }, []);

  return device;
}
```

### 7.3 触摸屏检测

```tsx
// app/hooks/useTouchScreen.ts
"use client";

import { useEffect, useState } from "react";

export function useTouchScreen(): boolean {
  const [isTouchScreen, setIsTouchScreen] = useState(false);

  useEffect(() => {
    const hasTouchScreen =
      "ontouchstart" in window || navigator.maxTouchPoints > 0;
    setIsTouchScreen(hasTouchScreen);
  }, []);

  return isTouchScreen;
}
```

### 7.4 网络状况检测

```tsx
// app/hooks/useNetworkStatus.ts
"use client";

import { useEffect, useState } from "react";

interface NetworkStatus {
  online: boolean;
  effectiveType?: string;
  downlink?: number;
  rtt?: number;
  saveData?: boolean;
}

export function useNetworkStatus(): NetworkStatus {
  const [status, setStatus] = useState<NetworkStatus>({
    online: true,
  });

  useEffect(() => {
    const updateStatus = () => {
      const connection =
        (navigator as any).connection ||
        (navigator as any).mozConnection ||
        (navigator as any).webkitConnection;

      setStatus({
        online: navigator.onLine,
        effectiveType: connection?.effectiveType,
        downlink: connection?.downlink,
        rtt: connection?.rtt,
        saveData: connection?.saveData,
      });
    };

    updateStatus();

    window.addEventListener("online", updateStatus);
    window.addEventListener("offline", updateStatus);

    const connection = (navigator as any).connection;
    if (connection) {
      connection.addEventListener("change", updateStatus);
    }

    return () => {
      window.removeEventListener("online", updateStatus);
      window.removeEventListener("offline", updateStatus);
      if (connection) {
        connection.removeEventListener("change", updateStatus);
      }
    };
  }, []);

  return status;
}
```

### 7.5 电池状态检测

```tsx
// app/hooks/useBatteryStatus.ts
"use client";

import { useEffect, useState } from "react";

interface BatteryStatus {
  level: number;
  charging: boolean;
  chargingTime: number;
  dischargingTime: number;
}

export function useBatteryStatus(): BatteryStatus | null {
  const [battery, setBattery] = useState<BatteryStatus | null>(null);

  useEffect(() => {
    if ("getBattery" in navigator) {
      (navigator as any).getBattery().then((battery: any) => {
        const updateBattery = () => {
          setBattery({
            level: battery.level,
            charging: battery.charging,
            chargingTime: battery.chargingTime,
            dischargingTime: battery.dischargingTime,
          });
        };

        updateBattery();

        battery.addEventListener("levelchange", updateBattery);
        battery.addEventListener("chargingchange", updateBattery);

        return () => {
          battery.removeEventListener("levelchange", updateBattery);
          battery.removeEventListener("chargingchange", updateBattery);
        };
      });
    }
  }, []);

  return battery;
}
```

### 7.6 屏幕尺寸检测

```tsx
// app/hooks/useScreenSize.ts
"use client";

import { useEffect, useState } from "react";

interface ScreenSize {
  width: number;
  height: number;
  availWidth: number;
  availHeight: number;
  colorDepth: number;
  pixelDepth: number;
}

export function useScreenSize(): ScreenSize {
  const [screenSize, setScreenSize] = useState<ScreenSize>({
    width: 0,
    height: 0,
    availWidth: 0,
    availHeight: 0,
    colorDepth: 0,
    pixelDepth: 0,
  });

  useEffect(() => {
    const updateScreenSize = () => {
      setScreenSize({
        width: window.screen.width,
        height: window.screen.height,
        availWidth: window.screen.availWidth,
        availHeight: window.screen.availHeight,
        colorDepth: window.screen.colorDepth,
        pixelDepth: window.screen.pixelDepth,
      });
    };

    updateScreenSize();

    window.addEventListener("resize", updateScreenSize);
    return () => window.removeEventListener("resize", updateScreenSize);
  }, []);

  return screenSize;
}
```

### 7.7 视口尺寸检测

```tsx
// app/hooks/useViewportSize.ts
"use client";

import { useEffect, useState } from "react";

interface ViewportSize {
  width: number;
  height: number;
}

export function useViewportSize(): ViewportSize {
  const [viewportSize, setViewportSize] = useState<ViewportSize>({
    width: 0,
    height: 0,
  });

  useEffect(() => {
    const updateViewportSize = () => {
      setViewportSize({
        width: window.innerWidth,
        height: window.innerHeight,
      });
    };

    updateViewportSize();

    window.addEventListener("resize", updateViewportSize);
    return () => window.removeEventListener("resize", updateViewportSize);
  }, []);

  return viewportSize;
}
```

---

## 8. 适用场景 (Applicable Scenarios)

### 8.1 电商网站

**产品列表**: 移动端单列,平板双列,桌面多列。

```tsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
  {products.map((product) => (
    <ProductCard key={product.id} product={product} />
  ))}
</div>
```

### 8.2 博客网站

**文章布局**: 移动端全宽,桌面端侧边栏。

```tsx
<div className="flex flex-col lg:flex-row gap-8">
  <main className="flex-1">
    <Article />
  </main>
  <aside className="lg:w-80">
    <Sidebar />
  </aside>
</div>
```

### 8.3 仪表板

**数据可视化**: 根据屏幕尺寸调整图表大小。

```tsx
"use client";

import { useMediaQuery } from "@/hooks/useMediaQuery";

export default function Dashboard() {
  const isMobile = useMediaQuery("(max-width: 768px)");

  return <Chart width={isMobile ? 300 : 800} height={isMobile ? 200 : 400} />;
}
```

### 8.4 视频网站

**视频播放器**: 根据设备调整播放器布局。

```tsx
"use client";

import { useMediaQuery } from "@/hooks/useMediaQuery";

export default function VideoPlayer() {
  const isMobile = useMediaQuery("(max-width: 768px)");

  return (
    <div className={`flex ${isMobile ? "flex-col" : "flex-row"} gap-4`}>
      <div className={isMobile ? "w-full" : "w-2/3"}>
        <video controls className="w-full">
          <source src="/videos/sample.mp4" type="video/mp4" />
        </video>
      </div>
      <div className={isMobile ? "w-full" : "w-1/3"}>
        <h2 className="text-xl font-bold">相关视频</h2>
        {/* 相关视频列表 */}
      </div>
    </div>
  );
}
```

### 8.5 社交媒体

**帖子布局**: 移动端单列,桌面端多列。

```tsx
"use client";

import { useMediaQuery } from "@/hooks/useMediaQuery";

export default function PostGrid() {
  const isMobile = useMediaQuery("(max-width: 768px)");
  const isTablet = useMediaQuery("(min-width: 769px) and (max-width: 1024px)");

  const columns = isMobile ? 1 : isTablet ? 2 : 3;

  return (
    <div className={`grid grid-cols-${columns} gap-4`}>
      {posts.map((post) => (
        <PostCard key={post.id} post={post} />
      ))}
    </div>
  );
}
```

### 8.6 新闻网站

**文章阅读**: 移动端全宽,桌面端限制宽度。

```tsx
export default function Article() {
  return (
    <article className="mx-auto px-4 sm:px-6 lg:px-8 max-w-prose">
      <h1 className="text-2xl sm:text-3xl lg:text-4xl font-bold mb-4">
        文章标题
      </h1>
      <div className="prose prose-sm sm:prose lg:prose-lg">
        <p>文章内容...</p>
      </div>
    </article>
  );
}
```

### 8.7 地图应用

**地图视图**: 根据设备调整地图和列表布局。

```tsx
"use client";

import { useMediaQuery } from "@/hooks/useMediaQuery";

export default function MapView() {
  const isMobile = useMediaQuery("(max-width: 768px)");

  return (
    <div className={`flex ${isMobile ? "flex-col" : "flex-row"} h-screen`}>
      <div className={isMobile ? "h-1/2" : "w-2/3"}>
        {/* 地图组件 */}
        <div className="w-full h-full bg-gray-200">地图</div>
      </div>
      <div
        className={isMobile ? "h-1/2 overflow-y-auto" : "w-1/3 overflow-y-auto"}
      >
        {/* 位置列表 */}
        <div className="p-4">
          <h2 className="text-xl font-bold mb-4">附近位置</h2>
          {/* 位置列表项 */}
        </div>
      </div>
    </div>
  );
}
```

### 8.8 表单应用

**表单布局**: 移动端垂直,桌面端水平。

```tsx
export default function ContactForm() {
  return (
    <form className="space-y-4 sm:space-y-0 sm:grid sm:grid-cols-2 sm:gap-4">
      <div>
        <label className="block mb-2">姓名</label>
        <input type="text" className="w-full border rounded px-3 py-2" />
      </div>
      <div>
        <label className="block mb-2">邮箱</label>
        <input type="email" className="w-full border rounded px-3 py-2" />
      </div>
      <div className="sm:col-span-2">
        <label className="block mb-2">消息</label>
        <textarea className="w-full border rounded px-3 py-2" rows={4} />
      </div>
      <div className="sm:col-span-2">
        <button
          type="submit"
          className="w-full sm:w-auto bg-blue-500 text-white px-6 py-2 rounded"
        >
          提交
        </button>
      </div>
    </form>
  );
}
```

---

## 9. 注意事项 (Precautions)

### 9.1 性能考虑

**避免过度查询**: 不要创建太多媒体查询监听器。

**使用防抖**: 窗口大小变化时使用防抖优化性能。

```tsx
"use client";

import { useEffect, useState } from "react";

function useDebounce<T>(value: T, delay: number): T {
  const [debouncedValue, setDebouncedValue] = useState(value);

  useEffect(() => {
    const handler = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);

    return () => clearTimeout(handler);
  }, [value, delay]);

  return debouncedValue;
}
```

### 9.2 SSR 考虑

**避免水合不匹配**: 服务端和客户端渲染结果应该一致。

```tsx
"use client";

import { useEffect, useState } from "react";

export default function ResponsiveSSR() {
  const [isMounted, setIsMounted] = useState(false);
  const [isMobile, setIsMobile] = useState(false);

  useEffect(() => {
    setIsMounted(true);
    setIsMobile(window.innerWidth < 768);
  }, []);

  if (!isMounted) {
    return <div>加载中...</div>;
  }

  return isMobile ? <MobileView /> : <DesktopView />;
}
```

### 9.3 断点选择

**使用标准断点**: 遵循常见设备尺寸。

| 设备 | 断点           |
| ---- | -------------- |
| 手机 | < 640px        |
| 平板 | 640px - 1024px |
| 桌面 | > 1024px       |

### 9.4 测试

**测试多设备**: 在不同设备和浏览器上测试。

**使用 DevTools**: 使用浏览器开发者工具模拟不同设备。

---

## 10. 常见问题 (FAQ)

### 10.1 如何选择断点?

**问题**: 应该使用哪些断点?

**解决方案**: 基于内容而非设备选择断点。

| 断点   | 设备   | Tailwind 类 |
| ------ | ------ | ----------- |
| 640px  | 手机   | sm:         |
| 768px  | 平板   | md:         |
| 1024px | 桌面   | lg:         |
| 1280px | 大屏   | xl:         |
| 1536px | 超大屏 | 2xl:        |

### 10.2 容器查询 vs 媒体查询?

**问题**: 什么时候使用容器查询?

**解决方案**:

| 场景       | 推荐     |
| ---------- | -------- |
| 全局布局   | 媒体查询 |
| 组件内部   | 容器查询 |
| 响应式网格 | 媒体查询 |
| 卡片组件   | 容器查询 |

### 10.3 如何处理 SSR?

**问题**: 服务端渲染时如何处理媒体查询?

**解决方案**: 使用 User-Agent 检测或延迟渲染。

```tsx
// 服务端检测
import { headers } from "next/headers";

export default function Page() {
  const headersList = headers();
  const userAgent = headersList.get("user-agent") || "";
  const isMobile = /Mobile/i.test(userAgent);

  return isMobile ? <MobileView /> : <DesktopView />;
}
```

### 10.4 如何优化性能?

**问题**: 媒体查询影响性能怎么办?

**解决方案**:

1. 减少监听器数量
2. 使用防抖
3. 使用 CSS 媒体查询优先
4. 避免频繁重渲染

```tsx
"use client";

import { useEffect, useState } from "react";

export function useMediaQuery(query: string) {
  const [matches, setMatches] = useState(false);

  useEffect(() => {
    const mediaQuery = window.matchMedia(query);
    setMatches(mediaQuery.matches);

    const handler = (e: MediaQueryListEvent) => {
      setMatches(e.matches);
    };

    // 使用passive选项优化性能
    mediaQuery.addEventListener("change", handler);
    return () => mediaQuery.removeEventListener("change", handler);
  }, [query]);

  return matches;
}
```

### 10.5 如何测试响应式设计?

**问题**: 如何测试不同设备?

**解决方案**:

1. 使用浏览器 DevTools
2. 使用真实设备测试
3. 使用 BrowserStack 等工具
4. 使用 Lighthouse 审计

### 10.6 如何处理图像?

**问题**: 如何为不同设备提供合适的图像?

**解决方案**: 使用 Next.js Image 组件的 sizes 属性。

```tsx
<Image
  src="/photo.jpg"
  alt="照片"
  width={1200}
  height={800}
  sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
/>
```

### 10.7 如何处理打印样式?

**问题**: 如何为打印提供特殊样式?

**解决方案**: 使用 print 媒体查询。

```css
@media print {
  .no-print {
    display: none;
  }

  .print-only {
    display: block;
  }

  body {
    font-size: 12pt;
    color: black;
    background: white;
  }
}
```

```tsx
export default function PrintablePage() {
  return (
    <div>
      <nav className="no-print">{/* 导航栏,打印时隐藏 */}</nav>
      <main>{/* 主要内容 */}</main>
      <div className="print-only">
        {/* 仅在打印时显示 */}
        <p>打印时间: {new Date().toLocaleDateString()}</p>
      </div>
    </div>
  );
}
```

### 10.8 如何处理横竖屏切换?

**问题**: 如何响应设备方向变化?

**解决方案**: 使用 orientation 媒体查询。

```css
@media (orientation: portrait) {
  .container {
    flex-direction: column;
  }
}

@media (orientation: landscape) {
  .container {
    flex-direction: row;
  }
}
```

```tsx
"use client";

import { useEffect, useState } from "react";

export default function OrientationAware() {
  const [isPortrait, setIsPortrait] = useState(true);

  useEffect(() => {
    const mediaQuery = window.matchMedia("(orientation: portrait)");
    setIsPortrait(mediaQuery.matches);

    const handler = (e: MediaQueryListEvent) => {
      setIsPortrait(e.matches);
    };

    mediaQuery.addEventListener("change", handler);
    return () => mediaQuery.removeEventListener("change", handler);
  }, []);

  return (
    <div className={isPortrait ? "flex-col" : "flex-row"}>{/* 内容 */}</div>
  );
}
```

### 10.9 如何处理高 DPI 屏幕?

**问题**: 如何为 Retina 等高 DPI 屏幕提供高清图像?

**解决方案**: 使用 resolution 媒体查询或 Next.js Image 组件。

```css
@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
  .logo {
    background-image: url("/logo@2x.png");
  }
}
```

```tsx
import Image from "next/image";

export default function RetinaImage() {
  return (
    <Image src="/photo.jpg" alt="照片" width={800} height={600} quality={90} />
  );
}
```

### 10.10 如何处理暗黑模式?

**问题**: 如何响应系统暗黑模式?

**解决方案**: 使用 prefers-color-scheme 媒体查询。

```css
@media (prefers-color-scheme: dark) {
  body {
    background: #1a1a1a;
    color: #ffffff;
  }
}

@media (prefers-color-scheme: light) {
  body {
    background: #ffffff;
    color: #000000;
  }
}
```

```tsx
"use client";

import { useEffect, useState } from "react";

export default function DarkModeAware() {
  const [isDark, setIsDark] = useState(false);

  useEffect(() => {
    const mediaQuery = window.matchMedia("(prefers-color-scheme: dark)");
    setIsDark(mediaQuery.matches);

    const handler = (e: MediaQueryListEvent) => {
      setIsDark(e.matches);
    };

    mediaQuery.addEventListener("change", handler);
    return () => mediaQuery.removeEventListener("change", handler);
  }, []);

  return (
    <div className={isDark ? "bg-gray-900 text-white" : "bg-white text-black"}>
      {/* 内容 */}
    </div>
  );
}
```

---

## 11. 总结 (Summary)

### 11.1 核心要点

响应式媒体查询是现代 Web 开发的基础。通过 CSS 媒体查询、容器查询、JavaScript 媒体查询等技术,我们可以为不同设备提供最佳体验。

**CSS 媒体查询**: 简单高效,适合大部分场景。

**容器查询**: 组件级响应式,更灵活。

**JavaScript 查询**: 复杂逻辑,动态调整。

**设备检测**: 服务端优化,提升性能。

### 11.2 技术对比表

| 技术            | 优点        | 缺点           | 适用场景   |
| --------------- | ----------- | -------------- | ---------- |
| CSS 媒体查询    | 性能好,简单 | 全局作用域     | 全局布局   |
| 容器查询        | 组件级,灵活 | 浏览器支持有限 | 组件内部   |
| JavaScript 查询 | 功能强大    | 性能开销       | 复杂逻辑   |
| 设备检测        | SSR 友好    | 不够精确       | 服务端优化 |

### 11.3 优化检查清单

- [ ] 使用移动优先设计
- [ ] 选择合适的断点
- [ ] 使用 Tailwind CSS 简化开发
- [ ] 使用容器查询优化组件
- [ ] 优化图像和视频
- [ ] 处理 SSR 水合问题
- [ ] 测试多设备兼容性
- [ ] 使用防抖优化性能
- [ ] 提供降级方案
- [ ] 监控性能指标

### 11.4 断点建议

| 设备类型 | 断点范围 | Tailwind 前缀 |
| -------- | -------- | ------------- |
| 手机     | < 640px  | 默认          |
| 大手机   | 640px+   | sm:           |
| 平板     | 768px+   | md:           |
| 桌面     | 1024px+  | lg:           |
| 大屏     | 1280px+  | xl:           |
| 超大屏   | 1536px+  | 2xl:          |

### 11.5 最佳实践

**移动优先**:

1. 从小屏幕开始设计
2. 逐步增强到大屏幕
3. 使用 min-width 媒体查询
4. 优化移动端性能

**性能优化**:

1. 使用 CSS 媒体查询优先
2. 减少 JavaScript 查询
3. 使用防抖和节流
4. 避免频繁重渲染

**组件设计**:

1. 使用容器查询
2. 保持组件独立
3. 提供多种布局
4. 测试不同尺寸

**测试策略**:

1. 使用真实设备
2. 使用 DevTools 模拟
3. 测试边界情况
4. 监控性能指标

### 11.6 媒体查询类型对比

| 媒体查询类型 | 语法示例                       | 适用场景    |
| ------------ | ------------------------------ | ----------- |
| 宽度查询     | `(max-width: 768px)`           | 响应式布局  |
| 高度查询     | `(max-height: 600px)`          | 全屏应用    |
| 方向查询     | `(orientation: portrait)`      | 横竖屏适配  |
| 分辨率查询   | `(min-resolution: 2dppx)`      | 高 DPI 屏幕 |
| 颜色模式查询 | `(prefers-color-scheme: dark)` | 暗黑模式    |
| 打印查询     | `@media print`                 | 打印样式    |
| 触摸查询     | `(hover: none)`                | 触摸设备    |

### 11.7 常见断点值

| 设备类型 | 最小宽度 | 最大宽度 | 推荐断点 |
| -------- | -------- | -------- | -------- |
| 小手机   | 320px    | 374px    | 320px    |
| 中手机   | 375px    | 424px    | 375px    |
| 大手机   | 425px    | 767px    | 425px    |
| 平板     | 768px    | 1023px   | 768px    |
| 小桌面   | 1024px   | 1279px   | 1024px   |
| 中桌面   | 1280px   | 1535px   | 1280px   |
| 大桌面   | 1536px+  | -        | 1536px   |

### 11.8 实用工具推荐

**开发工具**:

1. Chrome DevTools - 设备模拟
2. Firefox Responsive Design Mode - 响应式测试
3. BrowserStack - 真实设备测试
4. Responsively App - 多设备预览

**CSS 框架**:

1. Tailwind CSS - 实用优先的 CSS 框架
2. Bootstrap - 流行的响应式框架
3. Material-UI - React 组件库
4. Chakra UI - 可访问的组件库

**测试工具**:

1. Lighthouse - 性能审计
2. WebPageTest - 性能测试
3. GTmetrix - 网站速度测试
4. PageSpeed Insights - Google 性能工具

### 11.9 性能优化建议

**CSS 优化**:

1. 使用 CSS 媒体查询而非 JavaScript
2. 避免过度嵌套的媒体查询
3. 使用 CSS 变量简化主题切换
4. 合并相似的媒体查询

**JavaScript 优化**:

1. 使用防抖和节流
2. 避免频繁的 DOM 操作
3. 使用 requestAnimationFrame
4. 批量更新状态

**图像优化**:

1. 使用 Next.js Image 组件
2. 提供多种图像尺寸
3. 使用现代图像格式(WebP, AVIF)
4. 实现懒加载

**加载优化**:

1. 代码分割
2. 按需加载
3. 预加载关键资源
4. 使用 CDN

### 11.10 无障碍考虑

**键盘导航**:

1. 确保所有交互元素可通过键盘访问
2. 提供清晰的焦点指示
3. 支持 Tab 键导航
4. 实现快捷键

**屏幕阅读器**:

1. 使用语义化 HTML
2. 提供 ARIA 标签
3. 确保内容顺序合理
4. 提供替代文本

**视觉辅助**:

1. 确保足够的颜色对比度
2. 支持文本缩放
3. 避免仅依赖颜色传达信息
4. 提供清晰的错误提示

**触摸友好**:

1. 确保触摸目标足够大(至少 44x44px)
2. 提供足够的间距
3. 支持手势操作
4. 避免悬停依赖

响应式媒体查询是构建现代 Web 应用的核心技术。通过合理使用 CSS 媒体查询、容器查询、JavaScript 查询等技术,我们可以为用户提供最佳的跨设备体验。记住,响应式设计不仅仅是调整布局,更重要的是为不同设备提供最合适的内容和交互方式。始终以用户体验为中心,用数据驱动设计决策。性能优化和无障碍访问同样重要,不应被忽视。通过持续测试和优化,我们可以创建真正适应所有用户和设备的 Web 应用。
