**文档声明**
本资料由「高品质IT资源 / xy769003723321/小苏IT资源铺」独家提供，禁止盗版、转售。

# 数据分页与无限滚动

在处理大量数据时,分页和无限滚动是两种常见的数据加载策略。本文将介绍如何在 Next.js 中实现这两种方案。

## 基础概念

### 分页与无限滚动对比

| 特性     | 传统分页 | 无限滚动 |
| -------- | -------- | -------- |
| 用户体验 | 需要点击 | 自动加载 |
| SEO      | 好       | 一般     |
| 性能     | 可控     | 需要优化 |
| 实现难度 | 低       | 中       |
| 适用场景 | 搜索结果 | 社交媒体 |

### 选择建议

| 场景     | 推荐方案 | 原因                 |
| -------- | -------- | -------------------- |
| 搜索结果 | 传统分页 | 用户需要跳转到特定页 |
| 社交媒体 | 无限滚动 | 持续浏览体验         |
| 电商列表 | 传统分页 | 便于比较和返回       |
| 新闻流   | 无限滚动 | 沉浸式阅读           |

## 实战场景一: 传统分页

### 基础分页实现

```typescript
"use client";

import { useState } from "react";
import useSWR from "swr";

const fetcher = (url: string) => fetch(url).then((res) => res.json());

interface Product {
  id: number;
  name: string;
  price: number;
}

interface PaginatedResponse {
  products: Product[];
  total: number;
  page: number;
  pageSize: number;
  totalPages: number;
}

export default function ProductList() {
  const [page, setPage] = useState(1);
  const pageSize = 10;

  const { data, error, isLoading } = useSWR<PaginatedResponse>(
    `/api/products?page=${page}&pageSize=${pageSize}`,
    fetcher
  );

  if (error) return <div>加载失败</div>;
  if (isLoading) return <div>加载中...</div>;

  return (
    <div>
      <div className="product-grid">
        {data?.products.map((product) => (
          <div key={product.id} className="product-card">
            <h3>{product.name}</h3>
            <p>${product.price}</p>
          </div>
        ))}
      </div>

      <div className="pagination">
        <button
          onClick={() => setPage((p) => Math.max(1, p - 1))}
          disabled={page === 1}
        >
          上一页
        </button>

        <span>
          第 {page} 页 / 共 {data?.totalPages} 页
        </span>

        <button
          onClick={() => setPage((p) => p + 1)}
          disabled={page === data?.totalPages}
        >
          下一页
        </button>
      </div>
    </div>
  );
}
```

### API 路由实现

```typescript
// app/api/products/route.ts
import { NextRequest } from "next/server";

export async function GET(request: NextRequest) {
  const searchParams = request.nextUrl.searchParams;
  const page = parseInt(searchParams.get("page") || "1");
  const pageSize = parseInt(searchParams.get("pageSize") || "10");

  // 从数据库获取数据
  const offset = (page - 1) * pageSize;
  const products = await db.product.findMany({
    skip: offset,
    take: pageSize,
  });

  const total = await db.product.count();

  return Response.json({
    products,
    total,
    page,
    pageSize,
    totalPages: Math.ceil(total / pageSize),
  });
}
```

### 页码导航

```typescript
"use client";

import { useState } from "react";
import useSWR from "swr";

export default function PaginationWithNumbers() {
  const [page, setPage] = useState(1);
  const pageSize = 10;

  const { data } = useSWR(
    `/api/products?page=${page}&pageSize=${pageSize}`,
    fetcher
  );

  const totalPages = data?.totalPages || 1;
  const maxVisible = 5;

  // 计算显示的页码范围
  const getPageNumbers = () => {
    const pages: number[] = [];
    let start = Math.max(1, page - Math.floor(maxVisible / 2));
    let end = Math.min(totalPages, start + maxVisible - 1);

    if (end - start < maxVisible - 1) {
      start = Math.max(1, end - maxVisible + 1);
    }

    for (let i = start; i <= end; i++) {
      pages.push(i);
    }

    return pages;
  };

  return (
    <div className="pagination">
      <button onClick={() => setPage(1)} disabled={page === 1}>
        首页
      </button>

      <button onClick={() => setPage((p) => p - 1)} disabled={page === 1}>
        上一页
      </button>

      {getPageNumbers().map((num) => (
        <button
          key={num}
          onClick={() => setPage(num)}
          className={page === num ? "active" : ""}
        >
          {num}
        </button>
      ))}

      <button
        onClick={() => setPage((p) => p + 1)}
        disabled={page === totalPages}
      >
        下一页
      </button>

      <button
        onClick={() => setPage(totalPages)}
        disabled={page === totalPages}
      >
        末页
      </button>
    </div>
  );
}
```

## 实战场景二: 无限滚动

### 使用 Intersection Observer

```typescript
"use client";

import { useEffect, useRef, useState } from "react";

export default function InfiniteScroll() {
  const [items, setItems] = useState<any[]>([]);
  const [page, setPage] = useState(1);
  const [loading, setLoading] = useState(false);
  const [hasMore, setHasMore] = useState(true);
  const observerTarget = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const observer = new IntersectionObserver(
      (entries) => {
        if (entries[0].isIntersecting && hasMore && !loading) {
          loadMore();
        }
      },
      { threshold: 1.0 }
    );

    if (observerTarget.current) {
      observer.observe(observerTarget.current);
    }

    return () => observer.disconnect();
  }, [hasMore, loading]);

  async function loadMore() {
    setLoading(true);

    try {
      const res = await fetch(`/api/items?page=${page}&pageSize=20`);
      const data = await res.json();

      setItems((prev) => [...prev, ...data.items]);
      setPage((prev) => prev + 1);
      setHasMore(data.hasMore);
    } catch (error) {
      console.error("加载失败:", error);
    } finally {
      setLoading(false);
    }
  }

  return (
    <div>
      <div className="items-grid">
        {items.map((item, index) => (
          <div key={`${item.id}-${index}`} className="item-card">
            <h3>{item.title}</h3>
            <p>{item.description}</p>
          </div>
        ))}
      </div>

      <div ref={observerTarget} className="loading-trigger">
        {loading && <div>加载中...</div>}
        {!hasMore && <div>没有更多数据了</div>}
      </div>
    </div>
  );
}
```

### 使用 React Query

```typescript
"use client";

import { useInfiniteQuery } from "@tanstack/react-query";
import { useInView } from "react-intersection-observer";
import { useEffect } from "react";

async function fetchItems({ pageParam = 1 }) {
  const res = await fetch(`/api/items?page=${pageParam}&pageSize=20`);
  return res.json();
}

export default function InfiniteScrollWithQuery() {
  const { ref, inView } = useInView();

  const { data, fetchNextPage, hasNextPage, isFetchingNextPage, isLoading } =
    useInfiniteQuery({
      queryKey: ["items"],
      queryFn: fetchItems,
      getNextPageParam: (lastPage, pages) =>
        lastPage.hasMore ? pages.length + 1 : undefined,
      initialPageParam: 1,
    });

  useEffect(() => {
    if (inView && hasNextPage && !isFetchingNextPage) {
      fetchNextPage();
    }
  }, [inView, hasNextPage, isFetchingNextPage, fetchNextPage]);

  if (isLoading) return <div>加载中...</div>;

  return (
    <div>
      {data?.pages.map((page, i) => (
        <div key={i} className="page-group">
          {page.items.map((item: any) => (
            <div key={item.id} className="item-card">
              <h3>{item.title}</h3>
              <p>{item.description}</p>
            </div>
          ))}
        </div>
      ))}

      <div ref={ref} className="loading-trigger">
        {isFetchingNextPage && <div>加载更多...</div>}
        {!hasNextPage && <div>没有更多数据了</div>}
      </div>
    </div>
  );
}
```

### 虚拟滚动优化

```typescript
"use client";

import { useVirtualizer } from "@tanstack/react-virtual";
import { useRef } from "react";

export default function VirtualScroll({ items }: { items: any[] }) {
  const parentRef = useRef<HTMLDivElement>(null);

  const virtualizer = useVirtualizer({
    count: items.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 100,
    overscan: 5,
  });

  return (
    <div
      ref={parentRef}
      style={{
        height: "600px",
        overflow: "auto",
      }}
    >
      <div
        style={{
          height: `${virtualizer.getTotalSize()}px`,
          width: "100%",
          position: "relative",
        }}
      >
        {virtualizer.getVirtualItems().map((virtualItem) => (
          <div
            key={virtualItem.key}
            style={{
              position: "absolute",
              top: 0,
              left: 0,
              width: "100%",
              height: `${virtualItem.size}px`,
              transform: `translateY(${virtualItem.start}px)`,
            }}
          >
            <div className="item-card">
              <h3>{items[virtualItem.index].title}</h3>
              <p>{items[virtualItem.index].description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

## 实战场景三: 搜索与过滤

### 带搜索的分页

```typescript
"use client";

import { useState, useEffect } from "react";
import { useDebouncedCallback } from "use-debounce";
import useSWR from "swr";

export default function SearchablePagination() {
  const [page, setPage] = useState(1);
  const [search, setSearch] = useState("");
  const [debouncedSearch, setDebouncedSearch] = useState("");

  const debounced = useDebouncedCallback((value: string) => {
    setDebouncedSearch(value);
    setPage(1); // 重置到第一页
  }, 300);

  useEffect(() => {
    debounced(search);
  }, [search, debounced]);

  const { data, isLoading } = useSWR(
    `/api/products?page=${page}&search=${debouncedSearch}`,
    fetcher
  );

  return (
    <div>
      <input
        type="text"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
        placeholder="搜索产品..."
      />

      {isLoading ? (
        <div>搜索中...</div>
      ) : (
        <>
          <div className="results">
            {data?.products.map((product: any) => (
              <div key={product.id}>{product.name}</div>
            ))}
          </div>

          <div className="pagination">
            <button onClick={() => setPage((p) => p - 1)} disabled={page === 1}>
              上一页
            </button>
            <span>第 {page} 页</span>
            <button
              onClick={() => setPage((p) => p + 1)}
              disabled={!data?.hasMore}
            >
              下一页
            </button>
          </div>
        </>
      )}
    </div>
  );
}
```

### 带过滤的无限滚动

```typescript
"use client";

import { useState } from "react";
import { useInfiniteQuery } from "@tanstack/react-query";
import { useInView } from "react-intersection-observer";

export default function FilteredInfiniteScroll() {
  const [category, setCategory] = useState("all");
  const [priceRange, setPriceRange] = useState<[number, number]>([0, 1000]);
  const { ref, inView } = useInView();

  const { data, fetchNextPage, hasNextPage, isFetchingNextPage } =
    useInfiniteQuery({
      queryKey: ["products", category, priceRange],
      queryFn: ({ pageParam = 1 }) =>
        fetch(
          `/api/products?page=${pageParam}&category=${category}&minPrice=${priceRange[0]}&maxPrice=${priceRange[1]}`
        ).then((res) => res.json()),
      getNextPageParam: (lastPage, pages) =>
        lastPage.hasMore ? pages.length + 1 : undefined,
      initialPageParam: 1,
    });

  useEffect(() => {
    if (inView && hasNextPage) {
      fetchNextPage();
    }
  }, [inView, hasNextPage, fetchNextPage]);

  return (
    <div>
      <div className="filters">
        <select value={category} onChange={(e) => setCategory(e.target.value)}>
          <option value="all">全部分类</option>
          <option value="electronics">电子产品</option>
          <option value="clothing">服装</option>
        </select>

        <div>
          <label>
            价格范围: ${priceRange[0]} - ${priceRange[1]}
          </label>
          <input
            type="range"
            min="0"
            max="1000"
            value={priceRange[0]}
            onChange={(e) =>
              setPriceRange([parseInt(e.target.value), priceRange[1]])
            }
          />
          <input
            type="range"
            min="0"
            max="1000"
            value={priceRange[1]}
            onChange={(e) =>
              setPriceRange([priceRange[0], parseInt(e.target.value)])
            }
          />
        </div>
      </div>

      <div className="products">
        {data?.pages.map((page, i) => (
          <div key={i}>
            {page.products.map((product: any) => (
              <div key={product.id} className="product-card">
                <h3>{product.name}</h3>
                <p>${product.price}</p>
              </div>
            ))}
          </div>
        ))}
      </div>

      <div ref={ref}>{isFetchingNextPage && <div>加载更多...</div>}</div>
    </div>
  );
}
```

## 实战场景四: 性能优化

### 预加载下一页

```typescript
"use client";

import { useState, useEffect } from "react";
import useSWR from "swr";

export default function PrefetchPagination() {
  const [page, setPage] = useState(1);

  // 当前页数据
  const { data } = useSWR(`/api/products?page=${page}`, fetcher);

  // 预加载下一页
  useSWR(`/api/products?page=${page + 1}`, fetcher);

  return (
    <div>
      <div className="products">
        {data?.products.map((product: any) => (
          <div key={product.id}>{product.name}</div>
        ))}
      </div>

      <button onClick={() => setPage((p) => p + 1)}>下一页</button>
    </div>
  );
}
```

### 骨架屏加载

```typescript
"use client";

import { useState } from "react";
import useSWR from "swr";

function ProductSkeleton() {
  return (
    <div className="skeleton">
      <div className="skeleton-image" />
      <div className="skeleton-title" />
      <div className="skeleton-price" />
    </div>
  );
}

export default function SkeletonPagination() {
  const [page, setPage] = useState(1);
  const { data, isLoading } = useSWR(`/api/products?page=${page}`, fetcher);

  return (
    <div>
      <div className="products-grid">
        {isLoading
          ? Array.from({ length: 12 }).map((_, i) => (
              <ProductSkeleton key={i} />
            ))
          : data?.products.map((product: any) => (
              <div key={product.id} className="product-card">
                <img src={product.image} alt={product.name} />
                <h3>{product.name}</h3>
                <p>${product.price}</p>
              </div>
            ))}
      </div>

      <div className="pagination">
        <button onClick={() => setPage((p) => p - 1)} disabled={page === 1}>
          上一页
        </button>
        <button onClick={() => setPage((p) => p + 1)}>下一页</button>
      </div>
    </div>
  );
}
```

### 图片懒加载

```typescript
"use client";

import { useState, useRef, useEffect } from "react";

function LazyImage({ src, alt }: { src: string; alt: string }) {
  const [isLoaded, setIsLoaded] = useState(false);
  const [isInView, setIsInView] = useState(false);
  const imgRef = useRef<HTMLImageElement>(null);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsInView(true);
          observer.disconnect();
        }
      },
      { rootMargin: "50px" }
    );

    if (imgRef.current) {
      observer.observe(imgRef.current);
    }

    return () => observer.disconnect();
  }, []);

  return (
    <div ref={imgRef} className="lazy-image-container">
      {isInView && (
        <img
          src={src}
          alt={alt}
          onLoad={() => setIsLoaded(true)}
          style={{ opacity: isLoaded ? 1 : 0 }}
        />
      )}
      {!isLoaded && <div className="image-placeholder" />}
    </div>
  );
}

export default function LazyLoadList({ items }: { items: any[] }) {
  return (
    <div className="items-grid">
      {items.map((item) => (
        <div key={item.id} className="item-card">
          <LazyImage src={item.image} alt={item.title} />
          <h3>{item.title}</h3>
        </div>
      ))}
    </div>
  );
}
```

## 实战场景五: URL 同步

### 分页状态同步到 URL

```typescript
"use client";

import { useRouter, useSearchParams } from "next/navigation";
import useSWR from "swr";

export default function URLSyncPagination() {
  const router = useRouter();
  const searchParams = useSearchParams();
  const page = parseInt(searchParams.get("page") || "1");

  const { data } = useSWR(`/api/products?page=${page}`, fetcher);

  function setPage(newPage: number) {
    const params = new URLSearchParams(searchParams.toString());
    params.set("page", newPage.toString());
    router.push(`?${params.toString()}`);
  }

  return (
    <div>
      <div className="products">
        {data?.products.map((product: any) => (
          <div key={product.id}>{product.name}</div>
        ))}
      </div>

      <div className="pagination">
        <button onClick={() => setPage(page - 1)} disabled={page === 1}>
          上一页
        </button>
        <span>第 {page} 页</span>
        <button onClick={() => setPage(page + 1)}>下一页</button>
      </div>
    </div>
  );
}
```

### 过滤器同步到 URL

```typescript
"use client";

import { useRouter, useSearchParams } from "next/navigation";
import { useState, useEffect } from "react";
import useSWR from "swr";

export default function FilterURLSync() {
  const router = useRouter();
  const searchParams = useSearchParams();

  const [category, setCategory] = useState(
    searchParams.get("category") || "all"
  );
  const [sort, setSort] = useState(searchParams.get("sort") || "newest");
  const page = parseInt(searchParams.get("page") || "1");

  useEffect(() => {
    const params = new URLSearchParams();
    if (category !== "all") params.set("category", category);
    if (sort !== "newest") params.set("sort", sort);
    if (page !== 1) params.set("page", page.toString());

    router.push(`?${params.toString()}`);
  }, [category, sort, page, router]);

  const { data } = useSWR(
    `/api/products?category=${category}&sort=${sort}&page=${page}`,
    fetcher
  );

  return (
    <div>
      <div className="filters">
        <select value={category} onChange={(e) => setCategory(e.target.value)}>
          <option value="all">全部分类</option>
          <option value="electronics">电子产品</option>
          <option value="clothing">服装</option>
        </select>

        <select value={sort} onChange={(e) => setSort(e.target.value)}>
          <option value="newest">最新</option>
          <option value="price-asc">价格从低到高</option>
          <option value="price-desc">价格从高到低</option>
        </select>
      </div>

      <div className="products">
        {data?.products.map((product: any) => (
          <div key={product.id}>{product.name}</div>
        ))}
      </div>
    </div>
  );
}
```

## 适用场景

| 场景         | 推荐方案 | 原因                   |
| ------------ | -------- | ---------------------- |
| 搜索结果页   | 传统分页 | 用户需要跳转到特定页面 |
| 社交媒体流   | 无限滚动 | 持续浏览体验           |
| 电商产品列表 | 传统分页 | 便于比较和返回         |
| 新闻资讯     | 无限滚动 | 沉浸式阅读             |
| 数据表格     | 传统分页 | 精确定位数据           |
| 图片画廊     | 无限滚动 | 流畅浏览体验           |

## 注意事项

### 1. 避免重复加载

```typescript
"use client";

import { useState, useRef } from "react";

export default function PreventDuplicateLoad() {
  const [items, setItems] = useState<any[]>([]);
  const [loading, setLoading] = useState(false);
  const loadingRef = useRef(false);

  async function loadMore() {
    if (loadingRef.current) return;

    loadingRef.current = true;
    setLoading(true);

    try {
      const res = await fetch("/api/items");
      const data = await res.json();
      setItems((prev) => [...prev, ...data.items]);
    } finally {
      loadingRef.current = false;
      setLoading(false);
    }
  }

  return <div>{/* ... */}</div>;
}
```

### 2. 处理空状态

```typescript
"use client";

import useSWR from "swr";

export default function EmptyState() {
  const { data, isLoading } = useSWR("/api/products", fetcher);

  if (isLoading) return <div>加载中...</div>;

  if (!data || data.products.length === 0) {
    return (
      <div className="empty-state">
        <h2>暂无数据</h2>
        <p>没有找到符合条件的产品</p>
      </div>
    );
  }

  return (
    <div>
      {data.products.map((product: any) => (
        <div key={product.id}>{product.name}</div>
      ))}
    </div>
  );
}
```

### 3. 错误处理

```typescript
"use client";

import { useState } from "react";
import useSWR from "swr";

export default function ErrorHandling() {
  const [page, setPage] = useState(1);
  const { data, error, isLoading } = useSWR(
    `/api/products?page=${page}`,
    fetcher,
    {
      onError: (err) => {
        console.error("加载失败:", err);
      },
    }
  );

  if (error) {
    return (
      <div className="error-state">
        <h2>加载失败</h2>
        <p>{error.message}</p>
        <button onClick={() => window.location.reload()}>重试</button>
      </div>
    );
  }

  if (isLoading) return <div>加载中...</div>;

  return <div>{/* ... */}</div>;
}
```

### 4. 内存管理

```typescript
"use client";

import { useState, useEffect } from "react";

export default function MemoryManagement() {
  const [items, setItems] = useState<any[]>([]);
  const maxItems = 1000;

  async function loadMore() {
    const res = await fetch("/api/items");
    const data = await res.json();

    setItems((prev) => {
      const newItems = [...prev, ...data.items];
      // 只保留最近的1000条
      return newItems.slice(-maxItems);
    });
  }

  return <div>{/* ... */}</div>;
}
```

### 5. SEO 优化

```typescript
// app/products/page.tsx
import { Metadata } from "next";

export async function generateMetadata({
  searchParams,
}: {
  searchParams: { page?: string };
}): Promise<Metadata> {
  const page = parseInt(searchParams.page || "1");

  return {
    title: `产品列表 - 第 ${page} 页`,
    description: `浏览我们的产品列表,第 ${page} 页`,
  };
}

export default async function ProductsPage({
  searchParams,
}: {
  searchParams: { page?: string };
}) {
  const page = parseInt(searchParams.page || "1");
  const data = await fetch(`/api/products?page=${page}`).then((r) => r.json());

  return (
    <div>
      {data.products.map((product: any) => (
        <div key={product.id}>{product.name}</div>
      ))}
    </div>
  );
}
```

## 常见问题

### 1. 如何实现跳转到指定页?

```typescript
"use client";

import { useState } from "react";

export default function JumpToPage() {
  const [page, setPage] = useState(1);
  const [jumpPage, setJumpPage] = useState("");

  function handleJump() {
    const num = parseInt(jumpPage);
    if (num > 0) {
      setPage(num);
      setJumpPage("");
    }
  }

  return (
    <div className="pagination">
      <button onClick={() => setPage((p) => p - 1)}>上一页</button>
      <span>第 {page} 页</span>
      <button onClick={() => setPage((p) => p + 1)}>下一页</button>

      <div className="jump-to">
        <input
          type="number"
          value={jumpPage}
          onChange={(e) => setJumpPage(e.target.value)}
          placeholder="跳转到"
        />
        <button onClick={handleJump}>跳转</button>
      </div>
    </div>
  );
}
```

### 2. 如何实现每页显示数量选择?

```typescript
"use client";

import { useState } from "react";
import useSWR from "swr";

export default function PageSizeSelector() {
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(10);

  const { data } = useSWR(
    `/api/products?page=${page}&pageSize=${pageSize}`,
    fetcher
  );

  function handlePageSizeChange(newSize: number) {
    setPageSize(newSize);
    setPage(1); // 重置到第一页
  }

  return (
    <div>
      <div className="page-size-selector">
        <label>每页显示:</label>
        <select
          value={pageSize}
          onChange={(e) => handlePageSizeChange(parseInt(e.target.value))}
        >
          <option value="10">10</option>
          <option value="20">20</option>
          <option value="50">50</option>
          <option value="100">100</option>
        </select>
      </div>

      <div className="products">
        {data?.products.map((product: any) => (
          <div key={product.id}>{product.name}</div>
        ))}
      </div>
    </div>
  );
}
```

### 3. 无限滚动如何回到顶部?

```typescript
"use client";

import { useState, useEffect } from "react";

export default function ScrollToTop() {
  const [showButton, setShowButton] = useState(false);

  useEffect(() => {
    function handleScroll() {
      setShowButton(window.scrollY > 300);
    }

    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  function scrollToTop() {
    window.scrollTo({ top: 0, behavior: "smooth" });
  }

  return (
    <>
      {/* 内容 */}

      {showButton && (
        <button
          onClick={scrollToTop}
          style={{
            position: "fixed",
            bottom: "20px",
            right: "20px",
            padding: "10px 20px",
          }}
        >
          回到顶部
        </button>
      )}
    </>
  );
}
```

### 4. 如何保存滚动位置?

```typescript
"use client";

import { useEffect } from "react";
import { useRouter } from "next/navigation";

export default function SaveScrollPosition() {
  const router = useRouter();

  useEffect(() => {
    // 恢复滚动位置
    const savedPosition = sessionStorage.getItem("scrollPosition");
    if (savedPosition) {
      window.scrollTo(0, parseInt(savedPosition));
    }

    // 保存滚动位置
    function handleScroll() {
      sessionStorage.setItem("scrollPosition", window.scrollY.toString());
    }

    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  return <div>{/* 内容 */}</div>;
}
```

### 5. 如何实现双向无限滚动?

```typescript
"use client";

import { useState, useEffect, useRef } from "react";

export default function BidirectionalScroll() {
  const [items, setItems] = useState<any[]>([]);
  const [page, setPage] = useState(1);
  const topRef = useRef<HTMLDivElement>(null);
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const topObserver = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting && page > 1) {
        loadPrevious();
      }
    });

    const bottomObserver = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) {
        loadNext();
      }
    });

    if (topRef.current) topObserver.observe(topRef.current);
    if (bottomRef.current) bottomObserver.observe(bottomRef.current);

    return () => {
      topObserver.disconnect();
      bottomObserver.disconnect();
    };
  }, [page]);

  async function loadPrevious() {
    const res = await fetch(`/api/items?page=${page - 1}`);
    const data = await res.json();
    setItems((prev) => [...data.items, ...prev]);
    setPage((p) => p - 1);
  }

  async function loadNext() {
    const res = await fetch(`/api/items?page=${page + 1}`);
    const data = await res.json();
    setItems((prev) => [...prev, ...data.items]);
    setPage((p) => p + 1);
  }

  return (
    <div>
      <div ref={topRef}>加载上一页...</div>
      {items.map((item) => (
        <div key={item.id}>{item.title}</div>
      ))}
      <div ref={bottomRef}>加载下一页...</div>
    </div>
  );
}
```

### 6. 如何处理数据去重?

```typescript
"use client";

import { useState } from "react";

export default function DeduplicateItems() {
  const [items, setItems] = useState<any[]>([]);

  async function loadMore() {
    const res = await fetch("/api/items");
    const data = await res.json();

    setItems((prev) => {
      const combined = [...prev, ...data.items];
      // 使用 Map 去重
      const unique = Array.from(
        new Map(combined.map((item) => [item.id, item])).values()
      );
      return unique;
    });
  }

  return <div>{/* ... */}</div>;
}
```

### 7. 如何实现加载更多按钮?

```typescript
"use client";

import { useState } from "react";

export default function LoadMoreButton() {
  const [items, setItems] = useState<any[]>([]);
  const [page, setPage] = useState(1);
  const [loading, setLoading] = useState(false);
  const [hasMore, setHasMore] = useState(true);

  async function loadMore() {
    setLoading(true);

    try {
      const res = await fetch(`/api/items?page=${page}`);
      const data = await res.json();

      setItems((prev) => [...prev, ...data.items]);
      setPage((prev) => prev + 1);
      setHasMore(data.hasMore);
    } finally {
      setLoading(false);
    }
  }

  return (
    <div>
      <div className="items">
        {items.map((item) => (
          <div key={item.id}>{item.title}</div>
        ))}
      </div>

      {hasMore && (
        <button onClick={loadMore} disabled={loading}>
          {loading ? "加载中..." : "加载更多"}
        </button>
      )}
    </div>
  );
}
```

### 8. 如何实现分页缓存?

```typescript
"use client";

import { useState } from "react";
import useSWR from "swr";

export default function CachedPagination() {
  const [page, setPage] = useState(1);

  // SWR 会自动缓存每一页的数据
  const { data } = useSWR(`/api/products?page=${page}`, fetcher, {
    revalidateOnFocus: false,
    dedupingInterval: 60000, // 60秒内不重复请求
  });

  return (
    <div>
      <div className="products">
        {data?.products.map((product: any) => (
          <div key={product.id}>{product.name}</div>
        ))}
      </div>

      <div className="pagination">
        <button onClick={() => setPage((p) => p - 1)}>上一页</button>
        <button onClick={() => setPage((p) => p + 1)}>下一页</button>
      </div>
    </div>
  );
}
```

### 9. 如何实现服务端分页?

```typescript
// app/products/page.tsx
export default async function ProductsPage({
  searchParams,
}: {
  searchParams: { page?: string };
}) {
  const page = parseInt(searchParams.page || "1");

  const data = await fetch(`${process.env.API_URL}/products?page=${page}`, {
    cache: "no-store",
  }).then((r) => r.json());

  return (
    <div>
      <div className="products">
        {data.products.map((product: any) => (
          <div key={product.id}>{product.name}</div>
        ))}
      </div>

      <div className="pagination">
        <a href={`?page=${page - 1}`}>上一页</a>
        <span>第 {page} 页</span>
        <a href={`?page=${page + 1}`}>下一页</a>
      </div>
    </div>
  );
}
```

### 10. 如何优化大数据量渲染?

使用虚拟滚动:

```typescript
"use client";

import { useVirtualizer } from "@tanstack/react-virtual";
import { useRef } from "react";

export default function VirtualizedList({ items }: { items: any[] }) {
  const parentRef = useRef<HTMLDivElement>(null);

  const virtualizer = useVirtualizer({
    count: items.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 50,
    overscan: 10,
  });

  return (
    <div ref={parentRef} style={{ height: "600px", overflow: "auto" }}>
      <div
        style={{
          height: `${virtualizer.getTotalSize()}px`,
          position: "relative",
        }}
      >
        {virtualizer.getVirtualItems().map((virtualItem) => (
          <div
            key={virtualItem.key}
            style={{
              position: "absolute",
              top: 0,
              left: 0,
              width: "100%",
              height: `${virtualItem.size}px`,
              transform: `translateY(${virtualItem.start}px)`,
            }}
          >
            {items[virtualItem.index].title}
          </div>
        ))}
      </div>
    </div>
  );
}
```

## 总结

数据分页和无限滚动是处理大量数据的两种常见方案。通过本文的学习,我们掌握了:

### 核心技术

1. **传统分页**: 适合需要精确定位的场景
2. **无限滚动**: 适合持续浏览的场景
3. **虚拟滚动**: 优化大数据量渲染
4. **URL 同步**: 保持状态可分享

### 最佳实践

1. **性能优化**: 预加载、懒加载、虚拟滚动
2. **用户体验**: 骨架屏、加载状态、错误处理
3. **SEO 优化**: 服务端渲染、URL 同步
4. **内存管理**: 限制数据量、及时清理

### 实战技巧

1. **搜索与过滤**: 结合分页实现
2. **状态管理**: 使用 SWR 或 React Query
3. **错误处理**: 提供友好的错误提示
4. **缓存策略**: 合理使用缓存减少请求

通过合理选择分页方案,可以构建流畅的数据浏览体验。
